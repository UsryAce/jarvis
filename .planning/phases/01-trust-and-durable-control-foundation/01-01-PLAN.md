---
phase: 01-trust-and-durable-control-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: [KEYS-02]

must_haves:
  truths:
    - "KEYS-02 / D-04: pywin32 is not installed until Ahmed verifies the exact 312 release against official PyPI, documentation, source, and the selected wheel hash."
    - "A rejected or inconclusive dependency check stops all DPAPI implementation plans without substituting another secret runtime."
  artifacts: []
  key_links:
    - from: ".planning/phases/01-trust-and-durable-control-foundation/01-RESEARCH.md"
      to: "requirements.txt"
      via: "blocking package-legitimacy approval consumed by Plan 01-04"
      pattern: "pywin32==312"
---

<objective>
Authorize or reject the exact Windows DPAPI dependency before any package installation.

Purpose: Close the package-legitimacy seam for D-04 and KEYS-02 without silently approving a package whose download metrics were unavailable.
Output: A blocking human decision that Plan 01-04 must receive before changing `requirements.txt` or installing pywin32.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trust-and-durable-control-foundation/01-CONTEXT.md
@.planning/phases/01-trust-and-durable-control-foundation/01-RESEARCH.md
@.planning/phases/01-trust-and-durable-control-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify pywin32 312 package legitimacy</name>
  <files>None — human package-legitimacy verification only</files>
  <read_first>
    - `.planning/phases/01-trust-and-durable-control-foundation/01-RESEARCH.md` — read `Package Legitimacy Audit` and the DPAPI citations.
    - `.planning/phases/01-trust-and-durable-control-foundation/01-CONTEXT.md` — D-04 requires current-user DPAPI through pywin32.
    - `requirements.txt` — confirm the dependency has not already been added or installed by this plan.
  </read_first>
  <action>Review the exact `pywin32==312; sys_platform == "win32"` dependency against the authoritative PyPI project, source repository, documentation, and downloaded wheel identity. Do not install or approve a similarly named package; this blocking checkpoint is the required supply-chain gate for D-04.</action>
  <what-built>No files are changed. This is the mandatory pre-installation legitimacy gate for `pywin32==312; sys_platform == "win32"`.</what-built>
  <how-to-verify>
    1. Open `https://pypi.org/project/pywin32/312/` and confirm project `pywin32`, version `312`, publisher/project history, and a Windows wheel matching the active CPython architecture.
    2. Open `https://github.com/mhammond/pywin32` and `https://mhammond.github.io/pywin32/win32crypt.html`; confirm they identify the same project and expose `win32crypt`/`win32security` used by the plan.
    3. Before installation, download the selected wheel to a temporary directory with `python -m pip download --no-deps --only-binary=:all: --dest <temporary-directory> pywin32==312`; inspect its filename and SHA-256 with `Get-FileHash`. Do not commit the wheel or hash file.
    4. Confirm the research seam's only `SUS` reason is unavailable download metrics, not a registry/source/name mismatch.
  </how-to-verify>
  <verify>
    <automated>python -m pip index versions pywin32</automated>
    <human-check>Approve only after the official PyPI/docs/source and exact downloaded wheel identity match; no package installation occurs in this task.</human-check>
  </verify>
  <acceptance_criteria>
    - Ahmed explicitly responds `approved` before Plan 01-04 may install `pywin32==312`.
    - Any response other than approval leaves `requirements.txt` and the Python environment unchanged and blocks Plan 01-04.
    - No real, test, or canary secret is requested, displayed, logged, or persisted during verification.
  </acceptance_criteria>
  <resume-signal>Type `approved` to authorize the exact pin, or `rejected` with the mismatch found.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- No runtime artifact. This plan produces the blocking approval record consumed by Plan `01-04`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry/source -> local Python environment | Third-party code would gain the current-user process privileges needed for DPAPI and ACL operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `pywin32==312` package selection | mitigate | Blocking human comparison of official PyPI, official source/docs, exact version, wheel identity, and SHA-256 before install. |
| T-01-01 | Spoofing | package/project identity | mitigate | Reject lookalike names, alternate registries, source mismatches, or an unexpected wheel architecture. |
| T-01-02 | Information Disclosure | verification transcript | mitigate | Do not use, request, or print any secret while examining package metadata. |
</threat_model>

<verification>
The plan completes only with explicit human approval; the automated registry listing is supporting evidence and never auto-approves the package.
</verification>

<success_criteria>
- The exact Windows-only dependency is approved or rejected before installation.
- No fallback secret library or runtime replacement is introduced.
- Plan 01-04 is mechanically dependent on this plan.
</success_criteria>

<output>
Create `.planning/phases/01-trust-and-durable-control-foundation/01-01-SUMMARY.md` when done.
</output>
