---
phase: 01-trust-and-durable-control-foundation
plan: 13
type: execute
wave: 8
depends_on: [01-09, 01-12]
files_modified:
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/Dashboard.css
  - frontend/src/index.css
autonomous: true
requirements: [CTRL-01, CTRL-04, CTRL-05, KEYS-01, KEYS-03, KEYS-04, KEYS-05, KEYS-06]

must_haves:
  truths:
    - "Existing dashboard API-key entry opens the protected CredentialManager and legacy optimistic cancel/key-status behavior is removed."
    - "All new controls invoke real authenticated backend capabilities and render safe authoritative revisions/versions/allowed_actions."
    - "The existing 1280x720 JARVIS HUD and GLM 5.2 behavior remain intact beneath the narrow trust surfaces."
  artifacts:
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "protected key-manager launch and authoritative cancellation/status integration"
    - path: "frontend/src/pages/Dashboard.css"
      provides: "dashboard coexistence with fixed trust rail and accessible action sizing"
  key_links:
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/trust/CredentialManager.tsx"
      via: "existing API KEY MANAGER control"
      pattern: "CredentialManager"
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/trust/EmergencyControlRail.tsx"
      via: "shared authoritative control context rather than optimistic cancel toast"
      pattern: "control"
---

<objective>
Integrate the protected trust surfaces into the existing dashboard and remove conflicting optimistic or secret-derived UI behavior.

Purpose: Preserve current dashboard functionality while ensuring every key and stop control projects backend truth rather than legacy local assumptions.
Output: Dashboard wiring and style coexistence passing the checked production build.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trust-and-durable-control-foundation/01-UI-SPEC.md
@frontend/design-qa.md
@frontend/src/pages/Dashboard.tsx
@frontend/src/pages/Dashboard.css
@frontend/src/index.css
@frontend/src/components/trust/CredentialManager.tsx
@frontend/src/components/trust/EmergencyControlRail.tsx
@frontend/src/components/trust/trust.css
@frontend/src/services/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire protected credential controls and retire legacy key status</name>
  <files>frontend/src/pages/Dashboard.tsx</files>
  <read_first>
    - `frontend/src/pages/Dashboard.tsx` — preserve unrelated dashboard/voice/model behavior and replace only trust/key/control seams.
    - `frontend/src/components/trust/CredentialManager.tsx` — mount/use its authoritative state; do not duplicate lifecycle logic.
    - `frontend/src/services/api.ts` — remove any caller expectations for secret-derived `/api/keys/status` fields.
    - `.planning/phases/01-trust-and-durable-control-foundation/01-UI-SPEC.md` — keep this phase to minimum protected credential manager.
  </read_first>
  <action>Connect the existing visible `API KEY MANAGER` control to `CredentialManager`; remove legacy configured/masked prefix/suffix rendering and any assumption that an environment key is the active provider credential. Show only metadata/unknown values supplied by the manager. Preserve existing dashboard routes, voice/chat, model selector and GLM 5.2 preference. Do not add general routing, model health/quota console, audit browser, schedules, approvals, DAG/evidence or Phase 7 panels. Ensure manager close restores focus to its opener and session expiry closes/unmounts it.</action>
  <verify>
    <automated>cd frontend; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The existing key control opens the protected manager and focus returns on close.
    - Dashboard source has no visible key prefix/suffix or raw/config-derived key state.
    - Existing voice/chat/model/dashboard functionality and GLM 5.2 preference remain present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace optimistic cancellation feedback with authoritative control state</name>
  <files>frontend/src/pages/Dashboard.tsx</files>
  <read_first>
    - `frontend/src/pages/Dashboard.tsx` — replace `Agent run cancelled`/`Swarm mission aborted` success toasts and local terminal assignment.
    - `frontend/src/components/trust/EmergencyControlRail.tsx` and `frontend/src/components/trust/TrustBoundary.tsx` — use shared authoritative control revision/status.
    - `frontend/src/services/api.ts` — send expected revision/request ID and reconcile ambiguous/stale results.
    - `.planning/phases/01-trust-and-durable-control-foundation/01-CONTEXT.md` — preserve Phase 1 cooperative stop/residue semantics.
  </read_first>
  <action>Route agent/swarm cancel and global emergency controls through revision-aware control APIs/context. After submit, show `requested`/`accepted`/`stopping` and the prior authoritative runtime state until newer backend evidence arrives; remove immediate terminal success toasts. On stale revision refresh/review without replay. On ambiguous timeout show status unknown and block repeat until reconcile. Surface stopped/partial/unconfirmed and safe audit/correlation IDs inline. Existing per-run UI may display its own runtime status, but must not override the global rail or claim descendant termination. Keep pause/cancel/emergency scopes and backend allowed_actions distinct.</action>
  <verify>
    <automated>cd frontend; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - No cancel/emergency UI reports terminal success from button click or one HTTP acknowledgement.
    - Stale/ambiguous responses cannot duplicate a stop/reset/cancel mutation.
    - Partial/unconfirmed residue remains visible and no Job Object/process-tree evidence is fabricated.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve the validated dashboard composition under trust surfaces</name>
  <files>frontend/src/pages/Dashboard.css, frontend/src/index.css</files>
  <read_first>
    - `frontend/src/pages/Dashboard.css` and `frontend/src/index.css` — preserve existing variables/grid/reference layout and remove only conflicts.
    - `frontend/src/components/trust/trust.css` — keep trust styles scoped with correct z-index/rail geometry.
    - `frontend/design-qa.md` — retain 1280x720 no-overflow evidence and panel proportions.
    - `.planning/phases/01-trust-and-durable-control-foundation/01-UI-SPEC.md` — apply short viewport, narrow, 200% zoom and reduced-motion rules.
  </read_first>
  <action>Resolve style conflicts so the authenticated root is `48px` rail plus `minmax(0,1fr)` dashboard at desktop, with existing dashboard content constrained to the remaining 672px in the 720px reference. Extend compact/narrow/short-height/reduced-motion behavior only as required for the trust surfaces; do not redesign current panels. Ensure legacy tiny telemetry styles never cascade into trust body/critical labels and existing 1280x720 document overflow remains absent. Import `trust.css` once through the protected composition without globally recoloring unrelated routes.</action>
  <verify>
    <automated>cd frontend; npm run build:checked</automated>
  </verify>
  <acceptance_criteria>
    - Checked build passes with no TypeScript or Vite errors.
    - 1280x720 shell retains existing dashboard proportions with a fixed 48px rail and no document overflow.
    - Narrow/short/reduced-motion styles do not clip or hide critical trust actions/copy.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `frontend/src/pages/Dashboard.tsx`: protected `CredentialManager` launch, revision-aware cancel/control feedback and removal of legacy key mask/optimistic terminal copy.
- `frontend/src/pages/Dashboard.css`, `frontend/src/index.css`: trust-rail coexistence, no-overflow and scoped accessibility/responsive adjustments.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy dashboard state -> new trust authority | Existing local state/toasts cannot override durable revisions or metadata. |
| trust CSS -> existing dashboard | Security controls must remain visible/focusable without corrupting current UI behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/registry boundary | mitigate | No dependency installs; checked existing lock/build only. |
| T-01-52 | Information Disclosure | legacy key status/mask | mitigate | Remove env/secret-derived fields and mount metadata-only manager. |
| T-01-53 | Repudiation | optimistic cancel toast/local terminal state | mitigate | Revision-aware accepted/stopping/terminal reconciliation and safe IDs. |
| T-01-54 | Denial of Service | clipped/covered emergency control | mitigate | Fixed rail allocation/z-index, responsive/zoom/no-overflow build and browser gate. |
</threat_model>

<verification>
Run the checked build and source-grep for legacy key mask and optimistic terminal cancel copy. Real-browser visual/interaction evidence is the final Plan 01-14 checkpoint.
</verification>

<success_criteria>
- Existing dashboard integrates the real protected key and control capabilities.
- Legacy secret-derived/optimistic trust behavior is absent.
- The validated JARVIS visual/model behavior remains intact.
</success_criteria>

<output>
Create `.planning/phases/01-trust-and-durable-control-foundation/01-13-SUMMARY.md` when done.
</output>
