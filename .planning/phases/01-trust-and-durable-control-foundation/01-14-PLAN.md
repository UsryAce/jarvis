---
phase: 01-trust-and-durable-control-foundation
plan: 14
type: execute
wave: 9
depends_on: [01-09, 01-10, 01-13]
files_modified:
  - C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Architecture.md
  - C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-07-22-phase-01-trust-foundation.md
autonomous: false
requirements: [CTRL-01, CTRL-04, CTRL-05, KEYS-01, KEYS-02, KEYS-03, KEYS-04, KEYS-05, KEYS-06]

must_haves:
  truths:
    - "All nine Phase 1 requirements and every roadmap security drill pass before any later privileged authority is enabled."
    - "The real browser proves unlock, credential lifecycle, emergency-stop/restart, stale/ambiguous state and secret-free storage/network behavior."
    - "Wrong-user DPAPI, audit tamper, canary, rotation/crash/cache invalidation, NVIDIA safe errors and WAL backup/restore gates are explicitly evidenced, not skipped."
    - "Only after verification, Brain status/refresh and redacted Obsidian architecture/session handoff record safe decisions/evidence without any real or canary secret."
  artifacts:
    - path: "C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Architecture.md"
      provides: "reviewed trust/control/credential architecture decisions after passing verification"
    - path: "C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-07-22-phase-01-trust-foundation.md"
      provides: "redacted Phase 1 session handoff with evidence, risks and next gate"
  key_links:
    - from: "scripts/brain.ps1"
      to: ".planning/graphs/graph.json"
      via: "status then script-owned refresh after verified source changes"
      pattern: "refresh"
    - from: "tests/test_secret_canary.py"
      to: "C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis"
      via: "read-only prohibited-sink scan before redacted handoff"
      pattern: "canary"
---

<objective>
Run the full Phase 1 release/security gate, obtain real Windows/browser verification, then refresh Brain and write the redacted architecture/session handoff.

Purpose: Prevent later privileged capability expansion until every trust, recovery, secret, control and UI claim has objective evidence.
Output: Green automated/drill evidence, explicit human approval, current Brain status and redacted durable handoff notes.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trust-and-durable-control-foundation/01-CONTEXT.md
@.planning/phases/01-trust-and-durable-control-foundation/01-RESEARCH.md
@.planning/phases/01-trust-and-durable-control-foundation/01-VALIDATION.md
@.planning/phases/01-trust-and-durable-control-foundation/01-UI-SPEC.md
@scripts/brain.ps1
@frontend/package.json
@C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Architecture.md
@C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Agent Handoff Protocol.md
@C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/Session Template.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the complete automated security and recovery gate</name>
  <files></files>
  <read_first>
    - `.planning/ROADMAP.md` — use the Phase 1 security gate as the non-negotiable release checklist.
    - `.planning/phases/01-trust-and-durable-control-foundation/01-VALIDATION.md` — execute every mapped test/drill with no silent skips.
    - `tests/test_api_auth_matrix.py`, `tests/test_origin_csrf.py`, `tests/test_control_state.py`, `tests/test_audit_integrity.py`, `tests/test_secret_protector_windows.py`, `tests/test_secret_canary.py`, `tests/test_credential_lifecycle.py`, `tests/test_nvidia_credential_validation.py`, `tests/test_control_migrations_backup.py` — inspect test names/skip behavior before running.
    - `AGENTS.md` — run repository-wide pytest, checked frontend build and Brain status.
  </read_first>
  <action>Run the full suite and targeted drills on isolated generated values/injected transports. Explicitly prove: complete route/auth scope matrix across REST/stream/WS/audio; exact Origin/CSRF/CORS; persistent emergency stop across disconnected frontend/backend restart with new work/recovery blocked; audit payload/digest edit, middle deletion, insertion and reorder detection; canary absence from API/log/audit/stores/frontend/Graphify/Obsidian/Chroma/artifacts; credential add/validate/promote/drain/disable/rotate/revoke with crash injection and generation/cache invalidation; stable NVIDIA 401/403/429/5xx/timeout errors; SQLite 3.45.1 second-owner/manual-checkpoint rejection; online backup and verified restore. Inspect pytest results for skips/xfails: any missing security drill remains a blocker. Confirm no real/provider/unlock/canary value appears in captured output or artifacts. Run repository `python -m pytest -q`, then `npm run build:checked` in frontend. Do not broaden authority on failure.</action>
  <verify>
    <automated>python -m pytest -q; cd frontend; npm run build:checked</automated>
  </verify>
  <acceptance_criteria>
    - Full pytest and checked frontend build exit 0.
    - Every targeted Phase 1 drill passes with no security-relevant skip/xfail.
    - Installed SQLite runtime is either guarded by proven one-owner containment or the phase fails; unsafe multi-connection WAL cannot pass.
    - Test/output/artifact scans contain no real or generated canary/credential/unlock value.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify real browser, wrong-user DPAPI and persistent-stop behavior</name>
  <files>None — human end-to-end verification only</files>
  <read_first>
    - `.planning/phases/01-trust-and-durable-control-foundation/01-UI-SPEC.md` — use Acceptance Evidence and every authoritative/secret-safety rule.
    - `.planning/phases/01-trust-and-durable-control-foundation/01-VALIDATION.md` — perform all manual-only verifications.
    - `frontend/design-qa.md` — preserve and compare the validated 1280x720 dashboard reference.
    - `tests/helpers/dpapi_user_probe.py` — use safe file/exit-code interface; never pass secret via argv/output.
  </read_first>
  <action>Perform the numbered Windows browser, restart, wrong-user DPAPI, credential-lifecycle, and accessibility checks against the automated build. Record every discrepancy and withhold approval until every expected result is observed; Brain and redacted Obsidian handoff updates remain blocked until this checkpoint passes.</action>
  <what-built>Authenticated protected root, durable emergency-control rail, DPAPI credential lifecycle and all automated Phase 1 gates.</what-built>
  <how-to-verify>
    1. In a real browser at 1280x720 and 390x844, verify initial checking -> unlock; no protected content mounts behind the gate. Inspect localStorage, sessionStorage, IndexedDB, Cache Storage, URL/history and network responses: no unlock value, raw key, session/CSRF value, ciphertext or secret-derived mask exists.
    2. Using a generated non-production test credential through an injected/local fixture provider only, add -> validate -> promote/priority -> drain -> disable/rotate -> revoke. Confirm input clears immediately, metadata uses an opaque independent ID, unknown health/quota remains unknown, stale/timeout cases reconcile without replay, failed replacement leaves the old active key, and revoke completes only on newer backend state.
    3. Trigger emergency stop, close the UI, restart backend/supervisor, and confirm the persisted revision loads before protected paint and blocks recovery/new provider/tool work. Confirm the rail shows accepted -> stopping -> stopped or partial/unconfirmed honestly; reset requires re-auth/current revision. Do not require/claim Phase 2 Job Object descendant termination.
    4. Encrypt only a generated test value under the intended scheduled-task/current-user SID; run the safe helper under a distinct real Windows SID/account and confirm decrypt fails with a safe code, then confirm intended-user verified restore succeeds. No value may appear in command arguments or output.
    5. Keyboard-only test unlock, manager, every confirmation, `Ctrl+Shift+.`, Escape/cancel, focus return; verify 200% zoom/320 CSS px, reduced motion, semantic announcements, and operational/stopping/partial/integrity visuals with no critical clipping or color-only state.
  </how_to_verify>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/brain.ps1 status</automated>
    <human-check>Approve only if every real browser, cross-SID DPAPI and disconnected restart condition passes with no secret disclosure or false completion.</human-check>
  </verify>
  <acceptance_criteria>
    - Browser storage/network/DOM inspection is secret-free and protected routes never mount before auth.
    - Full credential and emergency flows reconcile authoritative versions/revisions across stale, timeout, reconnect and restart.
    - A distinct Windows SID cannot decrypt; intended current user can restore/decrypt via safe-code-only procedure.
    - Keyboard, focus, zoom, responsive, contrast, announcements and reduced-motion checks pass the approved UI contract.
  </acceptance_criteria>
  <resume-signal>Type `approved` only when every numbered check passes, or describe the failed gate.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Refresh Brain and write the redacted architecture/session handoff</name>
  <files>C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Architecture.md, C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-07-22-phase-01-trust-foundation.md</files>
  <read_first>
    - `AGENTS.md` and `docs/KNOWLEDGE_SYSTEM.md` — preserve Graphify/Obsidian/runtime responsibilities.
    - `scripts/brain.ps1` — only this script may refresh generated graph files.
    - `C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Architecture.md` — append reviewed trust decisions without copying implementation or sensitive values.
    - `C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Agent Handoff Protocol.md` and `Sessions/Session Template.md` — follow exact handoff sections.
    - `.planning/phases/01-trust-and-durable-control-foundation/01-14-SUMMARY.md` or current verified command output — use evidence only after Task 2 approval.
  </read_first>
  <action>Abort unless Tasks 1 and 2 passed. Run Brain status; because verified source changed, run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/brain.ps1 refresh`, then status again. Never manually edit `.planning/graphs`. Update `Architecture.md` with concise reviewed contracts: opaque DB sessions + exact Origin/CSRF/default-deny protocols; separate single-owner `data/control.db` and affected-runtime containment; current-user DPAPI opaque credentials and staged generation lifecycle; transactional redacted HMAC audit; durable cooperative stop with honest residue and Phase 2 Job Object boundary. Create the session handoff from the template with objective, changed source families, exact verification commands/results, decisions, residual risks (whole-DB rollback, Python zeroization, DPAPI identity portability, cooperative descendant residue), and next safe action. Include paths/safe IDs only. Do not include any real/generated canary/key/unlock/session/CSRF value, ciphertext, digest, raw log/provider body, prompt, or private runtime memory.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/brain.ps1 status; rg -n "opaque|control\.db|DPAPI|emergency|verification|Remaining Risks|Next Safe Action" "C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Architecture.md" "C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-07-22-phase-01-trust-foundation.md"</automated>
  </verify>
  <acceptance_criteria>
    - Brain refresh/status succeeds after verified source changes and generated graph files were not manually edited.
    - Architecture and session notes contain all five reviewed trust contracts, evidence, residual risks and next safe action.
    - Notes contain no secret/canary/session/ciphertext/digest/raw provider/log content.
    - The handoff states Phase 2 descendants/authority remains closed and Phase 6 routing remains out of scope.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- Automated release evidence for all nine requirement/security drills and repository checked build.
- Explicit real-browser/cross-SID/restart human approval record.
- `C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Architecture.md`: reviewed trust-foundation architecture contracts.
- `C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-07-22-phase-01-trust-foundation.md`: redacted Phase 1 handoff following the session template.
- Script-owned refreshed `.planning/graphs/*` output (never manually edited).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/browser evidence -> release authority | Green claims must come from complete non-skipped gates and real behavior. |
| Windows SID -> DPAPI recovery | Same-database backup is not portable secret recovery. |
| verified runtime/source -> Graphify/Obsidian | Derived/shared knowledge must receive only reviewed redacted outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | package/dependency state | mitigate | Confirm exact approved pywin32 version/import in full suite and no later package changes. |
| T-01-55 | Elevation | false-positive release from skipped drill | mitigate | Inspect skips/xfails, require every target matrix plus human browser/cross-SID/restart approval. |
| T-01-56 | Information Disclosure | test/browser/handoff artifacts | mitigate | Generated fixtures/injected transports, storage/network scans and redacted notes with no sensitive values. |
| T-01-57 | Repudiation/Tampering | incomplete audit/recovery evidence | mitigate | Explicit tamper/backup/restore/crash/restart command results in safe handoff. |
| T-01-58 | Information Disclosure | wrong-user DPAPI/backup | mitigate | Real distinct-SID failure plus intended-user restore through safe-code-only helper. |
| T-01-59 | Tampering | manual graph edits/unverified projection | mitigate | Brain script status/refresh only after verification; Obsidian receives reviewed evidence. |
</threat_model>

<verification>
Phase completion requires full pytest, checked frontend build, all targeted automated drills, explicit human approval of browser/cross-SID/restart behavior, then Brain refresh/status and redacted handoff verification.
</verification>

<success_criteria>
- All nine requirement IDs and all roadmap security gates have passing, non-skipped evidence.
- Real browser, DPAPI identity and persistent-stop behavior match the approved contracts.
- No real or canary secret appears in source, output, logs, artifacts, browser storage, graph or vault.
- Brain and Obsidian are updated only after verification and later privileged authority remains closed until this gate passes.
</success_criteria>

<output>
Create `.planning/phases/01-trust-and-durable-control-foundation/01-14-SUMMARY.md` when done.
</output>
