---
phase: 02-capability-policy-and-execution-isolation
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_effect_reconciliation.py
  - tests/test_execution_gateway_integration.py
  - tests/test_execution_sink_inventory.py
autonomous: true
requirements: [CTRL-02, CTRL-03, TOOL-03, TOOL-05, TOOL-07]
must_haves:
  truths:
    - "D-07 and D-16 have crash-point, mismatch, ambiguity, reconciliation, and no-duplicate contracts before storage implementation."
    - "Every caller reaches one sealed gateway and no direct consequential sink survives the cutover."
    - "Receipts and audit events preserve applied truth without raw inputs, output, environment, or exception text."
  artifacts:
    - path: tests/test_effect_reconciliation.py
      provides: durable idempotency and reconciliation state-machine contract
    - path: tests/test_execution_gateway_integration.py
      provides: authenticated proposal-to-receipt contract
    - path: tests/test_execution_sink_inventory.py
      provides: AST and runtime direct-sink guard
  key_links:
    - from: tests/test_effect_reconciliation.py
      to: src/core/capability_store.py
      via: crash-fault and unique-key fixtures
    - from: tests/test_execution_sink_inventory.py
      to: src/core/agent.py
      via: AST/import inventory and monkeypatch sink guards
---

<objective>
Create Wave 0 durable-effect truth and single-gateway bypass contracts for D-07, D-16, and TOOL-07 ownership.

Purpose: Make duplicate effects, unsupported success, audit omissions, and legacy bypasses fail before adapters are implemented.
Output: Effect-reconciliation, end-to-end gateway, and direct-sink inventory suites.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
@src/core/agent_store.py
@src/core/agent.py
@tests/test_dashboard_execution_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce idempotency and reconciliation crash contracts</name>
  <files>tests/test_effect_reconciliation.py</files>
  <read_first>
    - tests/test_effect_reconciliation.py (target; confirm absent before creation)
    - .planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
    - src/core/control_store.py
    - src/core/agent_store.py
    - tests/test_agent_runtime.py
  </read_first>
  <action>Per D-07 and D-16, define exact states reserved, dispatching, applied, not_applied, needs_reconciliation, reconciled_applied, reconciled_not_applied, and reconciliation_failed. Require SHA-256 domain-separated stable action-derived keys, same-key/same-digest idempotence, same-key/different-digest terminal conflict, reservation before dispatch, fencing, and crash points before reserve, after reserve, before dispatch, after remote apply, before receipt, and during reconciliation. Probes are read-only and authoritative; no ambiguous case may dispatch again or claim success. This is Phase 2 adapter contract ownership only; do not add Phase 3 queue, lease, scheduler, or restart orchestration.</action>
  <verify>
    <automated>python -m pytest -q tests/test_effect_reconciliation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every T-06 and T-07 fixture in 02-VALIDATION.md is covered.
    - Same intent never produces more than one dispatch; changed intent never reuses a prior result.
    - Unknown remote truth stays needs_reconciliation and cannot become success.
  </acceptance_criteria>
  <done>The reconciliation suite passes every T-06/T-07 crash case with at most one dispatch and no unsupported success.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the end-to-end gateway and sink inventory contracts</name>
  <files>tests/test_execution_gateway_integration.py, tests/test_execution_sink_inventory.py</files>
  <read_first>
    - tests/test_execution_gateway_integration.py (target; confirm absent before creation)
    - tests/test_execution_sink_inventory.py (target; confirm absent before creation)
    - .planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
    - src/core/agent.py
    - src/api/dashboard_routes.py
    - src/skills/code.py
    - src/skills/files.py
    - src/skills/system.py
    - tests/test_dashboard_execution_gate.py
  </read_first>
  <action>Specify one authenticated flow from trusted request principal through ResolvedAction, PolicyDecision, optional ApprovalVerifier, atomic Reservation, typed fixture Adapter, durable Receipt, and redacted AuditService event. Add AST/import inventory and runtime monkeypatch guards for subprocess, asyncio.create_subprocess_exec, os.startfile, direct filesystem mutation, Git/GitHub CLI, browser launch, and direct adapter imports. Preserve the exact authenticated, Origin/CSRF-aware body-agnostic `/api/code/execute` HTTP 423 response. Require GitHub and autonomous project-write identities to deny as deferred Phase 3/5 authority.</action>
  <verify>
    <automated>python -m pytest -q tests/test_execution_gateway_integration.py tests/test_execution_sink_inventory.py tests/test_dashboard_execution_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - T-24 and T-25 cover every current direct sink named in 02-PATTERNS.md.
    - A fixture action produces one safe receipt and matching audit sequence through one gateway.
    - `/api/code/execute` rejects before body parsing and never reaches any sink.
  </acceptance_criteria>
  <done>The gateway, sink-inventory, and dashboard-gate suites pass; one fixture action yields one safe receipt, and the raw code route remains pre-parse HTTP 423.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/test_effect_reconciliation.py`: durable effect and reconciliation fault matrix.
- `tests/test_execution_gateway_integration.py`: authenticated single-gateway integration contract.
- `tests/test_execution_sink_inventory.py`: direct-sink AST and runtime guard.

<threat_model>
Attacker goals: reuse a key for changed intent, exploit a crash window to duplicate an effect, bypass the gateway, or omit durable/audit evidence.

Trust boundaries: reservation-to-adapter dispatch, remote ambiguity-to-reconciler, and legacy caller-to-gateway.

| Threat IDs | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-06, T-07 | Tampering/Repudiation | mismatch or timeout triggers a second write | mitigate: unique key plus exact digest, durable ambiguity, read-only probe | `tests/test_effect_reconciliation.py` |
| T-24 | Elevation | caller imports or calls an old sink | mitigate: AST inventory, runtime guards, gateway-only dependency | `tests/test_execution_sink_inventory.py` |
| T-25 | Repudiation/Information Disclosure | effect lacks evidence or receipt leaks inputs | mitigate: required state/audit sequence and allowlisted projection | `tests/test_execution_gateway_integration.py` |

Residual risk: fixture adapters cannot prove Windows or browser containment; Plans 02-03, 02-04, 02-10, and 02-11 own those proofs. Any high-severity bypass blocks gateway cutover.
</threat_model>

<verification>
Run the three owned suites plus `tests/test_dashboard_execution_gate.py`; inspect that no unowned external effect, GitHub write, or raw sink is exercised.
</verification>

<success_criteria>
All T-06, T-07, T-24, and T-25 effect-truth and bypass cases exist before durability or gateway production work.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-02-SUMMARY.md` when done.
</output>
