---
phase: 02-capability-policy-and-execution-isolation
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_filesystem_broker_windows.py
  - tests/test_execution_broker_windows.py
  - tests/windows_helpers/process_tree_probe.py
  - tests/windows_helpers/path_swap_probe.py
autonomous: true
requirements: [CTRL-06, TOOL-03]
must_haves:
  truths:
    - "D-09 through D-12 have real Windows hostile contracts before local broker implementation."
    - "A process can never resume before Job Object assignment and containment verification."
    - "Filesystem identity is proven with handles across validation and mutation, including hostile namespace and race cases."
  artifacts:
    - path: tests/test_filesystem_broker_windows.py
      provides: Windows path, reparse, TOCTOU, staging, and evidence contracts
    - path: tests/test_execution_broker_windows.py
      provides: suspended process, resource, output, and descendant cleanup contracts
    - path: tests/windows_helpers/process_tree_probe.py
      provides: safe child/grandchild and bounded residue probe
    - path: tests/windows_helpers/path_swap_probe.py
      provides: safe parent-swap and reparse race fixture
  key_links:
    - from: tests/test_execution_broker_windows.py
      to: src/execution/windows_job.py
      via: assignment-failure and real descendant probes
    - from: tests/test_filesystem_broker_windows.py
      to: src/execution/filesystem.py
      via: handle identity and same-directory staging assertions
---

<objective>
Create Wave 0 Windows filesystem and process-tree containment contracts for D-09 through D-12.

Purpose: Force race-free path enforcement, suspended Job assignment, bounded I/O, minimal environment, and honest residue truth before implementation.
Output: Two Windows integration suites and safe helper probes.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
@src/core/workspaces.py
@src/core/agent.py
@tests/test_workspace_registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce hostile Windows filesystem contracts</name>
  <files>tests/test_filesystem_broker_windows.py, tests/windows_helpers/path_swap_probe.py</files>
  <read_first>
    - tests/test_filesystem_broker_windows.py (target; confirm absent before creation)
    - tests/windows_helpers/path_swap_probe.py (target; confirm absent before creation)
    - .planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
    - src/core/workspaces.py
    - tests/test_workspace_registry.py
  </read_first>
  <action>Per D-12, specify relative canonical-segment input only and reject `..`, absolute, drive-relative, UNC, `\\?\`, `\\.\`, `\??\`, ADS colon, reserved DOS names, trailing dot/space, invalid/control Unicode, symlink, junction, mount, reparse, hard-link write, volume change, and ambiguous casing. Add real temporary-root probes that hold parent/final handles, swap a parent during create, verify final handle identity under the registered root, stage in the same directory, bound and hash bytes, recheck preconditions, and report atomic replace or explicit reconciliation truth. Helpers accept generated paths only and emit stable codes, never file contents.</action>
  <verify>
    <automated>python -m pytest -q tests/test_filesystem_broker_windows.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every T-08 through T-10 fixture is executable on Windows and explicit elsewhere.
    - A string-level `Path.resolve()` check alone cannot satisfy any mutation test.
    - No failed or raced write appears outside the registered temporary grant.
  </acceptance_criteria>
  <done>The filesystem suite passes every T-08 through T-10 case and proves no raced or failed mutation escapes the registered temporary grant.</done>
</task>

<task type="auto">
  <name>Task 2: Produce suspended Job Object and bounded-stream contracts</name>
  <files>tests/test_execution_broker_windows.py, tests/windows_helpers/process_tree_probe.py</files>
  <read_first>
    - tests/test_execution_broker_windows.py (target; confirm absent before creation)
    - tests/windows_helpers/process_tree_probe.py (target; confirm absent before creation)
    - .planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
    - src/core/agent.py
    - tests/test_agent_runtime.py
  </read_first>
  <action>Per D-09 through D-11, require absolute allowlisted executable identity, explicit argv, verified cwd, minimal allowlisted environment, bounded stdin/stdout/stderr/combined bytes, wall deadline, CPU/memory/process caps, and no raw shell contract. Prove create-suspended, configure Job Object, assign and verify membership, then resume; assignment/limit/verification failures terminate the suspended child without observable work. Exercise child and grandchild, breakaway attempt, infinite output, invalid bytes, nonzero exit, timeout, cancellation, emergency stop, backend shutdown, port/lock residue, and classify cleanup only as confirmed, partial, or unconfirmed based on queried job evidence.</action>
  <verify>
    <automated>python -m pytest -q tests/test_execution_broker_windows.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every T-11 through T-16 hostile fixture exists.
    - Assignment failure produces no helper output or file and never resumes the process.
    - Cancellation and all stop paths use one whole-job termination contract and verify descendants.
  </acceptance_criteria>
  <done>The process suite passes every T-11 through T-16 case, assignment failure performs no work, and all stop paths report verified whole-job descendant truth.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/test_filesystem_broker_windows.py`: hostile Windows filesystem suite.
- `tests/test_execution_broker_windows.py`: suspended process and whole-tree cleanup suite.
- `tests/windows_helpers/process_tree_probe.py`: safe descendant/output/resource helper CLI.
- `tests/windows_helpers/path_swap_probe.py`: safe reparse and TOCTOU helper CLI.

<threat_model>
Attacker goals: escape a project through Windows namespaces or reparse races, inject shell semantics, inherit secrets, exhaust the host, or leave descendants alive while receipts claim success.

Trust boundaries: relative action path-to-Windows object identity, typed argv-to-Win32 process creation, and stop signal-to-Job termination evidence.

| Threat IDs | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-08..T-10 | Tampering/Disclosure | namespace, reparse, hard-link, or TOCTOU escape | mitigate: handle walk, held parent, same-volume stage, final identity | filesystem suite |
| T-11, T-12 | Elevation/Disclosure | shell/PATH/env widens grant | mitigate: absolute executable, argv-only, minimal env | process suite |
| T-13..T-16 | DoS/Elevation/Repudiation | resource abuse or surviving descendants | mitigate: pre-resume Job limits, bounded streams, whole-job termination, honest residue | process suite |

Residual risk: real release-machine policy and nested ambient-job behavior require the final manual drill in 02-14. Any high-severity containment failure blocks local adapters.
</threat_model>

<verification>
Run both Windows suites with `-x`; inspect process handles and generated temporary roots after completion, and keep the suites explicit rather than silently skipping on the target Windows release machine.
</verification>

<success_criteria>
All T-08 through T-16 path, process, resource, and descendant failure modes are executable before local broker implementation.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-03-SUMMARY.md` when done.
</output>
