---
phase: 02-capability-policy-and-execution-isolation
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_browser_broker.py
  - tests/fixtures/browser/hostile_pages.py
  - tests/fixtures/browser/fake_egress.py
autonomous: true
requirements: [TOOL-05]
must_haves:
  truths:
    - "D-13 through D-15 have offline hostile contracts before Playwright installation or browser implementation."
    - "A browser context is never treated as the SSRF boundary; actual connected-peer pinning is required for every connection."
    - "Downloads and artifacts remain bounded, quarantined, redacted, and never auto-opened."
  artifacts:
    - path: tests/test_browser_broker.py
      provides: browser isolation, egress, download, artifact, and cleanup contract
    - path: tests/fixtures/browser/hostile_pages.py
      provides: offline redirect, subresource, popup, WebSocket, and download pages
    - path: tests/fixtures/browser/fake_egress.py
      provides: deterministic A/AAAA, peer, rebinding, and scanner fixtures
  key_links:
    - from: tests/test_browser_broker.py
      to: src/browser/egress.py
      via: resolver and connected-peer assertions
    - from: tests/test_browser_broker.py
      to: src/browser/broker.py
      via: future-module and package gates that become strict after 02-06 and 02-11
---

<objective>
Create Wave 0 browser-state, pinned-egress, download, artifact, and cleanup contracts for D-13 through D-15.

Purpose: Keep Playwright from becoming a false SSRF boundary and make every browser effect fail closed until real containment evidence exists.
Output: One exhaustive browser suite plus offline page and egress/scanner fixtures.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
@src/api/dashboard_routes.py
@tests/test_dashboard_execution_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce ephemeral-context and resolver-pinned egress contracts</name>
  <files>tests/test_browser_broker.py, tests/fixtures/browser/hostile_pages.py, tests/fixtures/browser/fake_egress.py</files>
  <read_first>
    - tests/test_browser_broker.py (target; confirm absent before creation)
    - tests/fixtures/browser/hostile_pages.py (target; confirm absent before creation)
    - tests/fixtures/browser/fake_egress.py (target; confirm absent before creation)
    - .planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
    - src/api/dashboard_routes.py
    - tests/test_dashboard_execution_gate.py
  </read_first>
  <action>Per D-13 and D-14, define a fresh non-persistent Chromium context with no storage import, ambient cookies/credentials/extensions/cache, service workers blocked, routes installed before pages, downloads denied, and context-owned cleanup. Cover file/data/custom schemes, userinfo, IDNA/dot/case variants, decimal/hex/octal/mixed IPs, IPv4-in-IPv6, loopback/private/link-local/reserved/metadata, mixed A/AAAA, DNS answer change, redirect chains, iframe, popup, image, script, fetch, WebSocket, and service-worker registration. Require all browser sockets to traverse a run-local proxy/egress broker that resolves once, rejects if any answer is forbidden, connects to the selected vetted IP, preserves original Host/SNI/certificate checks, records peername, and denies when peer identity cannot be proved. Playwright absence remains an explicit package gate owned by 02-05/02-06.</action>
  <verify>
    <automated>python -m pytest -q tests/test_browser_broker.py -x -k "context or egress or redirect or subresource or websocket"</automated>
  </verify>
  <acceptance_criteria>
    - T-17 through T-20 every-hop and ambient-state cases are present.
    - A route callback without connected-peer proof cannot satisfy the suite.
    - Any direct, rebound, bypassed, or unprovable network path stays closed.
  </acceptance_criteria>
  <done>The browser egress tests pass T-17 through T-20 and every direct, rebound, bypassed, or unprovable connection remains closed.</done>
</task>

<task type="auto">
  <name>Task 2: Produce quarantine, artifact-redaction, and cleanup contracts</name>
  <files>tests/test_browser_broker.py, tests/fixtures/browser/hostile_pages.py, tests/fixtures/browser/fake_egress.py</files>
  <read_first>
    - tests/test_browser_broker.py (read Task 1 state before modifying)
    - tests/fixtures/browser/hostile_pages.py (read Task 1 state before modifying)
    - tests/fixtures/browser/fake_egress.py (read Task 1 state before modifying)
    - .planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
    - src/core/agent_store.py
  </read_first>
  <action>Per D-15, add explicit browser.download cases for oversized/chunked/compressed content, partial response, MIME/extension mismatch, traversal/device filename, timeout, malicious scanner verdict, and clean promotion. Require run-owned quarantine, compressed and decompressed caps, streaming hash, type check, scanner, atomic promotion only after clean, and never auto-open/execute. Add bounded secret-canary assertions for screenshot, DOM, HAR, console, accessibility, and download provenance. Close-context tests must prove profile, temporary download, file, port, process, and state cleanup or return partial/unconfirmed.</action>
  <verify>
    <automated>python -m pytest -q tests/test_browser_broker.py -x -k "download or artifact or cleanup"</automated>
  </verify>
  <acceptance_criteria>
    - T-21 through T-23 cases cover limits, scanner, redaction, and residue.
    - No hostile byte reaches a promoted or executable location.
    - Artifact receipts contain only bounded hashes, sizes, types, safe IDs, and redacted references.
  </acceptance_criteria>
  <done>The download/artifact tests pass T-21 through T-23, no hostile bytes are promoted, and all persisted artifact evidence is bounded and redacted.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/test_browser_broker.py`: complete browser isolation and effect suite.
- `tests/fixtures/browser/hostile_pages.py`: offline governed-page fixtures.
- `tests/fixtures/browser/fake_egress.py`: deterministic resolver, peer, transport, scanner, and artifact doubles.

<threat_model>
Attacker goals: inherit operator browser state, reach internal services through alternate URL/DNS paths, exfiltrate through subresources, or land malicious/unbounded bytes.

Trust boundaries: browser context-to-local proxy, proxy resolver-to-connected peer, and network/download bytes-to-quarantine/artifact store.

| Threat IDs | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-17 | Disclosure | ambient profile state crosses context | mitigate: fresh non-persistent context and cleanup proof | browser suite |
| T-18..T-20 | Disclosure/Elevation | URL encoding, redirect, subresource, or DNS rebind reaches private target | mitigate: every-connection proxy, all-answer validation, selected-IP socket and peer proof | egress tests |
| T-21..T-23 | Tampering/DoS/Disclosure | hostile download/artifact escapes limits or leaks canary | mitigate: deny-by-default, quarantine, caps, scan, redaction, residue truth | download/artifact tests |

Residual risk: offline doubles do not prove Chromium artifact identity or live socket behavior; 02-11 and 02-14 keep browser networking closed until the real drill passes.
</threat_model>

<verification>
Run `python -m pytest -q tests/test_browser_broker.py -x`; before Playwright approval, only the explicit package gate may remain, while pure URL/resolver/quarantine contracts must execute.
</verification>

<success_criteria>
All T-17 through T-23 browser, network, download, artifact, and cleanup hostile cases exist before package installation or production browser work.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-04-SUMMARY.md` when done.
</output>
