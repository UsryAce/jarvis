---
phase: 02-capability-policy-and-execution-isolation
plan: 05
type: execute
wave: 1
depends_on: [02-01, 02-04]
files_modified:
  - docs/security/phase-2-package-provenance.md
  - docs/security/phase-2-package-provenance.json
  - scripts/verify_phase2_package_provenance.py
  - tests/test_phase2_package_provenance.py
autonomous: false
requirements: [CTRL-03, TOOL-05]
must_haves:
  truths:
    - "No direct or transitive Python package or browser binary is installed before human legitimacy and exact artifact-hash approval."
    - "The approved identities are exactly cryptography==49.0.0 and playwright==1.61.0 from official PyPI projects and official source organizations."
    - "An executable validator proves the selected Windows CPython wheel closure, dependency edges, tags, filenames, sizes, registry hashes, downloaded-byte hashes, and approval hashes are complete and equal."
    - "Any identity, version, platform, publisher, source, or hash mismatch blocks 02-06 and all dependent approval/browser plans."
  artifacts:
    - path: docs/security/phase-2-package-provenance.md
      provides: non-secret official metadata, selected wheel names, hashes, and human decisions
    - path: docs/security/phase-2-package-provenance.json
      provides: machine-readable exact package and later Chromium provenance/approval manifest
    - path: scripts/verify_phase2_package_provenance.py
      provides: fail-closed closure, dependency-edge, tag, filename, size, and hash validator
    - path: tests/test_phase2_package_provenance.py
      provides: hostile incomplete-closure and provenance-drift contracts
  key_links:
    - from: scripts/verify_phase2_package_provenance.py
      to: docs/security/phase-2-package-provenance.json
      via: load_manifest and verify_python_closure reject missing/extra artifacts, edges, tag drift, size drift, and unequal registry/download/approval SHA-256 values
    - from: docs/security/phase-2-package-provenance.json
      to: requirements.txt
      via: 02-06 requires both explicit APPROVED records before pinning or installing
---

<objective>
Establish the blocking human supply-chain legitimacy and hash gate for cryptography 49.0.0 and Playwright 1.61.0 without installing either package.

Purpose: Prevent suspicious or substituted cryptographic/browser artifacts from entering the privileged runtime.
Output: Machine-readable and human-readable download-only evidence, an executable provenance validator with hostile tests, and two explicit non-auto-approvable human decisions.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
@.planning/phases/01-trust-and-durable-control-foundation/01-01-SUMMARY.md
@requirements.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and run the exact Python artifact provenance validator without installation</name>
  <files>docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json, scripts/verify_phase2_package_provenance.py, tests/test_phase2_package_provenance.py</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md (target; confirm absent before creation)
    - docs/security/phase-2-package-provenance.json (target; confirm absent before creation)
    - scripts/verify_phase2_package_provenance.py (target; confirm absent before creation)
    - tests/test_phase2_package_provenance.py (target; confirm absent before creation)
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
    - .planning/phases/01-trust-and-durable-control-foundation/01-01-SUMMARY.md
    - requirements.txt
  </read_first>
  <behavior>
    - A complete CPython 3.11 Windows x64 wheel closure with every active Requires-Dist edge, compatible wheel/WHEEL tag, exact filename and size, and equal PyPI-registry/downloaded-byte SHA-256 passes in PENDING state.
    - A missing or extra artifact/edge, source distribution, incompatible or inconsistent tag, filename/size drift, duplicate normalized identity, or registry/download/approval hash mismatch returns nonzero with a stable safe reason.
    - PENDING evidence cannot satisfy an APPROVED check, and approval fields cannot be inferred from downloaded metadata.
  </behavior>
  <action>Create a temporary download directory outside the repository and use `python -m pip download --only-binary=:all:` for cryptography==49.0.0 and playwright==1.61.0 matching CPython 3.11 Windows x64 so the complete direct and transitive wheel closure is materialized without installation. Create `docs/security/phase-2-package-provenance.json` as the canonical non-secret manifest with schema/target environment, roots, every normalized name/version, active dependency edge, wheel filename, filename/WHEEL tags, PyPI publisher/source/release URLs, registry filename/size/SHA-256, independently calculated downloaded filename/size/SHA-256, and explicit PENDING approval fields; render the Markdown receipt from that manifest. Implement `load_manifest`, `parse_wheel_artifact`, `read_wheel_metadata`, `derive_active_dependency_edges`, `verify_python_closure`, `verify_chromium_provenance`, and `main` in `scripts/verify_phase2_package_provenance.py`. The validator must derive the closure and edge set from the downloaded wheel METADATA for the exact target environment, require root versions exactly, reject sdists/duplicates/missing/extra artifacts or edges, cross-check filename and WHEEL tags, compare exact filename and size with PyPI JSON, calculate every local SHA-256 itself, and require `approved_sha256 == registry_sha256 == downloaded_sha256` whenever APPROVED is requested. Add hostile fixture tests for each failure class before accepting the complete fixture. Keep every artifact PENDING HUMAN APPROVAL, retain the exact local wheel set only until the two checkpoints finish, and do not run pip install, a Playwright browser installer, or any Chromium executable.</action>
  <verify>
    <automated>python -m pytest -q tests/test_phase2_package_provenance.py -x &amp;&amp; python scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status pending</automated>
  </verify>
  <acceptance_criteria>
    - The complete direct and transitive Windows CPython artifact closure, dependency edges, tags, filenames, sizes, and two-source SHA-256 comparisons are recorded and executable-validator checked.
    - Receipt status remains PENDING and contains no invented approval.
    - Neither package nor Chromium is installed by this task.
  </acceptance_criteria>
  <done>The hostile validator suite passes, the exact downloaded closure validates in PENDING state with complete edges/tags/filenames/sizes and equal registry/download hashes, and the environment contains no newly installed package or Chromium artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact cryptography artifact</name>
  <files>docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Review cryptography==49.0.0 and every transitive wheel it requires against each official PyPI project, official source/publisher identity, dependency edge, selected CPython 3.11 Windows x64 wheel identity, exact filename/size/tags, and matching registry/downloaded SHA-256. This checkpoint is never auto-approvable. On approval, record the human decision and exact approved hash for every artifact in the canonical JSON manifest and regenerate the Markdown receipt; on any missing identity, unresolved edge, or mismatch, leave status rejected and stop.</action>
  <verify>
    <automated>python scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope cryptography --require-status approved</automated>
    <human-check>Confirm every direct/transitive official project/source identity, exact version/platform wheel, dependency edge, and recorded SHA-256; type approved only for the complete exact closure.</human-check>
  </verify>
  <acceptance_criteria>
    - Human-approved exact hashes for cryptography and every transitive artifact are recorded, or the plan remains blocked.
  </acceptance_criteria>
  <done>The checkpoint completes only when the human records approval for every exact cryptography closure artifact; rejection, omission, or mismatch keeps the plan blocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Approve the exact Playwright package and Chromium source contract</name>
  <files>docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Review playwright==1.61.0 and every transitive wheel it requires against each official PyPI project, official source/publisher identity, dependency edge, exact wheel filename/size/tags, and matching registry/downloaded SHA-256. Also approve only the package-declared Chromium source/revision contract; explicitly leave the Chromium archive and executable byte approvals PENDING because those exact bytes do not exist until 02-06 collects them. This checkpoint is never auto-approvable. Record approved Python hashes in the canonical JSON manifest and regenerate the Markdown receipt; on any missing identity, unresolved edge, mismatch, or premature browser-byte approval, reject and stop.</action>
  <verify>
    <automated>python scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope playwright --require-status approved --require-chromium-status absent-or-pending</automated>
    <human-check>Confirm every direct/transitive official project/source identity, exact wheel/dependency/hash, and only the package-declared Chromium source/revision contract; type approved for the Python closure while leaving exact Chromium archive/executable bytes pending.</human-check>
  </verify>
  <acceptance_criteria>
    - Human-approved exact hashes for Playwright and every transitive artifact plus the browser-source constraint are recorded, or the plan remains blocked.
  </acceptance_criteria>
  <done>The checkpoint completes only when the human records approval for every exact Playwright closure artifact and declared Chromium source; rejection, omission, or mismatch keeps the plan blocked.</done>
</task>

</tasks>

## Artifacts this phase produces

- `docs/security/phase-2-package-provenance.json`: canonical exact Python closure plus later Chromium byte provenance and approval state.
- `docs/security/phase-2-package-provenance.md`: human-readable rendering of exact provenance and non-auto-approvable decisions for `cryptography==49.0.0` and `playwright==1.61.0`.
- `scripts/verify_phase2_package_provenance.py`: `load_manifest`, `parse_wheel_artifact`, `read_wheel_metadata`, `derive_active_dependency_edges`, `verify_python_closure`, `verify_chromium_provenance`, `main`.
- `tests/test_phase2_package_provenance.py`: complete-closure positive contract and hostile edge/tag/filename/size/hash/approval drift cases.

<threat_model>
Attacker goals: dependency confusion, typosquat substitution, malicious wheel/browser binary, or forged approval evidence.

Trust boundaries: official registries/source repositories-to-downloaded artifacts and automated evidence-to-human approval.

| Threat ID | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-02-SC | Tampering/Elevation | substituted package or browser executes privileged code | mitigate: official identity correlation, exact version/platform, two-source SHA-256, blocking human gate | receipt plus human checkpoints |

Residual risk: Chromium bytes are not present until 02-06; its exact archive and extracted executable provenance must be collected without execution, human-approved in a second blocking gate, and revalidated before the first Chromium process starts. Any mismatch blocks installation/use and all dependent plans.
</threat_model>

<verification>
Run the hostile provenance tests and validator, inspect the canonical JSON/Markdown receipt and environment to prove no install occurred, and require both human approvals to name exact Python identities and hashes while browser-byte approval remains pending.
</verification>

<success_criteria>
Both SUS packages, every transitive Python artifact, and the declared Chromium source/revision contract have executable-validator-checked exact human-approved public provenance and hashes before any Python installation command can run; exact Chromium bytes remain explicitly pending for 02-06.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-05-SUMMARY.md` when done.
</output>
