---
phase: 02-capability-policy-and-execution-isolation
plan: 05
type: execute
wave: 1
depends_on: [02-01, 02-04]
files_modified:
  - docs/security/phase-2-package-provenance.md
autonomous: false
requirements: [CTRL-03, TOOL-05]
must_haves:
  truths:
    - "No direct or transitive Python package or browser binary is installed before human legitimacy and exact artifact-hash approval."
    - "The approved identities are exactly cryptography==49.0.0 and playwright==1.61.0 from official PyPI projects and official source organizations."
    - "Any identity, version, platform, publisher, source, or hash mismatch blocks 02-06 and all dependent approval/browser plans."
  artifacts:
    - path: docs/security/phase-2-package-provenance.md
      provides: non-secret official metadata, selected wheel names, hashes, and human decisions
  key_links:
    - from: docs/security/phase-2-package-provenance.md
      to: requirements.txt
      via: 02-06 requires both explicit APPROVED records before pinning or installing
---

<objective>
Establish the blocking human supply-chain legitimacy and hash gate for cryptography 49.0.0 and Playwright 1.61.0 without installing either package.

Purpose: Prevent suspicious or substituted cryptographic/browser artifacts from entering the privileged runtime.
Output: Download-only evidence and two explicit non-auto-approvable human decisions.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
@.planning/phases/01-trust-and-durable-control-foundation/01-01-SUMMARY.md
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect official metadata and exact artifact hashes without installation</name>
  <files>docs/security/phase-2-package-provenance.md</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md (target; confirm absent before creation)
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
    - .planning/phases/01-trust-and-durable-control-foundation/01-01-SUMMARY.md
    - requirements.txt
  </read_first>
  <action>Create a temporary download directory outside the repository and use `python -m pip download --only-binary=:all:` for cryptography==49.0.0 and playwright==1.61.0 matching CPython 3.11 Windows x64 so the complete direct and transitive wheel closure is materialized without installation. For every artifact in the resolved closure, correlate exact normalized project name/version, dependency edge, platform tags, PyPI publisher/maintainer links, official source repository, release metadata, wheel filename/size, and SHA-256 from both downloaded bytes and PyPI JSON. Record only public provenance and hashes in the receipt, mark every artifact PENDING HUMAN APPROVAL, retain the exact local artifact set only until the two human checkpoints finish, and do not run pip install or Playwright browser installation.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='docs/security/phase-2-package-provenance.md'; if (!(Test-Path -LiteralPath $p) -or (Select-String -LiteralPath $p -Pattern 'APPROVED' -Quiet)) { exit 1 }; if ((Select-String -LiteralPath $p -Pattern 'cryptography==49.0.0|playwright==1.61.0').Count -lt 2) { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - The complete direct and transitive Windows CPython artifact closure and two-source SHA-256 comparisons are recorded.
    - Receipt status remains PENDING and contains no invented approval.
    - Neither package nor Chromium is installed by this task.
  </acceptance_criteria>
  <done>The provenance receipt contains the complete direct/transitive closure with two-source hashes in PENDING state, and the environment contains no newly installed package or Chromium artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact cryptography artifact</name>
  <files>docs/security/phase-2-package-provenance.md</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Review cryptography==49.0.0 and every transitive wheel it requires against each official PyPI project, official source/publisher identity, dependency edge, selected CPython 3.11 Windows x64 wheel identity, and matching SHA-256. This checkpoint is never auto-approvable. On approval, record the human decision and exact approved hash for every artifact in that closure; on any missing identity, unresolved edge, or mismatch, leave status rejected and stop.</action>
  <verify>
    <automated>python -m pip index versions cryptography</automated>
    <human-check>Confirm every direct/transitive official project/source identity, exact version/platform wheel, dependency edge, and recorded SHA-256; type approved only for the complete exact closure.</human-check>
  </verify>
  <acceptance_criteria>
    - Human-approved exact hashes for cryptography and every transitive artifact are recorded, or the plan remains blocked.
  </acceptance_criteria>
  <done>The checkpoint completes only when the human records approval for every exact cryptography closure artifact; rejection, omission, or mismatch keeps the plan blocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Approve the exact Playwright package and Chromium source contract</name>
  <files>docs/security/phase-2-package-provenance.md</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Review playwright==1.61.0 and every transitive wheel it requires against each official PyPI project, official source/publisher identity, dependency edge, selected wheel identity, and matching SHA-256. Also approve only the Playwright-managed Chromium revision declared by that package; 02-06 must record the downloaded browser archive and executable hashes before use. This checkpoint is never auto-approvable. On any missing identity, unresolved edge, or mismatch, reject and stop.</action>
  <verify>
    <automated>python -m pip index versions playwright</automated>
    <human-check>Confirm every direct/transitive official project/source identity, exact wheel/dependency/hash, and the package-declared Chromium artifact source; type approved only for the complete exact closure.</human-check>
  </verify>
  <acceptance_criteria>
    - Human-approved exact hashes for Playwright and every transitive artifact plus the browser-source constraint are recorded, or the plan remains blocked.
  </acceptance_criteria>
  <done>The checkpoint completes only when the human records approval for every exact Playwright closure artifact and declared Chromium source; rejection, omission, or mismatch keeps the plan blocked.</done>
</task>

</tasks>

## Artifacts this phase produces

- `docs/security/phase-2-package-provenance.md`: complete direct/transitive provenance, artifact hashes, dependency edges, and non-auto-approvable decisions for `cryptography==49.0.0` and `playwright==1.61.0`.

<threat_model>
Attacker goals: dependency confusion, typosquat substitution, malicious wheel/browser binary, or forged approval evidence.

Trust boundaries: official registries/source repositories-to-downloaded artifacts and automated evidence-to-human approval.

| Threat ID | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-02-SC | Tampering/Elevation | substituted package or browser executes privileged code | mitigate: official identity correlation, exact version/platform, two-source SHA-256, blocking human gate | receipt plus human checkpoints |

Residual risk: Chromium bytes are not present until 02-06; their exact executable hash must be appended there before browser imports are enabled. Any mismatch blocks installation and all dependent plans.
</threat_model>

<verification>
Inspect the receipt and environment to prove no install occurred; both human approvals must name exact identities and hashes.
</verification>

<success_criteria>
Both SUS packages, every transitive Python artifact, and the declared Chromium source have exact human-approved public provenance and hashes before any installation command can run.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-05-SUMMARY.md` when done.
</output>
