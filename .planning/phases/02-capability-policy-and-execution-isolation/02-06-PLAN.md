---
phase: 02-capability-policy-and-execution-isolation
plan: 06
type: execute
wave: 2
depends_on: [02-05]
files_modified:
  - requirements.txt
  - docs/security/phase-2-package-provenance.md
  - docs/security/phase-2-package-provenance.json
autonomous: false
requirements: [CTRL-03, TOOL-05]
must_haves:
  truths:
    - "Only human-approved exact package artifacts are installed and pinned."
    - "The exact Chromium archive and extracted executable identity, source/revision, filenames, sizes, and SHA-256 are collected without execution and human-approved before the first Chromium process starts."
    - "The first Chromium smoke launch occurs only after a non-auto-approvable exact-byte checkpoint and immediately revalidates the approved hashes."
    - "Package or browser hash drift fails closed and does not fall back to another version or ambient browser."
  artifacts:
    - path: requirements.txt
      provides: hash-locked approved direct/transitive package closure headed by cryptography 49.0.0 and Playwright 1.61.0
    - path: docs/security/phase-2-package-provenance.md
      provides: installed package and Chromium artifact verification
    - path: docs/security/phase-2-package-provenance.json
      provides: canonical approved Python and exact Chromium archive/executable byte identities
  key_links:
    - from: scripts/verify_phase2_package_provenance.py
      to: docs/security/phase-2-package-provenance.json
      via: every install/collection/launch transition fails unless current bytes and required approval state match
    - from: docs/security/phase-2-package-provenance.json
      to: requirements.txt
      via: exact Python pins and local wheel paths must match approved hashes
---

<objective>
Install and lock only the previously approved Python artifacts, collect exact Chromium archive/executable provenance without execution, require human approval of those exact bytes, and only then smoke-launch Chromium.

Purpose: Supply the approved cryptographic and browser primitives without widening the dependency boundary.
Output: Exact requirements pins, canonical installed-artifact receipt, blocking Chromium byte approval, and post-approval smoke evidence.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/security/phase-2-package-provenance.md
@docs/security/phase-2-package-provenance.json
@scripts/verify_phase2_package_provenance.py
@requirements.txt
@.planning/phases/02-capability-policy-and-execution-isolation/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install approved Python wheels offline and collect Chromium bytes without execution</name>
  <files>requirements.txt, docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json</files>
  <read_first>
    - requirements.txt
    - docs/security/phase-2-package-provenance.md
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-05-SUMMARY.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Abort unless `verify_python_closure` confirms explicit human APPROVED entries for the complete exact direct/transitive closures of cryptography==49.0.0 and playwright==1.61.0, including dependency edges, tags, filenames, sizes, and equality of approved/registry/download SHA-256 values. Add hash-locked direct and transitive pins to requirements.txt without weakening existing pins. Re-download the exact approved wheels into one temporary directory, revalidate the complete set, disable index/network access, and install only explicit verified local wheel paths with `python -m pip install --no-index --no-deps`; no resolver, alternate wheel, source distribution, ambient substitute, or browser installer may participate. Then read the installed Playwright package's own browser metadata to resolve its declared Windows x64 Chromium source/revision. Without invoking `python -m playwright install`, Playwright's driver/install helper, `BrowserType.launch`, subprocess, `os.startfile`, or any Chromium executable, download the exact archive with a bounded standard-library HTTPS collector, record requested/final official source URL and revision, archive filename/size/calculated SHA-256, safely extract it into a retained non-executable staging directory with traversal/link rejection, locate the declared executable, and record its relative filename/path, size, and calculated SHA-256. Set archive and executable approval fields to `PENDING_EXACT_BYTE_APPROVAL`, regenerate the Markdown receipt, and preserve those exact staged bytes unchanged until Task 2. Any source/revision/filename/size/hash ambiguity or change stops before browser execution.</action>
  <verify>
    <automated>python scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed &amp;&amp; python scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope chromium --require-status pending --verify-current-bytes --no-execute</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt hash-locks the complete approved direct/transitive set, with each exact pin present once.
    - Installed distributions match every approved name, version, dependency edge, wheel identity, filename, size, and artifact hash with offline `--no-index --no-deps` installation.
    - Exact Chromium source/revision, archive filename/size/SHA-256, and extracted executable filename/size/SHA-256 are recorded as PENDING while no Chromium process or browser install helper has run.
  </acceptance_criteria>
  <done>Requirements and installed Python distributions match the complete approved closure, exact Chromium archive/executable bytes validate in retained PENDING state, and collection started no Chromium process.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact collected Chromium archive and executable bytes</name>
  <files>docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Review the exact retained archive and extracted executable collected by Task 1 against the previously approved package-declared Chromium source/revision. Confirm requested/final official source URL, revision, archive filename, archive byte size, archive SHA-256, executable relative filename/path, executable byte size, and executable SHA-256. This checkpoint is never auto-approvable and occurs before the first Chromium process starts. On approval, record the human decision, timestamp, and both exact approved SHA-256 values in the canonical JSON manifest and regenerate the Markdown receipt; on missing data, source/revision drift, byte drift, or ambiguity, reject and stop. Do not invoke any browser installer, Playwright launch API, Chromium executable, or smoke test during this checkpoint.</action>
  <verify>
    <automated>python scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope chromium --require-status approved --verify-current-bytes --no-execute</automated>
    <human-check>Approve only the displayed package-declared source/revision and the exact retained archive/executable filenames, sizes, and SHA-256 values; confirm no Chromium process has started.</human-check>
  </verify>
  <acceptance_criteria>
    - Human approval names the exact source/revision plus archive and executable filenames, sizes, and SHA-256 values.
    - Validator rehashes the retained bytes and proves both approved hashes match before any process starts.
  </acceptance_criteria>
  <done>The checkpoint completes only when exact retained Chromium archive and executable bytes are human-approved and validator-confirmed without executing them; rejection, omission, or drift keeps the plan blocked.</done>
</task>

<task type="auto">
  <name>Task 3: Stage and smoke-launch only the approved Chromium executable</name>
  <files>docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - requirements.txt
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Only after Task 2 approval, re-run the provenance validator over the retained archive and executable before any launch. Offline-stage the already approved extracted tree into the explicit Phase 2 `PLAYWRIGHT_BROWSERS_PATH` without network, dependency resolution, a browser installer helper, or alternate download; rehash the staged executable and require equality with the approved executable SHA-256. Obtain the executable path only through `approved_chromium_executable` from the canonical manifest, require its identity to match the package-declared Chromium revision, and pass it explicitly to Playwright so system Edge/Chrome, an ambient browser cache, or an operator profile cannot be selected. Then and only then perform the first headless launch/new-context/close smoke test with no navigation, `accept_downloads=False`, `service_workers='block'`, no storage import, and external network denied. Record post-stage hash equality and smoke outcome in both receipts. Any mismatch stops before launch and cannot fall back.</action>
  <verify>
    <automated>python scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope chromium --require-status approved --verify-current-bytes --no-execute &amp;&amp; python -c "from scripts.verify_phase2_package_provenance import approved_chromium_executable,load_manifest,verify_chromium_provenance; m=load_manifest('docs/security/phase-2-package-provenance.json'); verify_chromium_provenance(m,require_status='approved',verify_current_bytes=True); from playwright.sync_api import sync_playwright; p=sync_playwright().start(); b=p.chromium.launch(headless=True,executable_path=str(approved_chromium_executable(m))); c=b.new_context(accept_downloads=False,service_workers='block'); c.close(); b.close(); p.stop()"</automated>
  </verify>
  <acceptance_criteria>
    - The exact approved executable hash still matches after offline staging and immediately before first launch.
    - A no-network ephemeral context opens and closes successfully only after the byte-approval checkpoint.
    - No browser installer, ambient browser/cache/profile fallback, alternate archive, or network resolution is configured.
  </acceptance_criteria>
  <done>The exact approved Chromium executable is offline-staged, rehashed, and smoke-launched only after human byte approval; the no-network ephemeral context passes and no fallback exists.</done>
</task>

</tasks>

## Artifacts this phase produces

- `requirements.txt`: exact hash-locked direct/transitive pins for the approved `cryptography==49.0.0` and `playwright==1.61.0` dependency closures.
- `docs/security/phase-2-package-provenance.json`: canonical approved Python closure, collected/approved Chromium archive and executable byte identities, and post-stage validation state.
- `docs/security/phase-2-package-provenance.md`: human-readable installed distribution, exact Chromium byte approval, and post-approval smoke evidence.

<threat_model>
Attacker goals: substitute a dependency after approval, drift to a newer version, or route automation through an unapproved ambient browser.

Trust boundaries: approval receipt-to-package installer and Playwright package-to-browser executable.

| Threat ID | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-02-SC | Tampering/Elevation | installer resolves different bytes/version/browser or Chromium starts before exact-byte approval | mitigate: executable receipt validator, exact pins, offline no-deps install, non-executing archive/executable collection, blocking exact-byte human gate, pre-launch rehash | validator, checkpoint, and smoke commands |

Residual risk: package legitimacy does not prove safe application composition. Approval and browser plans remain blocked by their hostile suites and final real release drills.
</threat_model>

<verification>
Run all three task commands in order, inspect exact pins and canonical receipt hashes/approval timestamps, prove the first Chromium launch follows the blocking byte checkpoint, then run `python -m pytest -q tests/test_approval_envelope.py tests/test_browser_broker.py -x` to activate package-gated contracts.
</verification>

<success_criteria>
Only the complete approved direct/transitive Python artifact set is installed offline; exact Chromium archive/executable bytes are collected without execution, human-approved, rehashed, and only then smoke-launched for downstream use.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-06-SUMMARY.md` when done.
</output>
