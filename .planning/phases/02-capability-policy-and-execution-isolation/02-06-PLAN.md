---
phase: 02-capability-policy-and-execution-isolation
plan: 06
type: execute
wave: 2
depends_on: [02-05]
files_modified:
  - requirements.txt
  - docs/security/phase-2-package-provenance.md
  - docs/security/phase-2-package-provenance.json
  - scripts/bootstrap_phase2_runtime_binding.py
  - scripts/verify_phase2_package_provenance.py
  - tests/test_phase2_package_provenance.py
autonomous: false
requirements: [CTRL-03, TOOL-05]
must_haves:
  truths:
    - "Only human-approved exact package artifacts are installed and pinned."
    - "The production runtime is evidence-bound to `C:/Users/Usry/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe` and packages install only to `C:/Users/Usry/AppData/Local/Jarvis/phase-2/python-site`; its prefix/platlib/Scripts identities are recorded before installation and ambient `python`, `py`, PATH, or user-site selection is forbidden."
    - "Before the approved-target runner or any third-party module is imported, a repository-owned standard-library-only bootstrap runs under `runtime_binding.executable -I -S`, proves the exact interpreter/isolation identity and its own manifest-recorded repository path/SHA-256, and rejects user-site, site, PYTHONPATH, cwd, link/reparse, or source-hash influence."
    - "A stdlib-first approved-target activation/attestation API and CLI runs under the manifest-bound absolute interpreter with `-I`, adds only the recorded package target, and rejects every required module or distribution whose canonical origin is outside that target."
    - "After the approved-target runner exists, the complete hostile provenance/activation suite (apart from the separately gated post-approval Chromium smoke case) passes through that runner with installed-closure plus cryptography and Playwright origin attestation before Task 1 can pass."
    - "Chromium pending/approved archive and executable records, installed Python distributions, current byte hashes, and no-execute evidence are hostile-tested validator modes before installation or launch."
    - "The exact Chromium archive and extracted executable identity, source/revision, filenames, sizes, and SHA-256 are collected without execution and human-approved before the first Chromium process starts."
    - "The first Chromium smoke launch occurs only after a non-auto-approvable exact-byte checkpoint and immediately revalidates the approved hashes."
    - "Package or browser hash drift fails closed and does not fall back to another version or ambient browser."
  artifacts:
    - path: requirements.txt
      provides: hash-locked approved direct/transitive package closure headed by cryptography 49.0.0 and Playwright 1.61.0
    - path: docs/security/phase-2-package-provenance.md
      provides: installed package and Chromium artifact verification
    - path: docs/security/phase-2-package-provenance.json
      provides: canonical approved Python and exact Chromium archive/executable byte identities
    - path: scripts/bootstrap_phase2_runtime_binding.py
      provides: standard-library-only exact interpreter, isolation-state, repository-path, and bootstrap-source-hash check before the approved-target runner exists
    - path: scripts/verify_phase2_package_provenance.py
      provides: exact interpreter, installed-distribution, approved-target activation/import attestation, Chromium current-byte, no-execute, pending/approved, and executable-path validation modes
    - path: tests/test_phase2_package_provenance.py
      provides: hostile interpreter ambiguity and Chromium archive/executable drift contracts
  key_links:
    - from: scripts/verify_phase2_package_provenance.py
      to: docs/security/phase-2-package-provenance.json
      via: every install/collection/launch transition fails unless current bytes and required approval state match
    - from: docs/security/phase-2-package-provenance.json
      to: requirements.txt
      via: exact Python pins and local wheel paths must match approved hashes
    - from: docs/security/phase-2-package-provenance.json
      to: bound CPython executable and install root
      via: every validator, pip, import, smoke, and Playwright command uses the recorded absolute runtime binding rather than shell resolution
    - from: scripts/verify_phase2_package_provenance.py
      to: C:/Users/Usry/AppData/Local/Jarvis/phase-2/python-site
      via: activate_approved_python_target rejects ambient/preloaded imports, explicitly installs the canonical target on sys.path without PYTHONPATH or .pth processing, and attests module/distribution origins beneath that target
    - from: scripts/bootstrap_phase2_runtime_binding.py
      to: docs/security/phase-2-package-provenance.json
      via: the bootstrap derives its canonical repository root from __file__, requires its manifest-recorded path and SHA-256, and validates runtime_binding under -I -S without importing the provenance runner or third-party code
---

<objective>
Extend the provenance validator with hostile-tested interpreter, installed-distribution, approved-target activation/import-attestation, and Chromium byte modes; bind one exact CPython/install location; install and lock only the previously approved Python artifacts; collect exact Chromium archive/executable provenance without execution; require human approval of those exact bytes; and only then smoke-launch Chromium.

Purpose: Supply the approved cryptographic and browser primitives without widening the dependency boundary.
Output: Standard-library bootstrap attestation, exact runtime/install binding, approved-target validator/runner modes and hostile tests, requirements pins, canonical installed-artifact receipt, blocking Chromium byte approval, and post-approval smoke evidence.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/security/phase-2-package-provenance.md
@docs/security/phase-2-package-provenance.json
@scripts/verify_phase2_package_provenance.py
@requirements.txt
@.planning/phases/02-capability-policy-and-execution-isolation/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove one Python target, complete Chromium validator modes, then install and collect without execution</name>
  <files>requirements.txt, docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json, scripts/bootstrap_phase2_runtime_binding.py, scripts/verify_phase2_package_provenance.py, tests/test_phase2_package_provenance.py</files>
  <read_first>
    - requirements.txt
    - docs/security/phase-2-package-provenance.md
    - docs/security/phase-2-package-provenance.json
    - scripts/bootstrap_phase2_runtime_binding.py (target; confirm absent before creation)
    - scripts/verify_phase2_package_provenance.py
    - tests/test_phase2_package_provenance.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-05-SUMMARY.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <behavior>
    - The validator accepts `--scope chromium` with pending or approved status, `--verify-current-bytes`, `--no-execute`, and Python `--verify-installed` only when the exact canonical manifest records and current bytes/distributions agree.
    - A missing/outside/link-swapped archive or executable, size/hash/path/source/revision drift, premature approval, missing no-execute evidence, installed distribution outside the bound root, or interpreter/version/prefix/ABI ambiguity returns one stable non-secret failure before install or launch.
    - `scripts/bootstrap_phase2_runtime_binding.py` uses only the standard library, runs only with the manifest interpreter under `-I -S`, validates `sys.flags`/`sys.path`/stdlib roots and exact executable/version/prefix/ABI identity, verifies its canonical repository-relative path and SHA-256 against `python.runtime_binding.bootstrap`, and fails if site, pytest, packaging, cryptography, Playwright, Pydantic, user-site, PYTHONPATH, cwd, or an unrecorded/link-swapped source can influence it.
    - `activate_approved_python_target` and its CLI mode run only when `sys.executable` equals the manifest-bound canonical executable, explicitly prepend the recorded target without processing `.pth`/`.egg-link` files or trusting PYTHONPATH/user-site, reject required modules preloaded from elsewhere, attest every approved distribution root plus each required module `__file__` beneath the canonical target, and add only the validator-derived canonical repository root after that target when `--run-module` needs project modules/tests.
    - `approved_chromium_executable` returns only the current canonical executable under the retained/staged approved root after approved-status and current-byte verification; it never searches ambient Playwright caches, Edge, Chrome, or PATH.
  </behavior>
  <action>Before importing pytest, packaging, cryptography, Playwright, or the existing provenance validator, create `scripts/bootstrap_phase2_runtime_binding.py` with public functions `load_bootstrap_manifest`, `verify_bootstrap_isolation`, `verify_bootstrap_interpreter`, `verify_bootstrap_source`, `run_bootstrap_self_tests`, and `main`. It may import only Python standard-library modules and its exact CLI is `--manifest PATH --check` or `--manifest PATH --self-test`. Require `runtime_binding.executable -I -S`: validate isolated/ignore-environment/no-user-site/no-site/safe-path flags, reject cwd/user-site/PYTHONPATH-derived `sys.path`, reject forbidden third-party/preloaded modules, and AST-check its own imports against `sys.stdlib_module_names`. Derive the canonical repository root and script relative path from `__file__` without trusting cwd, reject symlink/reparse/path swaps, calculate the executable and script SHA-256 itself, and match every exact interpreter/version/implementation/architecture/ABI/prefix/base-prefix/stdlib/dynload/Scripts field plus `python.runtime_binding.bootstrap.{relative_path,sha256}` in the manifest. Its self-tests use only `tempfile` fixtures and stable safe codes to prove wrong interpreter, missing `-I`/`-S`, path/hash drift, link/reparse escape, and forbidden import influence fail closed. Record the finalized bootstrap path/hash before its first accepted `--check`, and use this bootstrap as the only pre-runner verification; the current provenance script is ineligible because it imports `packaging`. Then extend `tests/test_phase2_package_provenance.py` and `scripts/verify_phase2_package_provenance.py` before any install or Chromium collection. Add exact manifest records and APIs `verify_interpreter_binding`, `verify_installed_python_closure`, `activate_approved_python_target`, `attest_approved_import_origins`, expanded `verify_chromium_provenance`, and `approved_chromium_executable`; add CLI modes `--scope chromium`, `--verify-installed`, `--activate-approved-target`, repeatable `--require-import`, `--run-module`, `--verify-current-bytes`, and `--no-execute`. Keep activation stdlib-first after the bootstrap: validate the canonical manifest, exact `sys.executable`, interpreter/prefix/ABI, target path/file identity and installed closure before adding the exact target at index zero; do not call `site.addsitedir`, process `.pth` or `.egg-link` files, or trust `PYTHONPATH`, user-site, PATH, current-working-directory package shadows, or ambient distributions. Reject a required import already present in `sys.modules` outside the target. Discover approved distributions only with `importlib.metadata.distributions(path=[approved_target])`; require each distribution metadata/root and every imported module/package `__file__` (including direct `cryptography` and `playwright.sync_api` imports) to canonicalize beneath the approved target, with no symlink/reparse escape, before `--run-module` executes in-process. When project modules/tests are requested, derive the repository root only from the canonical validator `__file__`, reject a link/reparse escape, and place that root after the approved target rather than trusting cwd/PYTHONPATH. Invoke every post-bootstrap CLI use through the manifest-recorded absolute interpreter with `-I`; hostile-test wrong interpreter, omitted `-I`/isolation state, nonempty/untrusted Python path influence, target/repository ordering, ambient same-version substitution, preloaded modules, missing/None/outside `__file__`, distribution roots outside target, `.pth`/`.egg-link`, target/link swaps, and module replacement after attestation. After the runner is implemented and the exact approved target is installed, require it to attest `cryptography` and `playwright.sync_api` and execute the entire hostile provenance/activation test module with `-x`; only the named `approved_chromium_smoke` case is excluded because its separate Task 2 human byte gate has not yet occurred, and no other skip/xfail/deselection satisfies Task 1. Add that named post-approval Chromium smoke test so Task 3 executes it through the same runner. Hostile tests must also reject zero/multiple interpreter matches; full version, implementation, architecture/ABI, prefix, platlib, Scripts, or install-root drift; ambient user-site content; Chromium archive/executable paths outside the retained root or through links/reparse points; missing/current size or SHA-256 mismatch; source/revision/filename drift; incomplete pending/approved evidence; missing proof that no browser helper/executable ran; and any executable lookup outside the approved record. Bind the audited production runtime exactly to `C:/Users/Usry/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe` and the dedicated package target exactly to `C:/Users/Usry/AppData/Local/Jarvis/phase-2/python-site`; record the executable real path/SHA-256, full CPython 3.11.15 Windows x64 implementation/architecture/ABI, prefix/base-prefix, platlib, Scripts directory, package-target path/file identity, approved manifest target, and explicit import roots for every approved distribution before installation. Current evidence distinguishes that Hermes 3.11.15 executable selected by the supervisor from `py -3.11` at 3.11.9; if any recorded identity is not uniquely proven or differs from the already approved manifest, return a blocking prerequisite error before installation and require the human to select/reapprove the Python closure rather than choosing automatically. Ambient `python -m pip`, `py -m pip`, PATH lookup, user-site installation, and installing into any other prefix/target are forbidden. Abort unless that exact absolute interpreter runs `verify_python_closure` and confirms explicit human APPROVED entries for the complete exact direct/transitive closures of cryptography==49.0.0 and playwright==1.61.0. Add hash-locked pins without weakening existing pins; re-download exact approved wheels into one temporary directory; disable indexes/network resolution; and invoke pip only as `C:/Users/Usry/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe -m pip install --no-index --no-deps --target C:/Users/Usry/AppData/Local/Jarvis/phase-2/python-site [explicit-verified-wheel-paths]`. Re-probe installed distributions from only that target and reject ambient substitutes. Then read the installed Playwright package's own browser metadata only after approved-target activation and attestation to resolve its declared Windows x64 Chromium source/revision. Without invoking a Playwright installer/helper, BrowserType launch, subprocess, `os.startfile`, or Chromium executable, use a bounded standard-library HTTPS collector to download the exact official archive, safely extract to the separately recorded `C:/Users/Usry/AppData/Local/Jarvis/phase-2/playwright-browsers` root with traversal/link/reparse rejection, and record requested/final source URL, revision, archive/executable canonical relative paths, filenames, sizes, and calculated SHA-256. Persist append-only no-execute evidence identifying collector mode and zero helper/executable launches; set both byte records to `PENDING_EXACT_BYTE_APPROVAL`; regenerate Markdown; preserve exact bytes unchanged. Keep the already separate Plan 02-05 Python human approvals distinct from the Plan 02-06 Chromium byte approval.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I -S scripts/bootstrap_phase2_runtime_binding.py --manifest docs/security/phase-2-package-provenance.json --self-test; if ($LASTEXITCODE) { exit $LASTEXITCODE }; &amp; $p -I -S scripts/bootstrap_phase2_runtime_binding.py --manifest docs/security/phase-2-package-provenance.json --check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_phase2_package_provenance.py -x -k 'not approved_chromium_smoke'; if ($LASTEXITCODE) { exit $LASTEXITCODE }; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope chromium --require-status pending --verify-current-bytes --no-execute; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt hash-locks the complete approved direct/transitive set, with each exact pin present once.
    - One recorded absolute interpreter and dedicated install root match the approved target and all commands use them; the 3.11.9/3.11.15 ambiguity cannot be hidden by PATH.
    - Before the approved-target runner exists, the standard-library bootstrap passes its hostile self-tests and exact check under the manifest interpreter with `-I -S`, matches its recorded repository path/SHA-256, and proves pytest/packaging/cryptography/Playwright/Pydantic/site/PYTHONPATH cannot influence it.
    - The isolated activation CLI and production API reject ambient/preloaded substitutions and attest every approved distribution plus required module `__file__` under the canonical target before cryptography, Playwright, pytest, or browser use.
    - The complete hostile provenance/activation suite passes through that runner with installed-closure and both import-origin attestations before Task 1 passes; the only excluded case is the explicitly named post-human-approval Chromium smoke test owned by Task 3.
    - Hostile tests prove every new Chromium/interpreter/installed-byte validator mode fails closed before installation or launch.
    - Installed distributions match every approved name, version, dependency edge, wheel identity, filename, size, and artifact hash with offline `--no-index --no-deps` installation.
    - Exact Chromium source/revision, archive filename/size/SHA-256, and extracted executable filename/size/SHA-256 are recorded as PENDING while no Chromium process or browser install helper has run.
  </acceptance_criteria>
  <done>The stdlib-only bootstrap and its hostile self-tests pass before third-party import; the complete post-runner hostile provenance/activation suite passes with installed-closure and import-origin attestation; the one bound interpreter/install root and installed distributions match the complete approved closure; exact Chromium bytes validate in retained PENDING no-execute state; and collection started no helper or Chromium process.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact collected Chromium archive and executable bytes</name>
  <files>docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Review the exact retained archive and extracted executable collected by Task 1 against the previously approved package-declared Chromium source/revision. Confirm requested/final official source URL, revision, archive filename, archive byte size, archive SHA-256, executable relative filename/path, executable byte size, and executable SHA-256. This checkpoint is never auto-approvable and occurs before the first Chromium process starts. On approval, record the human decision, timestamp, and both exact approved SHA-256 values in the canonical JSON manifest and regenerate the Markdown receipt; on missing data, source/revision drift, byte drift, or ambiguity, reject and stop. Do not invoke any browser installer, Playwright launch API, Chromium executable, or smoke test during this checkpoint.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope chromium --require-status approved --verify-current-bytes --no-execute; exit $LASTEXITCODE"</automated>
    <human-check>Approve only the displayed package-declared source/revision and the exact retained archive/executable filenames, sizes, and SHA-256 values; confirm no Chromium process has started.</human-check>
  </verify>
  <acceptance_criteria>
    - Human approval names the exact source/revision plus archive and executable filenames, sizes, and SHA-256 values.
    - Validator rehashes the retained bytes and proves both approved hashes match before any process starts.
  </acceptance_criteria>
  <done>The checkpoint completes only when exact retained Chromium archive and executable bytes are human-approved and validator-confirmed without executing them; rejection, omission, or drift keeps the plan blocked.</done>
</task>

<task type="auto">
  <name>Task 3: Stage and smoke-launch only the approved Chromium executable</name>
  <files>docs/security/phase-2-package-provenance.md, docs/security/phase-2-package-provenance.json</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - requirements.txt
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Only after Task 2 approval, use the manifest-recorded absolute interpreter with `-I` to re-run the provenance validator over the retained archive and executable before any launch. Offline-stage the already approved extracted tree into the manifest's explicit Phase 2 browser root without network, dependency resolution, a browser installer helper, or alternate download; rehash the staged executable and require equality with the approved executable SHA-256. Obtain the executable path only through hostile-tested `approved_chromium_executable`, require its identity to match the package-declared revision, then invoke the named smoke test through `--activate-approved-target --require-import playwright.sync_api --run-module pytest`; the activation path must attest the Playwright module `__file__` and its distribution root under the approved target before the smoke test imports it. Pass only that approved executable explicitly so system Edge/Chrome, pre-existing revision-1208 cache entries, an ambient Playwright/cache/PYTHONPATH, or an operator profile cannot be selected. Then and only then perform the first headless launch/new-context/close smoke test with no navigation, `accept_downloads=False`, `service_workers='block'`, `storage_state=None`, and external network denied. Record the exact approval record, prelaunch current-byte validation, post-stage hash equality, interpreter/install binding, target/import attestation, and smoke result in both receipts. Any mismatch stops before import/launch and cannot fall back.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope chromium --require-status approved --verify-current-bytes --no-execute; if ($LASTEXITCODE) { exit $LASTEXITCODE }; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import playwright.sync_api --run-module pytest -- -q tests/test_phase2_package_provenance.py::test_approved_chromium_smoke -x; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - The exact approved executable hash still matches after offline staging and immediately before first launch.
    - Playwright's imported module file and distribution root attest beneath the approved target under the manifest-bound isolated interpreter before first launch.
    - A no-network ephemeral context opens and closes successfully only after the byte-approval checkpoint.
    - No browser installer, ambient browser/cache/profile fallback, alternate archive, or network resolution is configured.
  </acceptance_criteria>
  <done>The exact approved Chromium executable is offline-staged, rehashed, and smoke-launched only after human byte approval; the no-network ephemeral context passes and no fallback exists.</done>
</task>

</tasks>

## Artifacts this phase produces

- `requirements.txt`: exact hash-locked direct/transitive pins for the approved `cryptography==49.0.0` and `playwright==1.61.0` dependency closures.
- `docs/security/phase-2-package-provenance.json`: canonical approved Python closure, collected/approved Chromium archive and executable byte identities, and post-stage validation state.
- `docs/security/phase-2-package-provenance.md`: human-readable installed distribution, exact Chromium byte approval, and post-approval smoke evidence.
- `scripts/bootstrap_phase2_runtime_binding.py`: standard-library-only `load_bootstrap_manifest`, `verify_bootstrap_isolation`, `verify_bootstrap_interpreter`, `verify_bootstrap_source`, `run_bootstrap_self_tests`, and `main` pre-runner contract.
- `scripts/verify_phase2_package_provenance.py`: interpreter binding, installed closure, stdlib-first approved-target activation/import attestation and isolated module runner, Chromium pending/approved current-byte/no-execute validation, and approved executable lookup.
- `tests/test_phase2_package_provenance.py`: hostile interpreter ambiguity, approved-target/module/distribution substitution, installed-root substitution, Chromium path/hash/status, no-execute evidence, and post-approval smoke tests.

<threat_model>
Attacker goals: substitute a dependency after approval, drift to a newer version, or route automation through an unapproved ambient browser.

Trust boundaries: approval receipt-to-package installer and Playwright package-to-browser executable.

| Threat ID | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-02-BOOT | Tampering/Elevation | ambient interpreter/site/PYTHONPATH or a substituted bootstrap script falsifies the runner foundation | mitigate: manifest interpreter under `-I -S`, stdlib-only import/source audit, exact repository path/hash and interpreter identity | bootstrap self-tests and exact check before any runner/third-party import |
| T-02-SC | Tampering/Elevation | installer/import machinery resolves different bytes/version/browser or Chromium starts before exact-byte approval | mitigate: executable receipt validator, exact pins, offline no-deps install, isolated explicit target activation, module/distribution origin attestation, non-executing archive/executable collection, blocking exact-byte human gate, pre-launch rehash | validator, hostile activation tests, checkpoint, and smoke commands |

Residual risk: package legitimacy and an exact runtime binding do not prove safe application composition. Approval and browser plans remain blocked by their hostile suites and final real release drills.
</threat_model>

<verification>
Run the standard-library bootstrap first through the recorded absolute interpreter with `-I -S`; only after its exact source/interpreter/isolation check passes may the approved-target runner execute the complete hostile provenance/activation suite with installed-closure and cryptography/Playwright origin attestation. Then run the remaining task commands through that recorded interpreter with `-I`, inspect pins, hashes, approval timestamps and no-execute evidence, prove first Chromium launch follows the blocking byte checkpoint, and use the same attested runner/install root for downstream approval and browser suites.
</verification>

<success_criteria>
Only the complete approved direct/transitive Python artifact set is installed offline into one evidence-bound location through one absolute interpreter; a hostile-tested isolated activation path proves required import/distribution origins under that target; Chromium archive/executable byte modes collect without execution, require a separate human approval, rehash, and only then smoke-launch the approved executable for downstream use.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-06-SUMMARY.md` when done.
</output>
