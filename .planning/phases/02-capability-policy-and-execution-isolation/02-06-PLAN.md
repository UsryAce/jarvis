---
phase: 02-capability-policy-and-execution-isolation
plan: 06
type: execute
wave: 2
depends_on: [02-05]
files_modified:
  - requirements.txt
  - docs/security/phase-2-package-provenance.md
autonomous: true
requirements: [CTRL-03, TOOL-05]
must_haves:
  truths:
    - "Only human-approved exact package artifacts are installed and pinned."
    - "The installed Chromium executable identity and SHA-256 are recorded before browser code may import it."
    - "Package or browser hash drift fails closed and does not fall back to another version or ambient browser."
  artifacts:
    - path: requirements.txt
      provides: hash-locked approved direct/transitive package closure headed by cryptography 49.0.0 and Playwright 1.61.0
    - path: docs/security/phase-2-package-provenance.md
      provides: installed package and Chromium artifact verification
  key_links:
    - from: requirements.txt
      to: docs/security/phase-2-package-provenance.md
      via: exact pins must match approved hashes
---

<objective>
Install and lock only the two previously approved Phase 2 dependencies and verify the Playwright Chromium artifact before use.

Purpose: Supply the approved cryptographic and browser primitives without widening the dependency boundary.
Output: Exact requirements pins and installed-artifact receipt.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/security/phase-2-package-provenance.md
@requirements.txt
@.planning/phases/02-capability-policy-and-execution-isolation/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hash-verify, install, and pin the approved Python packages</name>
  <files>requirements.txt, docs/security/phase-2-package-provenance.md</files>
  <read_first>
    - requirements.txt
    - docs/security/phase-2-package-provenance.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-05-SUMMARY.md
  </read_first>
  <action>Abort unless the receipt contains explicit human APPROVED entries for the complete exact direct and transitive artifact closures of cryptography==49.0.0 and playwright==1.61.0. Add hash-locked direct and transitive pins to requirements.txt without weakening existing pins. Re-download every approved artifact into one temporary local directory, verify normalized name, version, wheel tags, dependency edge, filename, size, and SHA-256 against the receipt before any install, disable index/network access, then install only the verified local wheel paths with `python -m pip install --no-deps` so pip cannot resolve or download anything. Verify installed metadata matches the complete approved closure and fail on an absent prerequisite, ambient substitute, extra distribution, alternate wheel, source distribution, or hash drift.</action>
  <verify>
    <automated>python -c "import cryptography,playwright; assert cryptography.__version__=='49.0.0'; import importlib.metadata as m; assert m.version('playwright')=='1.61.0'"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt hash-locks the complete approved direct/transitive set, with each exact pin present once.
    - Installed distributions match every approved name, version, dependency edge, wheel identity, and artifact hash.
    - Any mismatch aborts before an approval or browser module is exercised.
  </acceptance_criteria>
  <done>Requirements hash-locks the approved closure, local `--no-deps` installation has no network resolution, installed metadata matches every approved artifact, and all version checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Install and record the approved Playwright Chromium artifact</name>
  <files>docs/security/phase-2-package-provenance.md</files>
  <read_first>
    - docs/security/phase-2-package-provenance.md
    - requirements.txt
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <action>Use `python -m playwright install chromium` only after Task 1 and the Playwright human approval. Resolve the package-declared Chromium revision, verify it came from the approved Playwright download source, locate the actual executable, record revision, absolute installation location category, file size, SHA-256, and `browser_type.executable_path` match in the receipt. Run a launch/new-context/close smoke test with no navigation, profile import, or external network. Do not use system Edge/Chrome or an ambient profile as fallback.</action>
  <verify>
    <automated>python -c "from playwright.sync_api import sync_playwright; p=sync_playwright().start(); b=p.chromium.launch(headless=True); c=b.new_context(accept_downloads=False,service_workers='block'); c.close(); b.close(); p.stop()"</automated>
  </verify>
  <acceptance_criteria>
    - The exact Chromium revision and executable SHA-256 are recorded.
    - A no-network ephemeral context opens and closes successfully.
    - No ambient browser/profile fallback is configured.
  </acceptance_criteria>
  <done>The declared Chromium revision and executable hash are recorded, the no-network ephemeral-context smoke test passes, and no ambient browser fallback exists.</done>
</task>

</tasks>

## Artifacts this phase produces

- `requirements.txt`: exact hash-locked direct/transitive pins for the approved `cryptography==49.0.0` and `playwright==1.61.0` dependency closures.
- `docs/security/phase-2-package-provenance.md`: installed distribution and Chromium revision/executable-hash evidence.

<threat_model>
Attacker goals: substitute a dependency after approval, drift to a newer version, or route automation through an unapproved ambient browser.

Trust boundaries: approval receipt-to-package installer and Playwright package-to-browser executable.

| Threat ID | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-02-SC | Tampering/Elevation | installer resolves different bytes/version/browser | mitigate: receipt prerequisite, exact pins, pre-install hash, post-install version, Chromium executable hash | version and smoke commands |

Residual risk: package legitimacy does not prove safe application composition. Approval and browser plans remain blocked by their hostile suites and final real release drills.
</threat_model>

<verification>
Run the two automated commands, inspect exact pins and receipt hashes, then run `python -m pytest -q tests/test_approval_envelope.py tests/test_browser_broker.py -x` to activate package-gated contracts.
</verification>

<success_criteria>
Only the complete approved direct/transitive Python artifact set and approved Chromium artifact are available, pinned, and recorded for downstream use.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-06-SUMMARY.md` when done.
</output>
