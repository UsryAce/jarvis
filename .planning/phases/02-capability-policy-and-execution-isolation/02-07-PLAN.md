---
phase: 02-capability-policy-and-execution-isolation
plan: 07
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/security/action_contracts.py
  - src/security/manifests.py
  - src/security/policy.py
autonomous: true
requirements: [CTRL-02, CTRL-06, TOOL-03, TOOL-05]
must_haves:
  truths:
    - "D-01 through D-04 are implemented as exact sealed records and module-owned pure functions."
    - "No proposal/model value can supply trusted actor/session/project/policy/manifest/effect identity."
    - "Unknown or malformed actions deterministically deny before storage or adapters."
  artifacts:
    - path: src/security/action_contracts.py
      provides: strict transport models, frozen internal ResolvedAction, canonical bytes and digest
    - path: src/security/manifests.py
      provides: versioned typed tool schemas, limits and effect declarations
    - path: src/security/policy.py
      provides: pure total PolicyKernel and stable PolicyDecision
  key_links:
    - from: src/security/action_contracts.py
      to: src/security/auth.py
      via: trusted resolver copies OperatorPrincipal fields outside proposal JSON
    - from: src/security/policy.py
      to: src/security/manifests.py
      via: exact tool/schema manifest lookup without callbacks
---

<objective>
Implement the sealed ResolvedAction, typed manifest registry, and deterministic total policy kernel required by D-01 through D-04.

Purpose: Establish the immutable authority-neutral contract consumed by approval, persistence, and every adapter.
Output: Three security modules satisfying the Wave 0 hostile policy suite.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-01-SUMMARY.md
@src/core/audit.py
@src/security/auth.py
@tests/test_policy_kernel.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact action records and typed manifests</name>
  <files>src/security/action_contracts.py, src/security/manifests.py</files>
  <read_first>
    - src/security/action_contracts.py (target; confirm absent before creation)
    - src/security/manifests.py (target; confirm absent before creation)
    - tests/test_policy_kernel.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
    - src/core/audit.py
    - src/security/auth.py
  </read_first>
  <behavior>
    - Exact base records accept only the closed bounded grammar and reject subclasses, coercion, extras, hooks, non-finite and unbounded values.
    - `canonical_action_bytes` is domain-separated, compact sorted UTF-8 JSON and `resolved_action_digest` is stable for identical semantics.
    - Manifests define exact schema, declared effect, project/worktree need, executable/path/network constraints, input/output/resource limits, and default-denied shell/GitHub/project-write capabilities.
  </behavior>
  <action>Per D-01, D-03, and D-04, add strict nested Pydantic boundary models with strict, frozen, extra-forbid, finite, hidden-input errors, then copy to exact frozen slots internal records. Implement module-owned normalization, `canonical_action_bytes`, and `resolved_action_digest` over exact None/bool/bounded-int/normalized-string/tuple/string-key-map values; never use default=str, pickle, asdict, arbitrary Mapping, model serializer, or input method dispatch. Create ManifestRegistry with immutable version/digest and exact schemas for scoped filesystem read/write, executable argv process/build/test/app actions, browser navigate/download/capture, and fixture external-write/reconcile. Keep raw shell, GitHub mutation, and autonomous project writes unregistered/default-deny.</action>
  <verify>
    <automated>python -m pytest -q tests/test_policy_kernel.py -x -k "canonical or manifest or malformed"</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 import gates for action contracts and manifests are strict and pass.
    - Trusted identity and resolved boundary fields cannot be populated from proposal arguments.
    - Diagnostics expose only stable codes and safe identifiers.
  </acceptance_criteria>
  <done>The action/manifest tests pass, proposal input cannot populate trusted fields, and diagnostics expose only stable safe identifiers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure total policy kernel</name>
  <files>src/security/policy.py</files>
  <read_first>
    - src/security/policy.py (target; confirm absent before creation)
    - src/security/action_contracts.py
    - src/security/manifests.py
    - tests/test_policy_kernel.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
  </read_first>
  <behavior>
    - `evaluate_policy(ResolvedAction, PolicySnapshot)` returns exactly allow, ask, or deny with a stable safe reason.
    - Unknown versions/tools/effects, stale manifest/policy, invalid boundary/preconditions, raw shell, GitHub, and project-write authority deny.
    - The kernel performs no I/O, mutation, callbacks, dynamic import, or polymorphic dispatch; wrapper exceptions map to deny/policy_evaluation_failed.
  </behavior>
  <action>Per D-02, compile policy snapshots into exact immutable rule data and implement a module-owned PolicyKernel/evaluate_policy decision table. Include stable schema, identity, scope, path, process, network, approval, precondition, idempotency, control, and internal reason-code families. The kernel must be pure and total; catch unexpected failures only at the public wrapper and return deny/policy_evaluation_failed without exception text. Explain safe capability, boundary, effect, expiry/limits, and argument summary through allowlisted fields only.</action>
  <verify>
    <automated>python -m pytest -q tests/test_policy_kernel.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every hostile vector returns the exact expected decision and safe code.
    - Reordered equivalent values are deterministic and mutation cases fail closed.
    - No policy hook or input-owned callable is invoked.
  </acceptance_criteria>
  <done>The full policy suite passes every hostile vector deterministically and proves no callback or input-owned callable executes.</done>
</task>

</tasks>

## Artifacts this phase produces

- `StrictActionEnvelope`, `CanonicalArguments`, `ResolvedAction`, `canonical_action_bytes`, `resolved_action_digest` in `src/security/action_contracts.py`.
- `ToolManifest`, `ManifestSnapshot`, `ManifestRegistry`, `resolve_manifest_action` in `src/security/manifests.py`.
- `PolicyOutcome`, `PolicyDecision`, `PolicySnapshot`, `PolicyKernel`, `evaluate_policy` in `src/security/policy.py`.

<threat_model>
Attacker goals: smuggle coercible/polymorphic data, claim trusted identity, select an undeclared effect, or crash policy into permissive behavior.

Trust boundaries: untrusted proposal-to-strict transport, trusted resolver-to-ResolvedAction, and action/policy data-to-total decision.

| Threat IDs | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-01, T-02 | Tampering/Elevation | hostile value changes canonical meaning or invokes code | mitigate: exact bounded grammar and module-owned canonicalization | policy suite |
| T-24 | Elevation | raw shell/GitHub/project-write manifest opens | mitigate: absent/default-deny manifests | policy suite and later sink inventory |

Residual risk: a correct decision does not authorize execution by itself; 02-09 and 02-12 enforce approval/reservation/gateway ordering. High-severity policy failures block all adapters.
</threat_model>

<verification>
Run the full policy suite and `python -m compileall -q src/security`; inspect public exports for exact types and absence of dynamic/callback policy hooks.
</verification>

<success_criteria>
D-01 through D-04 are fully implemented and all CTRL-02 hostile contracts pass.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-07-SUMMARY.md` when done.
</output>
