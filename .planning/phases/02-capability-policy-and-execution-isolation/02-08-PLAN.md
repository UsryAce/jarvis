---
phase: 02-capability-policy-and-execution-isolation
plan: 08
type: execute
wave: 1
depends_on: [02-02]
files_modified:
  - src/core/control_store.py
  - src/core/capability_store.py
  - src/execution/receipts.py
autonomous: true
requirements: [CTRL-03, TOOL-03, TOOL-05, TOOL-07]
must_haves:
  truths:
    - "D-07 and D-16 durable authority lives in the existing serialized ControlStore, never AgentStore or a parallel database."
    - "Reservation precedes dispatch; same key with changed digest is terminal conflict; ambiguity never authorizes replay or success."
    - "Receipts and audit projections store safe identifiers, hashes, bounds, cleanup and reconciliation truth only."
  artifacts:
    - path: src/core/capability_store.py
      provides: manifests, policies, actions, approval consumption, reservations, idempotency, effects, receipts, reconciliation and artifact repositories
    - path: src/execution/receipts.py
      provides: immutable typed receipt and safe projection
    - path: src/core/control_store.py
      provides: checksummed Phase 2 authority migrations and restore-required tables
  key_links:
    - from: src/core/capability_store.py
      to: src/core/control_store.py
      via: ControlStoreTransaction and BEGIN IMMEDIATE only
    - from: src/core/capability_store.py
      to: src/core/audit.py
      via: caller-transaction redacted append
---

<objective>
Implement the authoritative Phase 2 persistence, receipt, idempotency, and reconciliation contracts in the existing ControlStore.

Purpose: Close replay/crash races and preserve applied truth before any real adapter dispatches.
Output: Checksummed migrations, capability repositories/state machines, and safe receipt projection.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-02-SUMMARY.md
@src/core/control_store.py
@src/core/audit.py
@src/core/agent_store.py
@tests/test_effect_reconciliation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add checksummed capability-authority migrations and repositories</name>
  <files>src/core/control_store.py, src/core/capability_store.py</files>
  <read_first>
    - src/core/control_store.py
    - src/core/capability_store.py (target; confirm absent before creation)
    - src/core/audit.py
    - src/core/agent_store.py
    - tests/test_effect_reconciliation.py
    - tests/test_control_migrations_backup.py
  </read_first>
  <behavior>
    - ControlStore migrations create required Phase 2 rows with uniqueness, foreign keys, checksums, restore validation, and single-owner serialized authority.
    - One immediate transaction can re-read current truth, reserve action/idempotency, transition effect, store receipt/reconciliation/artifact metadata, and append audit.
    - Same idempotency key with another action digest rejects and never replaces the original row.
  </behavior>
  <action>Extend the existing checksummed migration chain and restore-required-table checks for capability_manifests, policy_snapshots, resolved_actions, approval_keys, approval_consumptions, action_reservations, idempotency_records, effect_attempts, effect_receipts, reconciliation_attempts, artifact_records, and capability_release_state. Implement CapabilityStore repositories that accept only active ControlStoreTransaction for mutations. Use unique nonce/key/digest constraints and states reserved, dispatching, applied, not_applied, needs_reconciliation, reconciled_applied, reconciled_not_applied, reconciliation_failed. Preserve AgentStore rows as compatibility evidence only; do not write Phase 2 authority there or add queue/lease/restart orchestration.</action>
  <verify>
    <automated>python -m pytest -q tests/test_effect_reconciliation.py tests/test_control_migrations_backup.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Fresh migration, checksum rejection, backup/restore, contention and crash tests pass.
    - Original idempotency intent cannot be overwritten by conflict syntax.
    - Every authoritative mutation can append audit in the same transaction.
  </acceptance_criteria>
  <done>Migration, restore, contention, and crash tests pass; conflicting intent cannot overwrite authority; and each mutation can atomically append audit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed durable receipt and reconciliation projections</name>
  <files>src/execution/receipts.py, src/core/capability_store.py</files>
  <read_first>
    - src/execution/receipts.py (target; confirm absent before creation)
    - src/core/capability_store.py
    - src/core/audit.py
    - src/api/approval_receipts.py
    - tests/test_effect_reconciliation.py
    - tests/test_execution_gateway_integration.py
  </read_first>
  <behavior>
    - Receipt records expose stable IDs, decision/reason, timing/limits, bounded counts/hashes, cleanup/applied/reconciliation truth, and artifact references only.
    - Raw arguments, environment, browser content, command output, secrets and exception text are rejected before persistence.
    - Ambiguous receipts stay nonterminal until an adapter-specific read-only probe records authoritative reconciliation evidence.
  </behavior>
  <action>Define immutable ActionReceipt, CleanupTruth, AppliedTruth, ReconciliationTruth, ArtifactReference, and `project_safe_receipt` with exact allowlisted fields and bounded safe identifiers. Add CapabilityStore methods for reserve-or-match, mark-dispatching, record-applied/not-applied/ambiguous, begin/finish reconciliation, and load authoritative receipt. Use before/after hashes and canonical remote IDs without storing raw bytes. Append decision, approval, reservation, dispatch, cancel, ambiguity, result, and reconciliation audit events through existing AuditService transaction calls.</action>
  <verify>
    <automated>python -m pytest -q tests/test_effect_reconciliation.py tests/test_execution_gateway_integration.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All crash points converge to an honest durable state without second dispatch.
    - Canary fields cannot reach receipt rows, audit payloads, API projections, or assertion messages.
    - Reconciliation requires adapter-specific authoritative evidence.
  </acceptance_criteria>
  <done>Receipt/reconciliation tests pass all crash paths, canaries are absent from durable/API evidence, and ambiguity resolves only through adapter-specific authoritative proof.</done>
</task>

</tasks>

## Artifacts this phase produces

- Phase 2 ControlStore tables listed in Task 1 and `capability_release_state`.
- `CapabilityStore`, `reserve_or_match`, `record_dispatching`, `record_effect_result`, `begin_reconciliation`, `finish_reconciliation`, `load_receipt` in `src/core/capability_store.py`.
- `ActionReceipt`, `CleanupTruth`, `AppliedTruth`, `ReconciliationTruth`, `ArtifactReference`, `project_safe_receipt` in `src/execution/receipts.py`.

<threat_model>
Attacker goals: race approval/reservation, overwrite idempotency intent, fabricate success after a crash, or leak inputs through durable evidence.

Trust boundaries: gateway-to-authority transaction, transaction-to-adapter dispatch, and raw result-to-safe receipt/audit.

| Threat IDs | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-05..T-07 | Tampering/Repudiation | race or crash duplicates/changes effect | mitigate: BEGIN IMMEDIATE, unique original intent, explicit state machine | effect suite |
| T-25 | Repudiation/Disclosure | missing or secret-bearing evidence | mitigate: same-transaction audit and exact receipt projection | gateway/effect suites |

Residual risk: store correctness cannot contain an OS/browser adapter; those plans must populate honest evidence and the final release gate remains closed on any unknown.
</threat_model>

<verification>
Run the three focused suites, then `python -m pytest -q tests/test_audit_integrity.py tests/test_control_migrations_backup.py -x` to guard Phase 1 authority invariants.
</verification>

<success_criteria>
D-07 and D-16 durable reservation, idempotency, receipt, audit, and reconciliation truth pass all hostile fault points without a parallel authority store.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-08-SUMMARY.md` when done.
</output>
