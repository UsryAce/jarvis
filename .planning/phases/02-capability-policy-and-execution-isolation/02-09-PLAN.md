---
phase: 02-capability-policy-and-execution-isolation
plan: 09
type: execute
wave: 3
depends_on: [02-06, 02-07, 02-08]
files_modified:
  - src/security/approvals.py
  - src/core/capability_store.py
  - tests/test_approval_envelope.py
autonomous: true
requirements: [CTRL-03, TOOL-07]
must_haves:
  truths:
    - "D-05 through D-08 are enforced with a DPAPI-protected Ed25519 issuer and public-only verifier."
    - "Policy, manifest, session, boundary, effect, expiry, nonce and precondition truth are re-read before one atomic consume/reserve."
    - "Execution dependencies cannot reach signing material or mint approval artifacts."
    - "Cryptography is imported only after the manifest-bound isolated interpreter activates the approved package target and attests the module file and distribution root beneath it; ambient or preloaded substitutions fail closed."
  artifacts:
    - path: src/security/approvals.py
      provides: versioned claims/envelope, DPAPI-backed issuer, public-only verifier and safe errors
    - path: src/core/capability_store.py
      provides: atomic verify-current/consume/reserve operation
    - path: scripts/verify_phase2_package_provenance.py
      provides: hostile-tested approved-target activation and cryptography import/distribution attestation required by this plan
    - path: tests/test_approval_envelope.py
      provides: Ed25519 behavior plus approved-target import-origin and public-only authority contracts
  key_links:
    - from: src/security/approvals.py
      to: src/security/secrets.py
      via: issuer-only current-user DPAPI private-key custody
    - from: src/core/capability_store.py
      to: src/security/approvals.py
      via: public verifier inside short authority transaction
    - from: src/security/approvals.py
      to: scripts/verify_phase2_package_provenance.py
      via: activate_approved_python_target and attest_approved_import_origins run before any cryptography import and reject non-target origins
---

<objective>
Implement separated Ed25519 approval issuance/verification and atomic current-truth consume/reserve for D-05 through D-08.

Purpose: Turn operator approval into a narrow single-use artifact without giving execution code issuance authority.
Output: Approval module and transactional reservation integration passing all approval hostile tests.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-01-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-06-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-08-SUMMARY.md
@src/security/secrets.py
@tests/test_approval_envelope.py
@scripts/verify_phase2_package_provenance.py
@docs/security/phase-2-package-provenance.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the versioned DPAPI-backed issuer and public-only verifier</name>
  <files>src/security/approvals.py, tests/test_approval_envelope.py</files>
  <read_first>
    - src/security/approvals.py (target; confirm absent before creation)
    - src/security/action_contracts.py
    - src/security/secrets.py
    - src/security/auth.py
    - tests/test_approval_envelope.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
    - docs/security/phase-2-package-provenance.md
  </read_first>
  <behavior>
    - `ApprovalIssuer` signs only exact v1 claims with an Ed25519 private key protected by current-user DPAPI.
    - `ApprovalVerifier` is constructed only from key ID and public bytes and verifies domain-separated exact claims.
    - Bad key/signature/type/version/time or any bound-field drift returns stable safe rejection without input/exception text.
    - Importing approvals under the wrong interpreter, without approved-target activation, or with cryptography preloaded/resolved outside the approved target fails before key generation, signing, verification, or test execution.
  </behavior>
  <action>Per D-05, D-06, and D-08, first extend `tests/test_approval_envelope.py` with import-origin cases, then implement ApprovalClaims and ApprovalEnvelope exact frozen records. Before `src/security/approvals.py` imports any `cryptography` module, call the Plan 02-06 stdlib-first `activate_approved_python_target`/`attest_approved_import_origins` path against the canonical provenance manifest; require the current canonical `sys.executable` to equal the manifest binding and require both the `cryptography` distribution root and every imported cryptography module `__file__` to resolve beneath `C:/Users/Usry/AppData/Local/Jarvis/phase-2/python-site`. Reject a preloaded ambient module, same-version distribution elsewhere, missing/outside `__file__`, target/link swap, unapproved PYTHONPATH/user-site influence, or manifest/runtime drift with a stable safe code before Ed25519 use. All automated commands invoke the manifest-recorded absolute interpreter with `-I` and the validator's `--activate-approved-target --require-import cryptography --run-module pytest` path; do not use ambient `python`, `py`, raw PYTHONPATH, or an unverified `sys.path` edit. Bind actor/session/request/run/project/worktree/policy/manifest/action digest/effect/limits/not-before/expiry/nonce/precondition. ApprovalIssuer exists only in the authenticated approval command service and loads raw private bytes through existing current-user DPAPI custody for the shortest signing scope; persist only DPAPI envelope/public metadata. ApprovalVerifier exposes verify only, accepts an immutable key-id-to-public-bytes map, and has no issuer/private loader/mint/symmetric secret reference. Zeroize mutable private buffers after use and return stable safe codes.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --run-module pytest -- -q tests/test_approval_envelope.py -x -k 'signature or issuer or verifier or drift or expiry or import_origin'; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - Exact cryptography pin/hash receipt is present and tests use Ed25519 only.
    - Dependency-graph tests prove verifier/execution cannot reach issuance material.
    - Every bound-field drift and time/key/signature error rejects safely.
    - The executed cryptography module file and distribution root are both attested beneath the approved target before any Ed25519 operation.
  </acceptance_criteria>
  <done>The Ed25519 approval tests pass against the approved artifact, verifier dependencies contain no issuance authority, and every drift/time/key/signature error rejects safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic current-truth approval consumption and reservation</name>
  <files>src/core/capability_store.py</files>
  <read_first>
    - src/core/capability_store.py
    - src/security/approvals.py
    - src/security/action_contracts.py
    - src/core/control_store.py
    - src/core/audit.py
    - tests/test_approval_envelope.py
    - tests/test_effect_reconciliation.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - One immediate transaction re-reads session/policy/manifest/action/project/precondition, verifies envelope, inserts unique consumption, reserves action/idempotency and appends audit.
    - Replay, concurrent consume, stale current truth, changed precondition, or key mismatch never dispatches.
    - Crash before commit leaves neither consume nor reservation; committed consume always has its matching reservation.
  </behavior>
  <action>Per D-07, add `consume_approval_and_reserve` and `reserve_allowed_action` operations. Under one ControlStore immediate transaction, load current trusted rows, compare all envelope/action digests and versions, verify time/signature/nonce, insert unique approval consumption when decision is ask, create the action reservation and reserve-or-match idempotency intent, append safe audit, then commit. An allow decision uses the same reservation/current-truth path without fabricating approval. Never use AgentStore approval claims as authority and never dispatch inside the database transaction.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --run-module pytest -- -q tests/test_approval_envelope.py tests/test_effect_reconciliation.py -x; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - Concurrent tests yield one reservation and one consumption.
    - Every stale/drift/replay path leaves adapters untouched.
    - Atomic crash tests show no split consume/reservation state.
  </acceptance_criteria>
  <done>Concurrent and crash tests pass with exactly one atomic consume/reservation and no adapter call on replay, drift, or stale truth.</done>
</task>

</tasks>

## Artifacts this phase produces

- `ApprovalClaims`, `ApprovalEnvelope`, `ApprovalIssuer`, `ApprovalVerifier`, `ApprovalError` in `src/security/approvals.py`.
- `CapabilityStore.consume_approval_and_reserve` and `CapabilityStore.reserve_allowed_action`.
- Approved-target cryptography import-origin and distribution-root coverage in `tests/test_approval_envelope.py`, executed only through the Plan 02-06 isolated activation/attestation runner.

<threat_model>
Attacker goals: forge or mint approval, swap claims, replay a nonce, or race current authority between verification and reservation.

Trust boundaries: DPAPI private custody-to-issuer, issuer artifact-to-public verifier, and verifier/current authority-to-atomic reservation.

| Threat IDs | STRIDE | Abuse path | Disposition and mitigation | Verification |
|---|---|---|---|---|
| T-03, T-04 | Spoofing/Elevation | omitted claim or reachable signer forges authority | mitigate: exact signed claims and public-only execution graph | approval suite |
| T-05 | Tampering | replay/race/stale truth executes | mitigate: re-read plus unique consume/reserve in one transaction | contention/fault tests |
| T-02-SC | Tampering | crypto package substitution or ambient/preloaded import bypass | mitigate: approved exact pin/hash plus manifest-bound isolated target activation and module/distribution origin attestation before Ed25519 use | provenance receipt and approval import-origin tests |

Residual risk: Phase 1 distinct-SID receipt is still absent; issuer may be tested in isolated fixtures, but no live adapter/privileged route opens until 02-14.
</threat_model>

<verification>
Run approval, effect, and `tests/test_secret_protector_windows.py` through the manifest-recorded absolute interpreter with `-I` and `scripts/verify_phase2_package_provenance.py --activate-approved-target --require-import cryptography --run-module pytest`; inspect the attested cryptography module/distribution origins. The cross-SID xfail remains a release blocker rather than an implementation failure; ambient `python`, `py`, or PYTHONPATH is never a valid substitute.
</verification>

<success_criteria>
D-05 through D-08 are implemented with public-only verification and atomic single-use reservation, with no symmetric evaluator authority.
</success_criteria>

<output>
Create `.planning/phases/02-capability-policy-and-execution-isolation/02-09-SUMMARY.md` when done.
</output>
