---
phase: 02-capability-policy-and-execution-isolation
plan: 10
type: execute
wave: 3
depends_on: [02-03, 02-06, 02-07, 02-08]
files_modified: [src/security/manifests.py, src/security/policy.py, src/execution/filesystem.py, src/execution/windows_job.py, src/execution/local_adapters.py, src/core/workspaces.py, tests/test_policy_kernel.py]
autonomous: true
requirements: [CTRL-06, TOOL-03]
must_haves:
  truths:
    - "Before any local adapter is callable, manifest resolution and policy independently reject every executable identity or argv value outside one exact typed contract."
    - "Raw shell, script-host, GitHub, Git mutation, and autonomous project-write proposals remain denied; there is no generic command fallback."
    - "D-09, D-10, D-11, and D-12 are enforced at the actual Windows filesystem and process sinks."
    - "Raw shell, PATH search, ambient environment, running-before-assignment, PID-only kill, and string-only path containment are impossible production contracts."
    - "Every local action returns bounded durable evidence and honest rollback/cleanup truth."
    - "Every automated task and plan verification runs through Plan 02-06's exact manifest-bound interpreter with `-I`, installed-closure verification, approved-target activation, and cryptography/Playwright module/distribution origin attestation; ambient `python`, `py`, PATH, PYTHONPATH, user-site, or direct `-m pytest` output is not acceptance evidence."
  artifacts:
    - {path: src/security/manifests.py, provides: immutable executable identity and value-level argv contracts resolved before adapters}
    - {path: src/security/policy.py, provides: independent fail-closed executable and argv enforcement}
    - {path: tests/test_policy_kernel.py, provides: hostile executable substitution and argv-value bypass coverage with zero adapter calls}
    - {path: src/execution/filesystem.py, provides: handle-verified filesystem broker}
    - {path: src/execution/windows_job.py, provides: suspended Job Object process broker}
    - {path: src/execution/local_adapters.py, provides: typed filesystem/process/build/test/application adapters}
    - {path: scripts/verify_phase2_package_provenance.py, provides: exact manifest-bound isolated approved-target pytest runner and import-origin attestation used by all Plan 02-10 verification}
  key_links:
    - {from: src/security/manifests.py, to: src/security/policy.py, via: sealed executable identity and canonical argv values are independently rechecked before dispatch}
    - {from: src/security/policy.py, to: src/execution/local_adapters.py, via: only an allowed ResolvedExecutableInvocation reaches a typed adapter}
    - {from: src/execution/local_adapters.py, to: src/execution/windows_job.py, via: argv-only process actions}
    - {from: src/execution/local_adapters.py, to: src/execution/filesystem.py, via: registered-boundary file actions}
    - {from: Plan 02-10 automated verification, to: scripts/verify_phase2_package_provenance.py, via: each preserved test selection runs only through runtime_binding.executable -I after installed closure and cryptography/playwright.sync_api origins attest beneath the approved target}
---

<objective>
Enforce exact executable and argv values before adapters, then implement the Windows filesystem and process containment brokers plus typed local adapters for D-09 through D-12.

Purpose: Enforce granted boundaries at OS sinks and produce bounded, truthful receipts.
Output: Hostile-tested manifest/policy value contracts, filesystem broker, Job Object broker, adapter registry, and hardened workspace integration.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-03-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-06-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-07-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-08-SUMMARY.md
@tests/test_filesystem_broker_windows.py
@tests/test_execution_broker_windows.py
@scripts/verify_phase2_package_provenance.py
@docs/security/phase-2-package-provenance.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Seal executable identities and argv values before any adapter</name>
  <files>src/security/manifests.py, src/security/policy.py, tests/test_policy_kernel.py</files>
  <read_first>
    - src/security/manifests.py
    - src/security/policy.py
    - src/security/action_contracts.py
    - tests/test_policy_kernel.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-READINESS-AUDIT.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-06-SUMMARY.md
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - A manifest-owned executable ID resolves through an approved registry to one canonical absolute executable path and expected byte/version identity; proposals cannot supply or override paths.
    - Canonical argv is an immutable tuple matched value-by-value against typed per-position rules, exact verbs/subcommands/flags, maximum count, and per-value/aggregate byte limits before policy or adapter selection.
    - Unknown IDs, PATH aliases, relative or alternate executables, shell/script-host/proxy indirection, extra/reordered/duplicate flags, option smuggling, empty/NUL/control values, scalar/mapping/subclass confusion, case tricks, oversized argv, Git writes, GitHub operations, or project mutations produce one stable deny and zero adapter calls.
    - Policy independently compares the sealed executable registry identity and argv-rule digest with the active manifest snapshot; a forged or stale ResolvedAction remains denied even if manifest resolution is bypassed.
  </behavior>
  <action>Write the hostile cases first and prove they fail against the current name/key-only resolver. Extend the immutable ToolManifest contract with manifest-owned `ExecutableInvocationSpec` records: each record names an opaque executable ID, its approved-registry identity, exact action IDs, typed argv grammar, count/value/aggregate bounds, and a digest included in the manifest snapshot. The trusted production executable registry, not the proposal, binds that ID to one canonical absolute executable path plus approved byte/version evidence; proposal fields may contain only the opaque ID and typed arguments. Make `resolve_manifest_action` canonicalize arguments once to an immutable tuple and validate every executable ID and argv value before it creates `ResolvedExecutableInvocation`; never accept a raw command string, PATH lookup, shell join/split, environment expansion, response file, script host, executable path supplied by the model, or an argv token not consumed by the declared grammar. Make PolicyKernel independently require the active executable-spec digest, canonical registry identity, action ID, and exact canonical argv contract before ALLOW/ASK; missing, malformed, stale, forged, or ambiguous inputs are DENY. Keep raw shell, PowerShell/cmd/script-host, GitHub, Git mutation (including commit/checkout/branch/merge/worktree/reset/clean/push), and autonomous project-write authority absent from production manifests and explicitly denied. If bounded read-only Git observation remains necessary, give each exact read verb its own manifest/action and literal argv prefix; never create a generic `git` capability. Tests must instrument the adapter boundary and assert zero calls for every hostile case. Complete this task and its tests before implementing or registering any local adapter.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_policy_kernel.py -x; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - Manifest resolution rejects executable and argv values, not only field names and required keys.
    - Policy independently rejects forged/stale executable identity, argv tuple, or executable-spec digest before adapter selection.
    - Hostile tests cover every listed substitution/smuggling class and prove zero adapter calls.
    - No production manifest grants raw shell, GitHub, Git mutation, or project-write authority.
  </acceptance_criteria>
  <done>Exact executable identities and canonical argv values are hostile-tested at both manifest and policy layers, and no local adapter can receive an unvalidated invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement handle-verified filesystem operations</name>
  <files>src/execution/filesystem.py, src/core/workspaces.py</files>
  <read_first>
    - src/execution/filesystem.py (target; confirm absent before creation)
    - src/core/workspaces.py
    - tests/test_filesystem_broker_windows.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
    - src/execution/receipts.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Relative segments reject every Windows namespace, ADS, reserved-name, link/reparse, volume and normalization escape.
    - Reads/writes prove final handle identity under the registered root; writes hold parent, stage same-directory, bound/hash, recheck, then replace.
    - Failure records not-applied or needs-reconciliation with before/after evidence and no escaped mutation.
  </behavior>
  <action>Per D-12, implement FilesystemBroker using registered workspace identity but Win32 handle walks and final-handle paths for enforcement; replace `Path.resolve()` security decisions in Workspaces with broker calls. Reject absolute/drive-relative/UNC/device/native paths, colon/ADS, reserved/trailing/invalid segments, reparse/symlink/junction/mount, volume change, ambiguous case, and multi-link writes. Hold the verified parent, stage exclusive same-directory bytes under caps, flush/hash, recheck file ID/precondition, then atomic replace when supported; otherwise return explicit rollback/reconciliation truth.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_filesystem_broker_windows.py tests/test_workspace_registry.py -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - T-08 through T-10 pass against real temporary Windows objects.
    - No mutation relies only on string resolution or pre-check existence.
    - Receipts contain bounded identity/hash/rollback evidence without contents.
  </acceptance_criteria>
  <done>The Windows filesystem and workspace suites pass T-08 through T-10, no mutation relies on string-only checks, and receipts contain no file contents.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Implement suspended Job Object execution and typed local adapters</name>
  <files>src/execution/windows_job.py, src/execution/local_adapters.py</files>
  <read_first>
    - src/execution/windows_job.py (target; confirm absent before creation)
    - src/execution/local_adapters.py (target; confirm absent before creation)
    - tests/test_execution_broker_windows.py
    - src/core/agent.py
    - src/execution/receipts.py
    - src/security/manifests.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Create limits/job/pipes first; CreateProcess suspended with absolute executable, argv-derived command line, verified cwd and minimal env; assign/verify then resume only.
    - One termination path handles cap, timeout, cancel, emergency stop and shutdown and verifies descendants as confirmed/partial/unconfirmed.
    - Typed terminal/build/test/app adapters dispatch only registered manifests; raw shell, script hosts, GitHub and project writes deny.
  </behavior>
  <action>Per D-09, D-10, and D-11, implement WindowsJobBroker with kill-on-close, active-process/memory/CPU/deadline limits, bounded inheritable pipes, `CREATE_SUSPENDED | CREATE_NO_WINDOW`, Job assignment plus verification before ResumeThread, and exact handle cleanup. Count stream bytes before retention and terminate the whole job on stdout/stderr/combined/stdin/time caps. Build LocalAdapterRegistry for filesystem, exact executable argv, build, test, and application actions using only the `ResolvedExecutableInvocation` sealed and independently allowed by Task 1; adapters accept neither proposal mappings nor raw executable IDs/paths/argv and do not re-resolve, split, join, or amend arguments. Minimal environment is explicit and excludes secrets/provider/browser/shell startup state. Production accepts no raw shell string; shell/script-host, GitHub, Git mutation, and autonomous project-write invocation stay denied capabilities. No fallback to asyncio subprocess, os.startfile, PATH lookup, process-name lookup, or PID-only kill.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_execution_broker_windows.py tests/test_filesystem_broker_windows.py -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - T-11 through T-16 pass, including assignment failure before resume and real grandchild cleanup.
    - Output/resource caps operate during streaming rather than after unlimited buffering.
    - GitHub/autonomous project-write and raw shell identities remain unavailable.
  </acceptance_criteria>
  <done>The Windows process suites pass T-11 through T-16 with bounded streaming and verified descendant cleanup, while raw shell and deferred write identities remain unavailable.</done>
</task>
</tasks>

## Artifacts this phase produces

- `ExecutableInvocationSpec` and value-level resolution in `src/security/manifests.py`, with independent executable/argv enforcement in `src/security/policy.py`.
- Hostile executable substitution, argv smuggling, stale/forged resolution, and zero-adapter-call tests in `tests/test_policy_kernel.py`.
- `FilesystemBroker`, `VerifiedPath`, `FileMutationEvidence` in `src/execution/filesystem.py`.
- `WindowsJobBroker`, `ProcessLimits`, `ProcessOutcome`, `terminate_job_tree` in `src/execution/windows_job.py`.
- `LocalAdapterRegistry` and typed filesystem/process/build/test/application adapters in `src/execution/local_adapters.py`.

<threat_model>
Attacker goals: executable substitution or argv smuggling, path/race escape, shell or environment escalation, host exhaustion, or surviving descendants with false completion.
Trust boundaries: untrusted executable/argv proposal to manifest and policy, grant-to-Windows handles, sealed invocation-to-process, and stop-to-job evidence.

| Threat IDs | STRIDE | Abuse path | Mitigation | Verification |
|---|---|---|---|---|
| T-08..T-10 | Tampering/Disclosure | namespace/reparse/TOCTOU escape | held handles and same-dir staging | filesystem suite |
| T-02-ARGV | Tampering/Elevation | executable substitution, argv option smuggling, raw shell/Git/project-write bypass | manifest-owned registry identity and typed argv grammar independently rechecked by policy before adapters | policy suite plus zero-call adapter assertions |
| T-02-ATTEST | Tampering/Elevation | ambient interpreter/package shadow falsifies local containment evidence | manifest-bound absolute interpreter with `-I`, installed-closure verification, approved-target activation, and cryptography/Playwright origin attestation before pytest | every task and plan verification command |
| T-11..T-15 | Elevation/DoS/Disclosure | shell/env/spawn/resource escape | argv-only minimal env, suspended Job, live caps | process suite |
| T-16 | Repudiation | residue claimed clean | queried whole-job cleanup truth | descendant drill |

Residual risk: the real release-machine drill remains required by 02-14; any partial/unconfirmed containment keeps adapters unavailable in production.
</threat_model>

<verification>Run the hostile policy suite first and preserve every selected test path and `-x`; then run both Windows hostile suites and the workspace baseline. Every command must use the manifest-recorded absolute interpreter with `-I` and `scripts/verify_phase2_package_provenance.py --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest`; ambient `python`, `py`, direct `-m pytest`, PATH, PYTHONPATH, or user-site output is invalid. Prove every executable/argv failure records zero adapter calls.</verification>
<success_criteria>D-09 through D-12 pass all T-08 through T-16 contracts with value-level executable/argv enforcement before adapters and no raw shell, GitHub/project-write authority, or uncontained process path.</success_criteria>
<output>Create `.planning/phases/02-capability-policy-and-execution-isolation/02-10-SUMMARY.md` when done.</output>
