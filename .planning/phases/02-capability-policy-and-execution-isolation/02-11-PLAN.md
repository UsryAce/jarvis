---
phase: 02-capability-policy-and-execution-isolation
plan: 11
type: execute
wave: 4
depends_on: [02-04, 02-06, 02-07, 02-08, 02-10]
files_modified: [src/browser/egress.py, src/browser/downloads.py, src/browser/artifacts.py, src/browser/broker.py, tests/test_browser_broker.py]
autonomous: true
requirements: [TOOL-05, TOOL-07]
must_haves:
  truths:
    - "A real approved Playwright driver process and the Chromium processes it spawns are observed inside the same Windows Job while alive, and broker close/cancel removes the entire job tree."
    - "D-13 through D-15 are enforced by fresh Playwright contexts plus a separate resolver-pinned socket boundary."
    - "Every redirect, subresource, popup and WebSocket traverses the run-local proxy; unprovable peer identity keeps live networking closed."
    - "Downloads/artifacts are bounded, quarantined, scanned, redacted and never auto-opened."
    - "Playwright is imported only after the manifest-bound isolated interpreter activates the approved package target and attests both module files and the distribution root beneath it; ambient, PYTHONPATH, cache, or preloaded-package substitution is impossible."
  artifacts:
    - {path: tests/test_browser_broker.py, provides: real Windows Job membership and cleanup proof for Playwright driver and Chromium descendants}
    - {path: src/browser/egress.py, provides: run-local pinned HTTP/CONNECT proxy and egress decisions}
    - {path: src/browser/broker.py, provides: governed ephemeral Playwright adapter}
    - {path: src/browser/downloads.py, provides: quarantine and promotion pipeline}
    - {path: src/browser/artifacts.py, provides: bounded redacted capture store}
    - {path: scripts/verify_phase2_package_provenance.py, provides: hostile-tested approved-target activation and Playwright module/distribution origin attestation required by every plan command and production import}
  key_links:
    - {from: src/browser/broker.py, to: src/execution/windows_job.py, via: the Playwright driver is the suspended assigned job root and Chromium descendants inherit the same non-breakaway Job}
    - {from: src/browser/broker.py, to: src/browser/egress.py, via: mandatory proxy with no bypass}
    - {from: src/browser/downloads.py, to: src/core/capability_store.py, via: artifact metadata and effect receipt}
    - {from: src/browser/broker.py, to: scripts/verify_phase2_package_provenance.py, via: approved-target activation and playwright.sync_api origin attestation occur before any Playwright import or driver start}
---

<objective>
Implement the ephemeral browser, resolver-pinned every-connection egress, quarantine download, and redacted artifact boundary.

Purpose: Deliver TOOL-05 without mistaking Playwright context isolation for SSRF protection.
Output: Four browser modules plus real Playwright driver/browser Windows Job containment evidence satisfying T-17 through T-23.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-04-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-06-SUMMARY.md
@tests/test_browser_broker.py
@src/execution/windows_job.py
@scripts/verify_phase2_package_provenance.py
@docs/security/phase-2-package-provenance.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement resolver-pinned egress and deny-on-unprovable-peer behavior</name>
  <files>src/browser/egress.py</files>
  <read_first>
    - src/browser/egress.py (target; confirm absent before creation)
    - tests/test_browser_broker.py
    - tests/fixtures/browser/fake_egress.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Canonical HTTP/HTTPS host/port policy validates all A/AAAA answers and denies alternate/private/reserved/metadata forms.
    - Run-local proxy resolves once, connects the selected vetted IP, preserves original Host/SNI/certificate behavior, checks peername and revalidates every connection.
    - Redirects, subresources, popups and WebSockets cannot bypass proxy; inability to prove peer causes stable deny and live closure.
  </behavior>
  <action>Per D-14, implement EgressPolicy, ResolutionDecision and PinnedEgressProxy using stdlib asyncio sockets as a run-local HTTP/CONNECT proxy. Accept only canonical HTTP/HTTPS, validate every A/AAAA answer, reject forbidden address classes and mixed answers, select one vetted IP, connect by that IP without a second lookup, preserve the requested host for Host/SNI/certificate validation, and confirm socket peername equals the selection. Configure no proxy bypass, deny QUIC/direct UDP and automatic redirects, and require every new connection/hop/subresource/popup/WebSocket to re-enter policy. Fail closed when tunneling, peer proof, or cleanup cannot be demonstrated.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import playwright.sync_api --run-module pytest -- -q tests/test_browser_broker.py -x -k 'egress or redirect or subresource or websocket or rebinding'; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - T-18 through T-20 pass including DNS-change and mixed-answer cases.
    - Browser-side DNS cannot select another peer after policy.
    - No direct network fallback exists.
  </acceptance_criteria>
  <done>The egress suite passes T-18 through T-20, browser-side DNS cannot change the selected peer, and no direct network fallback exists.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement bounded quarantine and redacted artifact storage</name>
  <files>src/browser/downloads.py, src/browser/artifacts.py</files>
  <read_first>
    - src/browser/downloads.py (target; confirm absent before creation)
    - src/browser/artifacts.py (target; confirm absent before creation)
    - src/browser/egress.py
    - src/execution/filesystem.py
    - src/execution/receipts.py
    - tests/test_browser_broker.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Downloads stream through pinned egress into run-owned quarantine with compressed/decompressed/time caps, hash/type/scanner checks and clean-only atomic promotion.
    - DOM/screenshot/HAR/console/accessibility projections are bounded and canary-redacted before persistence.
    - Failure/close removes quarantine and returns honest residue; nothing auto-opens or executes.
  </behavior>
  <action>Per D-15, implement QuarantineDownloader and BrowserArtifactStore. Deny browser-native downloads; explicit browser.download uses pinned egress, sanitized non-device filename, exclusive quarantine file, byte/time/decompression bounds, streaming SHA-256, declared/detected type comparison, injected scanner, and clean-only atomic promotion. Artifact projections use exact allowlists, size/count/time caps and Phase 1 canary redaction; database rows store safe metadata/reference only. Never call open/start/execute on promoted files.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import playwright.sync_api --run-module pytest -- -q tests/test_browser_broker.py -x -k 'download or artifact or cleanup'; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - T-21 through T-23 pass with no hostile promoted bytes or leaked canary.
    - Partial/failed downloads are removed or reported as residue.
    - Artifact metadata is bounded and content-free.
  </acceptance_criteria>
  <done>The download/artifact suite passes T-21 through T-23, no hostile bytes or canaries are persisted, and failed residue is removed or reported honestly.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Implement the governed ephemeral Playwright broker</name>
  <files>src/browser/broker.py, tests/test_browser_broker.py</files>
  <read_first>
    - src/browser/broker.py (target; confirm absent before creation)
    - src/browser/egress.py
    - src/browser/downloads.py
    - src/browser/artifacts.py
    - src/execution/windows_job.py
    - tests/test_browser_broker.py
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.md
  </read_first>
  <behavior>
    - Approved Chromium launches through WindowsJobBroker and uses one fresh non-persistent context with service workers blocked and downloads denied.
    - The actual Playwright Node driver is created suspended, assigned and verified in the non-breakaway Job before resume; its actual Chromium child/grandchild PIDs are members of that same Job while live.
    - A real Windows integration test queries Job membership for both driver and browser descendants, then proves normal close, cancellation and forced cleanup leave zero active processes.
    - Proxy/routes are installed before page creation; no storage/profile/extensions/cache import occurs.
    - Close verifies context, proxy, profile, files, ports and descendants; unproven cleanup is partial/unconfirmed.
    - Wrong interpreter/target, an ambient or preloaded Playwright module, or a module/distribution origin outside the approved target fails before driver import, process creation, or browser selection.
  </behavior>
  <action>Per D-13, write the real containment and import-origin tests before completing BrowserBroker/BrowserAdapter around the approved Playwright 1.61.0 Chromium artifact. Before `src/browser/broker.py` imports `playwright.sync_api` or any Playwright submodule, invoke the Plan 02-06 stdlib-first approved-target activation/attestation API against the canonical manifest; require the current canonical `sys.executable` to equal the manifest binding and require Playwright's distribution root plus every imported Playwright module `__file__` to resolve beneath `C:/Users/Usry/AppData/Local/Jarvis/phase-2/python-site`. Fail closed on a preloaded/ambient same-version module, missing/outside `__file__`, PYTHONPATH/user-site influence, target/link swap, or manifest/runtime drift. Every automated command uses the manifest-bound absolute interpreter with `-I` and the validator's `--activate-approved-target --require-import playwright.sync_api --run-module pytest` path; do not use ambient `python`, `py`, raw PYTHONPATH, or direct unvalidated `sys.path` edits. Do not let the Python Playwright transport call ambient asyncio/subprocess directly: make WindowsJobBroker create the package-declared Playwright Node driver as `CREATE_SUSPENDED | CREATE_NO_WINDOW`, assign it to the kill-on-close Job, verify membership with `IsProcessInJob`, prohibit breakaway, and only then resume and communicate over bounded inherited pipes. Pass only the exact executable returned by `approved_chromium_executable`; Chromium and utility descendants must inherit that same Job. The test must use the real approved Node driver and Chromium bytes on Windows, open a fresh context/page without external navigation, capture the driver PID and actual Chromium descendant PIDs from job accounting/process evidence, assert each is alive and `IsProcessInJob(..., broker_job_handle)` is true, and assert at least one driver plus one Chromium process (not only the test process or static context flags). Then exercise normal close, cancellation during a live page, and forced job close; poll boundedly until `JOBOBJECT_BASIC_ACCOUNTING_INFORMATION.ActiveProcesses == 0` and assert no recorded descendant, profile directory, pipe, port, or lock survives. The proof cannot be a mocked launcher, fixture-only adapter, source-text assertion, or a test skipped/xfail when Plan 02-06 approved artifacts exist; assignment/membership uncertainty stops before driver resume and keeps live selection closed. Create a fresh context per action with `storage_state=None`, `service_workers='block'`, `accept_downloads=False`, no extensions/profile import, and context routes before pages. Force all connections through PinnedEgressProxy with no bypass. Support exact navigate, download and bounded capture actions only; close context/browser/proxy and verify all residue. Keep live selection disabled unless the release state from 02-14 is open.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope chromium --require-status approved --verify-current-bytes --no-execute; if ($LASTEXITCODE) { exit $LASTEXITCODE }; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import playwright.sync_api --run-module pytest -- -q tests/test_browser_broker.py::test_playwright_import_origin_is_approved_target tests/test_browser_broker.py::test_real_playwright_driver_and_chromium_descendants_share_job_and_cleanup -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - T-17 through T-23 all pass against approved Chromium and offline/live-controlled drills.
    - The real test observes at least one live Playwright driver PID and one live Chromium PID in the same Job, and job accounting reaches zero after close and cancel.
    - No ambient state or direct network route exists.
    - Playwright's module files and distribution root attest beneath the approved target before any real driver/browser process starts.
    - Production selection remains closed before release evidence.
  </acceptance_criteria>
  <done>The complete approved-Chromium browser suite passes, ambient state/direct networking are absent, and production selection remains closed pending release evidence.</done>
</task>
</tasks>

## Artifacts this phase produces

- `EgressPolicy`, `ResolutionDecision`, `PinnedEgressProxy` in `src/browser/egress.py`.
- `QuarantineDownloader`, `DownloadEvidence` in `src/browser/downloads.py`.
- `BrowserArtifactStore`, `ArtifactPolicy` in `src/browser/artifacts.py`.
- `BrowserBroker`, `BrowserAdapter`, `BrowserOutcome` in `src/browser/broker.py`.
- Real driver/Chromium same-Job membership and cleanup coverage in `tests/test_browser_broker.py`.
- Approved-target Playwright module/distribution origin and pre-driver fail-closed coverage in `tests/test_browser_broker.py`, executed only through the Plan 02-06 isolated activation runner.

<threat_model>
Attacker goals: browser-state theft, SSRF/rebinding, malicious download, artifact exfiltration, or browser residue.
Trust boundaries: context-to-proxy, resolver-to-peer socket, bytes-to-quarantine/artifacts.

| Threat IDs | STRIDE | Abuse path | Mitigation | Verification |
|---|---|---|---|---|
| T-17 | Disclosure | ambient profile | fresh context/no imports | browser suite |
| T-18..T-20 | Disclosure/Elevation | alternate URL/hop/DNS/direct socket | mandatory pinned proxy and peer proof | egress suite |
| T-21..T-23 | Tampering/DoS/Disclosure | hostile bytes/artifacts/residue | bounds, quarantine, scan, redaction, cleanup truth | browser suite |
| T-02-JOB | Elevation/Repudiation | Playwright driver launches before assignment or Chromium descendants escape/are only statically claimed contained | suspend/assign/verify driver before resume, deny breakaway, query every real descendant and zero active processes after close | real approved Playwright/Chromium Windows Job integration test |
| T-02-SC | Tampering | browser artifact substitution | approved package/executable hash | provenance receipt |
| T-02-IMPORT | Tampering/Elevation | ambient, PYTHONPATH, preloaded, or same-version Playwright package bypasses the approved target | manifest-bound `-I` activation plus required module `__file__` and distribution-root attestation before driver import/start | provenance activation tests and browser import-origin test |

Residual risk: fixture success does not open production; 02-14 requires a real controlled peer/rebinding, download and cleanup drill.
</threat_model>
<verification>Through the manifest-recorded absolute interpreter with `-I` and the provenance validator's approved-target runner, attest `playwright.sync_api` and its distribution beneath the approved target, run the named real Windows Job test without mock/skip/xfail, then the entire browser suite. Inspect live driver and Chromium Job membership, zero active processes after close/cancel, and that the live gate stays closed unless explicit release state exists; ambient `python`, `py`, or PYTHONPATH is never valid evidence.</verification>
<success_criteria>D-13 through D-15 satisfy TOOL-05 with actual pinned-peer proof and real same-Job driver/browser cleanup evidence, or live browser authority remains closed.</success_criteria>
<output>Create `.planning/phases/02-capability-policy-and-execution-isolation/02-11-SUMMARY.md` when done.</output>
