---
phase: 02-capability-policy-and-execution-isolation
plan: 11
type: execute
wave: 3
depends_on: [02-04, 02-06, 02-07, 02-08, 02-10]
files_modified: [src/browser/egress.py, src/browser/downloads.py, src/browser/artifacts.py, src/browser/broker.py]
autonomous: true
requirements: [TOOL-05, TOOL-07]
must_haves:
  truths:
    - "D-13 through D-15 are enforced by fresh Playwright contexts plus a separate resolver-pinned socket boundary."
    - "Every redirect, subresource, popup and WebSocket traverses the run-local proxy; unprovable peer identity keeps live networking closed."
    - "Downloads/artifacts are bounded, quarantined, scanned, redacted and never auto-opened."
  artifacts:
    - {path: src/browser/egress.py, provides: run-local pinned HTTP/CONNECT proxy and egress decisions}
    - {path: src/browser/broker.py, provides: governed ephemeral Playwright adapter}
    - {path: src/browser/downloads.py, provides: quarantine and promotion pipeline}
    - {path: src/browser/artifacts.py, provides: bounded redacted capture store}
  key_links:
    - {from: src/browser/broker.py, to: src/browser/egress.py, via: mandatory proxy with no bypass}
    - {from: src/browser/downloads.py, to: src/core/capability_store.py, via: artifact metadata and effect receipt}
---

<objective>
Implement the ephemeral browser, resolver-pinned every-connection egress, quarantine download, and redacted artifact boundary.

Purpose: Deliver TOOL-05 without mistaking Playwright context isolation for SSRF protection.
Output: Four browser modules satisfying T-17 through T-23.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-04-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-06-SUMMARY.md
@tests/test_browser_broker.py
@src/execution/windows_job.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement resolver-pinned egress and deny-on-unprovable-peer behavior</name>
  <files>src/browser/egress.py</files>
  <read_first>
    - src/browser/egress.py (target; confirm absent before creation)
    - tests/test_browser_broker.py
    - tests/fixtures/browser/fake_egress.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
  </read_first>
  <behavior>
    - Canonical HTTP/HTTPS host/port policy validates all A/AAAA answers and denies alternate/private/reserved/metadata forms.
    - Run-local proxy resolves once, connects the selected vetted IP, preserves original Host/SNI/certificate behavior, checks peername and revalidates every connection.
    - Redirects, subresources, popups and WebSockets cannot bypass proxy; inability to prove peer causes stable deny and live closure.
  </behavior>
  <action>Per D-14, implement EgressPolicy, ResolutionDecision and PinnedEgressProxy using stdlib asyncio sockets as a run-local HTTP/CONNECT proxy. Accept only canonical HTTP/HTTPS, validate every A/AAAA answer, reject forbidden address classes and mixed answers, select one vetted IP, connect by that IP without a second lookup, preserve the requested host for Host/SNI/certificate validation, and confirm socket peername equals the selection. Configure no proxy bypass, deny QUIC/direct UDP and automatic redirects, and require every new connection/hop/subresource/popup/WebSocket to re-enter policy. Fail closed when tunneling, peer proof, or cleanup cannot be demonstrated.</action>
  <verify><automated>python -m pytest -q tests/test_browser_broker.py -x -k "egress or redirect or subresource or websocket or rebinding"</automated></verify>
  <acceptance_criteria>
    - T-18 through T-20 pass including DNS-change and mixed-answer cases.
    - Browser-side DNS cannot select another peer after policy.
    - No direct network fallback exists.
  </acceptance_criteria>
  <done>The egress suite passes T-18 through T-20, browser-side DNS cannot change the selected peer, and no direct network fallback exists.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement bounded quarantine and redacted artifact storage</name>
  <files>src/browser/downloads.py, src/browser/artifacts.py</files>
  <read_first>
    - src/browser/downloads.py (target; confirm absent before creation)
    - src/browser/artifacts.py (target; confirm absent before creation)
    - src/browser/egress.py
    - src/execution/filesystem.py
    - src/execution/receipts.py
    - tests/test_browser_broker.py
  </read_first>
  <behavior>
    - Downloads stream through pinned egress into run-owned quarantine with compressed/decompressed/time caps, hash/type/scanner checks and clean-only atomic promotion.
    - DOM/screenshot/HAR/console/accessibility projections are bounded and canary-redacted before persistence.
    - Failure/close removes quarantine and returns honest residue; nothing auto-opens or executes.
  </behavior>
  <action>Per D-15, implement QuarantineDownloader and BrowserArtifactStore. Deny browser-native downloads; explicit browser.download uses pinned egress, sanitized non-device filename, exclusive quarantine file, byte/time/decompression bounds, streaming SHA-256, declared/detected type comparison, injected scanner, and clean-only atomic promotion. Artifact projections use exact allowlists, size/count/time caps and Phase 1 canary redaction; database rows store safe metadata/reference only. Never call open/start/execute on promoted files.</action>
  <verify><automated>python -m pytest -q tests/test_browser_broker.py -x -k "download or artifact or cleanup"</automated></verify>
  <acceptance_criteria>
    - T-21 through T-23 pass with no hostile promoted bytes or leaked canary.
    - Partial/failed downloads are removed or reported as residue.
    - Artifact metadata is bounded and content-free.
  </acceptance_criteria>
  <done>The download/artifact suite passes T-21 through T-23, no hostile bytes or canaries are persisted, and failed residue is removed or reported honestly.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Implement the governed ephemeral Playwright broker</name>
  <files>src/browser/broker.py</files>
  <read_first>
    - src/browser/broker.py (target; confirm absent before creation)
    - src/browser/egress.py
    - src/browser/downloads.py
    - src/browser/artifacts.py
    - src/execution/windows_job.py
    - tests/test_browser_broker.py
    - docs/security/phase-2-package-provenance.md
  </read_first>
  <behavior>
    - Approved Chromium launches through WindowsJobBroker and uses one fresh non-persistent context with service workers blocked and downloads denied.
    - Proxy/routes are installed before page creation; no storage/profile/extensions/cache import occurs.
    - Close verifies context, proxy, profile, files, ports and descendants; unproven cleanup is partial/unconfirmed.
  </behavior>
  <action>Per D-13, implement BrowserBroker/BrowserAdapter around the approved Playwright 1.61.0 Chromium artifact, launched through WindowsJobBroker. Create a fresh context per action with storage_state None, service_workers block, accept_downloads false, no extensions/profile import, and context routes before pages. Force all connections through PinnedEgressProxy with no bypass. Support exact navigate, download and bounded capture actions only; close context/browser/proxy and verify all residue. Keep live selection disabled unless the release state from 02-14 is open.</action>
  <verify><automated>python -m pytest -q tests/test_browser_broker.py -x</automated></verify>
  <acceptance_criteria>
    - T-17 through T-23 all pass against approved Chromium and offline/live-controlled drills.
    - No ambient state or direct network route exists.
    - Production selection remains closed before release evidence.
  </acceptance_criteria>
  <done>The complete approved-Chromium browser suite passes, ambient state/direct networking are absent, and production selection remains closed pending release evidence.</done>
</task>
</tasks>

## Artifacts this phase produces

- `EgressPolicy`, `ResolutionDecision`, `PinnedEgressProxy` in `src/browser/egress.py`.
- `QuarantineDownloader`, `DownloadEvidence` in `src/browser/downloads.py`.
- `BrowserArtifactStore`, `ArtifactPolicy` in `src/browser/artifacts.py`.
- `BrowserBroker`, `BrowserAdapter`, `BrowserOutcome` in `src/browser/broker.py`.

<threat_model>
Attacker goals: browser-state theft, SSRF/rebinding, malicious download, artifact exfiltration, or browser residue.
Trust boundaries: context-to-proxy, resolver-to-peer socket, bytes-to-quarantine/artifacts.

| Threat IDs | STRIDE | Abuse path | Mitigation | Verification |
|---|---|---|---|---|
| T-17 | Disclosure | ambient profile | fresh context/no imports | browser suite |
| T-18..T-20 | Disclosure/Elevation | alternate URL/hop/DNS/direct socket | mandatory pinned proxy and peer proof | egress suite |
| T-21..T-23 | Tampering/DoS/Disclosure | hostile bytes/artifacts/residue | bounds, quarantine, scan, redaction, cleanup truth | browser suite |
| T-02-SC | Tampering | browser artifact substitution | approved package/executable hash | provenance receipt |

Residual risk: fixture success does not open production; 02-14 requires a real controlled peer/rebinding, download and cleanup drill.
</threat_model>
<verification>Run the entire browser suite and inspect that its live gate stays closed unless explicit release state exists.</verification>
<success_criteria>D-13 through D-15 satisfy TOOL-05 with actual pinned-peer proof or remain closed.</success_criteria>
<output>Create `.planning/phases/02-capability-policy-and-execution-isolation/02-11-SUMMARY.md` when done.</output>
