---
phase: 02-capability-policy-and-execution-isolation
plan: 12
type: execute
wave: 5
depends_on: [02-09, 02-10, 02-11]
files_modified: [src/core/control_store.py, src/core/capability_store.py, src/core/execution_gateway.py, src/api/capability_routes.py, src/api/server.py, tests/test_control_migrations_backup.py, tests/test_execution_gateway_integration.py, tests/test_api_auth_matrix.py, tests/test_origin_csrf.py]
autonomous: true
requirements: [CTRL-02, CTRL-03, CTRL-06, TOOL-03, TOOL-05, TOOL-07]
must_haves:
  truths:
    - "An append-only migration stores closed/open release state together with evidence digest, source commit, and release version; prior unbound rows migrate closed."
    - "Release state can be read or opened only through an active ControlStore transaction with revision checks and same-transaction audit, never through an unbound convenience read."
    - "One sealed ResolvedAction -> PolicyDecision -> Approval -> atomic Reservation -> typed Adapter -> durable Receipt/Reconciliation gateway is the only execution entry."
    - "Issuer authority is absent from the gateway and the entire capability router remains unregistered while the atomic Phase 1+2 capability release state is not open."
    - "Trusted actor/session and resolved authority come from request state and stores, never request/model JSON."
    - "A real production `create_app` rebuilt after a matching release registers and serves the typed capability HTTP flow with production stores, gateway, adapters, and issuer-only approval wiring; closed/unbound/mismatched or pre-release application instances expose no capability routes and no fixture adapter enters the production graph."
  artifacts:
    - {path: src/core/control_store.py, provides: immutable release-binding schema migration}
    - {path: src/core/capability_store.py, provides: transaction-only release read/open APIs and typed record}
    - {path: tests/test_control_migrations_backup.py, provides: fresh/upgrade/restore/checksum and closed-unbound migration coverage}
    - {path: tests/test_execution_gateway_integration.py, provides: production gateway allow/ask/deny/stale/ambiguous/closed-gate tests before router wiring}
    - {path: tests/test_api_auth_matrix.py, provides: exact per-route scope and missing/invalid/expired/wrong-scope HTTP matrix for released capability routes}
    - {path: tests/test_origin_csrf.py, provides: exact Origin and synchronizer-CSRF matrix for every released capability mutation route}
    - {path: src/core/execution_gateway.py, provides: single ordered execution gateway and release gate}
    - {path: src/api/capability_routes.py, provides: authenticated propose/approve/execute/query routes}
    - {path: src/api/server.py, provides: production-unregistered capability router plus explicit test-only fixture registration}
  key_links:
    - {from: src/core/execution_gateway.py, to: src/core/capability_store.py, via: active transaction reads an evidence/commit/version-bound open record before policy, reservation, or dispatch}
    - {from: src/api/capability_routes.py, to: src/core/execution_gateway.py, via: trusted principal plus untrusted proposal}
    - {from: src/core/execution_gateway.py, to: src/core/capability_store.py, via: reserve-before-dispatch and receipt/reconciliation}
    - {from: tests/test_execution_gateway_integration.py, to: src/api/server.py, via: real production create_app is reconstructed against closed/unbound/mismatched and matching-open release records, then execute/query/reconcile HTTP behavior is asserted with zero fixture adapters}
---

<objective>
Add immutable evidence-bound release storage and production gateway behavior tests, then assemble the single execution gateway and authenticated capability API while keeping production authority release-gated.

Purpose: Connect all independently tested boundaries in the only allowed order without opening legacy raw execution.
Output: Append-only release migration, transaction-only release APIs, production gateway integration coverage, CapabilityReleaseGate, capability routes, and injected application wiring.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-09-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-10-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-11-SUMMARY.md
@tests/test_execution_gateway_integration.py
@src/security/auth.py
@scripts/verify_phase2_package_provenance.py
@docs/security/phase-2-package-provenance.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Append immutable release binding schema and transaction-only APIs</name>
  <files>src/core/control_store.py, src/core/capability_store.py, tests/test_control_migrations_backup.py</files>
  <read_first>
    - src/core/control_store.py
    - src/core/capability_store.py
    - tests/test_control_migrations_backup.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-08-SUMMARY.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-READINESS-AUDIT.md
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Fresh creation, upgrade from the audited v5 schema, backup/restore, and repeated startup converge on the same v6 schema/checksum without editing migrations v1-v5.
    - Any pre-v6 release row becomes closed and unbound; it cannot inherit/open authority without a new evidence digest, exact source commit, release version, revision CAS, and same-transaction audit.
    - `read_release_state` and `open_release_state` reject a missing, foreign, inactive, committed, or rolled-back ControlStoreTransaction; no non-transaction release read/open API exists.
    - Open requires the singleton row to be closed at `expected_revision`, validates canonical digest/commit/version formats and bounded reason/time values, writes all bindings plus state atomically, increments revision once, and rolls back the state if its audit append fails.
  </behavior>
  <action>Write migration and transaction tests first. Append a new `_Migration` v6 after the immutable audited v1-v5 migration sequence; do not edit prior SQL or checksum literals. The migration must add explicit `evidence_digest`, `source_commit`, `release_version`, `opened_at`, and `closed_at` binding columns (or an equivalently constrained singleton replacement table), preserve the singleton/check constraints, and force every legacy/unbound row to canonical `closed` with null bindings and a bounded migration reason. Add an immutable `CapabilityReleaseRecord` projection and CapabilityStore methods `read_release_state(tx)` and `open_release_state(tx, *, expected_revision, evidence_digest, source_commit, release_version, opened_at, reason, audit_context)` that require the caller's currently active ControlStore transaction. Opening uses compare-and-swap from closed at the expected revision, accepts one canonical SHA-256 evidence digest, one exact full source commit, and one bounded release-version identifier, persists all fields with `state='open'`, appends the safe release-open audit event in that same transaction, and returns the new record. Missing/malformed bindings, already-open state, stale revision, transaction mismatch, or audit failure leaves the row closed/unchanged with stable fail-closed errors. Do not add a convenience method that opens or reads through its own hidden transaction, and do not open any production row in this plan. Extend tests for fresh v6 creation, v5 upgrade, prior-row forced closed, immutable checksum detection, backup/restore, idempotent startup, open-once success, stale/double-open denial, foreign/inactive transaction denial, and audit-triggered rollback.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --run-module pytest -- -q tests/test_control_migrations_backup.py -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - Migration v6 is append-only and all v1-v5 SQL/checksum contracts remain byte-for-byte unchanged.
    - Release state contains state, revision, evidence digest, source commit, release version, open/closed timestamps, and reason; unbound legacy state is closed.
    - All release reads and opens require one caller-owned active transaction and same-transaction audit/CAS behavior.
    - Fresh, upgrade, restore, stale, double-open, transaction misuse, and rollback tests pass.
  </acceptance_criteria>
  <done>The immutable migration and hostile store tests prove release authority is transaction-only, revisioned, and inseparably bound to evidence digest, exact source commit, release version, and closed/open state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove production gateway ordering and implement the release gate</name>
  <files>src/core/execution_gateway.py, tests/test_execution_gateway_integration.py</files>
  <read_first>
    - src/core/execution_gateway.py (target; confirm absent before creation)
    - src/security/action_contracts.py
    - src/security/policy.py
    - src/security/approvals.py
    - src/core/control_store.py
    - src/core/capability_store.py
    - src/execution/local_adapters.py
    - src/browser/broker.py
    - tests/test_execution_gateway_integration.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Real production ManifestRegistry, PolicyKernel, ApprovalVerifier, ControlStore/CapabilityStore, AuditService, ControlService, release gate, and ExecutionGateway execute allow/ask/deny paths in the required order before any router exists.
    - Deny never reserves/dispatches; ask requires one exact real signed envelope and rejects replay; allow still rechecks current truth/reserves; stale policy/manifest/workspace/release revisions never dispatch.
    - An ambiguous adapter outcome persists uncertain/partial truth and requires reconciliation; it is never replayed as a fresh effect.
    - CapabilityReleaseGate reads the singleton through the caller-owned transaction, matches evidence digest/source commit/release version and defaults closed; missing/unbound/closed/malformed/stale state yields zero adapter calls.
  </behavior>
  <action>Before any capability router is written or included, replace reference/import/static-context tests with integration tests that instantiate the real production classes listed in the behavior block against an isolated real SQLite ControlStore. Use the production ApprovalIssuer only in test setup to create an exact signed ASK envelope, but inject only the public ApprovalVerifier into ExecutionGateway. Use the actual immutable manifests/policy/reservation/effect/audit/release APIs; the sole test seam may be a terminal recording adapter implementing the production typed adapter protocol so tests can count dispatch without causing an OS effect. Exercise an evidence-bound open row created through Task 1's transaction-only API for allow/ask cases, and separately missing, closed, unbound, digest-mismatched, commit-mismatched, version-mismatched, and stale-revision rows. Assert real ALLOW dispatches once; real ASK without/wrong/stale/replayed approval dispatches zero and an exact approval dispatches once; real DENY dispatches zero; policy/manifest/workspace/release drift after seal dispatches zero; ambiguous adapter return/exception records uncertain applied truth and reconcile intent without a second dispatch; and emergency stop wins before dispatch. Only after those tests fail, implement ExecutionGateway with injected ManifestRegistry, PolicyKernel, public-only ApprovalVerifier, CapabilityStore, AuditService, ControlService, registered workspace resolver, LocalAdapterRegistry and BrowserAdapter. It accepts only trusted request context plus untrusted proposal, resolves D-01 fields from principal/stores, seals once, evaluates, verifies exact approval when required, opens one ControlStore transaction, uses `read_release_state(tx)` to require the expected evidence digest/source commit/release version and revision, atomically reserves/current-checks, dispatches one typed adapter, and persists safe receipt/reconciliation. Never use a non-transaction release query or cache an open decision across executions. Fixture adapters require explicit test construction and can never be selected by production `create_app`.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_execution_gateway_integration.py tests/test_effect_reconciliation.py -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - Real production-class integration tests, written before router wiring, pass for allow/ask/deny, stale/drift, closed/unbound release, stop, ambiguity and receipt truth.
    - The only terminal test seam counts dispatch; all pre-adapter failures prove zero calls and ambiguous completion proves no replay.
    - Gateway object graph has no issuer/private signer or raw adapter call escape.
    - Closed release state leaves all production dispatch unreachable.
  </acceptance_criteria>
  <done>The gateway/effect suites pass the ordered allow/ask/deny and ambiguity cases, the object graph has public verification only, and closed release state makes production dispatch unreachable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Add protected capability routes and injected app construction</name>
  <files>src/api/capability_routes.py, src/api/server.py, tests/test_execution_gateway_integration.py, tests/test_api_auth_matrix.py, tests/test_origin_csrf.py</files>
  <read_first>
    - src/api/capability_routes.py (target; confirm absent before creation)
    - src/api/server.py
    - src/security/auth.py
    - src/api/dashboard_routes.py
    - tests/test_api_auth_matrix.py
    - tests/test_origin_csrf.py
    - tests/test_execution_gateway_integration.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Propose/approve/execute/query routes require exact scopes, session, Origin/CSRF and request.state OperatorPrincipal only after the whole router is release-registered.
    - Payload cannot supply actor/session/resolved boundary/policy/manifest/effect fields; extras reject before gateway.
    - Closed or invalid release state leaves the whole router unregistered, so no body parsing, policy evaluation, approval issuance, query disclosure, reconciliation, or sink access can occur.
    - Real production `create_app` route inventory and authenticated HTTP requests prove closed, unbound, digest/commit/version/revision-mismatched release records expose no capability routes; a matching open record exposes all five routes only after a new application instance is constructed.
    - Opening a matching isolated release after a closed application was constructed does not mutate that running app's route table; reconstructing/restarting `create_app` against the same matching record registers the routes and serves propose/approve/execute/receipt/reconcile with production dependencies.
    - Positive production HTTP coverage uses real ManifestRegistry, PolicyKernel, ApprovalVerifier, ControlStore/CapabilityStore, ExecutionGateway, LocalAdapterRegistry/BrowserAdapter and an isolated registered workspace; no terminal recording, fixture, tests.*, or fixture-only adapter is reachable from production construction, and ApprovalIssuer exists only in `/approve` wiring.
  </behavior>
  <action>Only after Task 2's real production gateway cases pass, extend all three owned test modules first, then add `/api/capabilities/propose`, `/approve`, `/execute`, `/receipts/{id}`, and `/reconcile/{id}` with strict request/response DTOs, safe correlation/applied/retryable truth, exact scopes, and Phase 1 Origin/CSRF/session controls. Assign `/propose` and `/execute` `operator.execute`, `/approve` `approvals.write`, `/receipts/{id}` `operator.read`, and `/reconcile/{id}` `operator.execute`. In `tests/test_api_auth_matrix.py`, add `test_released_capability_routes_have_exact_scopes_and_auth_matrix` to assert all five released routes carry those exact scopes and cover missing, invalid, expired, and wrong-scope sessions with 401/403 before handlers. In `tests/test_origin_csrf.py`, add `test_released_capability_mutations_require_exact_origin_and_csrf` to parametrically prove every unsafe released POST rejects missing/non-exact Origin and missing/invalid synchronizer CSRF with `applied=false`, while exact Origin+token crosses the guard; receipt GET remains authenticated but is not incorrectly made a CSRF mutation. Approval issuance route alone receives ApprovalIssuer; execution, query, and reconciliation receive only ExecutionGateway/public verification/store capabilities. In production `create_app`, open an explicit read transaction and call only Task 1's `read_release_state(tx)` before router inclusion; require `state='open'` and exact equality of the persisted evidence digest, full source commit, release version, and expected revision with the immutable release configuration. Never open or repair release state during application construction. Missing, unbound, closed, malformed, stale, digest/commit/version/revision-mismatched, or transaction-error state leaves every capability path absent from the route table before request body parsing or handler/dependency construction. Make startup/reconstruction semantics explicit and test them: build an app against a closed isolated store and prove all five paths absent; open the matching row afterward and prove the existing app remains unchanged/absent; then construct a new app against the same store/config and prove all five paths are registered. A matching-open app must still re-read release truth inside ExecutionGateway before every execute/reconcile operation, so startup registration never becomes cached dispatch authority. Add named real production HTTP tests in `tests/test_execution_gateway_integration.py`: `test_production_create_app_capabilities_absent_for_closed_unbound_or_mismatched_release`, `test_production_create_app_requires_reconstruction_after_matching_release_opens`, `test_production_create_app_serves_matching_release_execute_query_and_reconcile`, and `test_production_create_app_keeps_issuer_approval_only_and_has_zero_fixture_adapters`. Construct isolated release rows only through `open_release_state(tx)` and instantiate `src.api.server.create_app` without any fixture/test dependency flag. Assert production dependency types/modules contain no terminal-recording adapter, `tests`, or fixture implementation; issuer is reachable only from the approve command handler and is absent from ExecutionGateway/execute/query/reconcile graphs. With a real isolated registered workspace and real production typed local adapter, perform an authenticated/Origin+CSRF matching-open propose→execute request for a bounded non-destructive action, fetch its durable receipt, and invoke reconcile to prove the real production route/store/gateway link (authoritative reconcile result when eligible, stable no-reconcile result otherwise) without a fixture adapter. Cover exact signed ASK through `/approve` as issuer-only wiring and prove wrong/stale/replayed approval produces zero dispatch. Controlled fixture registration, if retained for lower-level tests, requires an explicit test-only constructor that production `create_app` rejects and is never used by these named tests. Register released routes in the central default-deny inventory. Run the tests only through the manifest-bound `-I` approved-target runner with cryptography and Playwright origins attested; do not alter `/api/code/execute` here.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_execution_gateway_integration.py tests/test_api_auth_matrix.py tests/test_origin_csrf.py tests/test_dashboard_execution_gate.py -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - Auth/origin/CSRF/wrong-scope matrices cover all new routes.
    - Issuer is reachable only in approval command wiring.
    - Before release, no production capability endpoint is registered or can parse a body, issue approval, disclose a query, reconcile, or reach a sink.
    - A matching-open reconstructed production app serves execute/receipt/reconcile through real production dependencies, while the already-running closed app remains unchanged until reconstruction/restart.
    - Production HTTP tests contain zero fixture/terminal-recording adapters and prove exact ASK approval wiring is issuer-only.
  </acceptance_criteria>
  <done>Auth/Origin/CSRF matrices pass for all five routes; closed/unbound/mismatched production construction registers none; a newly reconstructed matching-open production app serves execute/query/reconcile with real dependencies and issuer-only approval wiring; and zero fixture adapters enter the production graph.</done>
</task>
</tasks>

## Artifacts this phase produces

- Immutable v6 release-binding migration in `src/core/control_store.py` and transaction-only `CapabilityReleaseRecord`, `read_release_state`, and `open_release_state` APIs in `src/core/capability_store.py`.
- Fresh/upgrade/restore/checksum/revision/rollback coverage in `tests/test_control_migrations_backup.py`.
- Real production-class allow/ask/deny/stale/ambiguous/closed-gate coverage in `tests/test_execution_gateway_integration.py`, written before router wiring.
- Real production `create_app` closed/unbound/mismatch absence, matching-open reconstruction, execute/receipt/reconcile, issuer-only, and zero-fixture-adapter HTTP coverage in `tests/test_execution_gateway_integration.py`.
- Exact released-route scope/session matrix in `tests/test_api_auth_matrix.py` and unsafe-route Origin/CSRF matrix in `tests/test_origin_csrf.py`.
- `ExecutionGateway`, `TrustedActionContext`, `CapabilityReleaseGate`, `execute`, `reconcile` in `src/core/execution_gateway.py`.
- Capability propose/approve/execute/receipt/reconcile route handlers and strict DTOs in `src/api/capability_routes.py`.

<threat_model>
Attacker goals: inherit authority from an unbound legacy release row, bypass transaction/revision ordering, inject trusted identity, mint approval through gateway, or reach adapters while release is closed.
Trust boundaries: migration-to-release record, transaction-to-release open/read, HTTP-to-principal/proposal, gateway stages, release-state-to-adapter registry.

| Threat IDs | STRIDE | Abuse path | Mitigation | Verification |
|---|---|---|---|---|
| T-01..T-07 | Spoofing/Tampering/Elevation | injected/stale/replayed action | trusted resolver, policy, public verifier, atomic reserve | integration suites |
| T-02-REL | Tampering/Elevation | legacy/unbound or stale release row opens live authority | append-only migration forces closed; transaction-only CAS binds digest, commit, version, revision and audit | migration/store and real production gateway suites |
| T-24 | Elevation | direct adapter or closed-gate dispatch | gateway-only registry and durable default-closed release state | sink/integration suites |
| T-25 | Repudiation/Disclosure | unsafe/missing evidence | same-transaction audit and receipt projection | integration suite |
| T-02-ROUTE | Elevation/Spoofing | startup registration caches stale release, production wiring substitutes a fixture adapter, or issuer leaks into execute/query/reconcile | absent routes until matching open at construction, explicit reconstruction after open, per-operation release re-read, production object-graph assertions, issuer-only approve route | production create_app HTTP and auth/Origin/CSRF suites |

Residual risk: legacy AgentRuntime/skills still contain sinks until 02-13; production release remains closed.
</threat_model>
<verification>Run every suite through the manifest-bound absolute interpreter with `-I` and approved-target import attestation. Run migration/restore tests first, then real production gateway allow/ask/deny/stale/ambiguous/closed-gate tests before route tests; finally run the named production `create_app` absence/matching-open/reconstruction/execute/query/reconcile/issuer-only/zero-fixture cases, exact auth/Origin/CSRF matrices, effect tests, and dashboard gate suite. Assert absent/unbound/closed/mismatched release records register no production route or adapter call and an application built before open remains route-closed until explicitly reconstructed.</verification>
<success_criteria>All six phase requirements traverse one closed-by-default ordered gateway whose transaction-only release record binds evidence digest, exact source commit, release version and revision, with separated issuer/verifier authority.</success_criteria>
<output>Create `.planning/phases/02-capability-policy-and-execution-isolation/02-12-SUMMARY.md` when done.</output>
