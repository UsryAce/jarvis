---
phase: 02-capability-policy-and-execution-isolation
plan: 12
type: execute
wave: 4
depends_on: [02-09, 02-10, 02-11]
files_modified: [src/core/execution_gateway.py, src/api/capability_routes.py, src/api/server.py]
autonomous: true
requirements: [CTRL-02, CTRL-03, CTRL-06, TOOL-03, TOOL-05, TOOL-07]
must_haves:
  truths:
    - "One sealed ResolvedAction -> PolicyDecision -> Approval -> atomic Reservation -> typed Adapter -> durable Receipt/Reconciliation gateway is the only execution entry."
    - "Issuer authority is absent from the gateway and the entire capability router remains unregistered while the atomic Phase 1+2 capability release state is not open."
    - "Trusted actor/session and resolved authority come from request state and stores, never request/model JSON."
  artifacts:
    - {path: src/core/execution_gateway.py, provides: single ordered execution gateway and release gate}
    - {path: src/api/capability_routes.py, provides: authenticated propose/approve/execute/query routes}
    - {path: src/api/server.py, provides: production-unregistered capability router plus explicit test-only fixture registration}
  key_links:
    - {from: src/api/capability_routes.py, to: src/core/execution_gateway.py, via: trusted principal plus untrusted proposal}
    - {from: src/core/execution_gateway.py, to: src/core/capability_store.py, via: reserve-before-dispatch and receipt/reconciliation}
---

<objective>
Assemble the required single execution gateway and authenticated capability API while keeping production authority release-gated.

Purpose: Connect all independently tested boundaries in the only allowed order without opening legacy raw execution.
Output: ExecutionGateway, CapabilityReleaseGate, capability routes, and injected application wiring.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-09-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-10-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-11-SUMMARY.md
@tests/test_execution_gateway_integration.py
@src/security/auth.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the only ordered execution gateway and release gate</name>
  <files>src/core/execution_gateway.py</files>
  <read_first>
    - src/core/execution_gateway.py (target; confirm absent before creation)
    - src/security/action_contracts.py
    - src/security/policy.py
    - src/security/approvals.py
    - src/core/capability_store.py
    - src/execution/local_adapters.py
    - src/browser/broker.py
    - tests/test_execution_gateway_integration.py
  </read_first>
  <behavior>
    - Trusted resolver seals action, policy evaluates, approval verifies if ask, transaction reserves, adapter dispatches, and receipt/reconciliation persists in that order.
    - Deny never reserves/dispatches; ask without exact approval never dispatches; allow still rechecks current truth/reserves.
    - CapabilityReleaseGate defaults closed and blocks every non-fixture adapter until durable release evidence opens it.
  </behavior>
  <action>Implement ExecutionGateway with injected ManifestRegistry, PolicyKernel, public-only ApprovalVerifier, CapabilityStore, AuditService, ControlService, registered workspace resolver, LocalAdapterRegistry and BrowserAdapter. It accepts only a trusted request context plus untrusted proposal, resolves D-01 fields from principal/stores, seals once, evaluates, verifies exact approval when required, atomically reserves/current-checks, dispatches one typed adapter, and persists safe receipt/reconciliation. Implement CapabilityReleaseGate backed by singleton ControlStore release state; default/missing/invalid/stale state is closed. Fixture adapters require explicit testing injection and can never be selected by production construction.</action>
  <verify><automated>python -m pytest -q tests/test_execution_gateway_integration.py tests/test_effect_reconciliation.py -x</automated></verify>
  <acceptance_criteria>
    - Ordered integration tests pass for allow/ask/deny, stale/drift, stop, ambiguity and receipt truth.
    - Gateway object graph has no issuer/private signer or raw adapter call escape.
    - Closed release state leaves all production dispatch unreachable.
  </acceptance_criteria>
  <done>The gateway/effect suites pass the ordered allow/ask/deny and ambiguity cases, the object graph has public verification only, and closed release state makes production dispatch unreachable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add protected capability routes and injected app construction</name>
  <files>src/api/capability_routes.py, src/api/server.py</files>
  <read_first>
    - src/api/capability_routes.py (target; confirm absent before creation)
    - src/api/server.py
    - src/security/auth.py
    - src/api/dashboard_routes.py
    - tests/test_api_auth_matrix.py
    - tests/test_origin_csrf.py
    - tests/test_execution_gateway_integration.py
  </read_first>
  <behavior>
    - Propose/approve/execute/query routes require exact scopes, session, Origin/CSRF and request.state OperatorPrincipal only after the whole router is release-registered.
    - Payload cannot supply actor/session/resolved boundary/policy/manifest/effect fields; extras reject before gateway.
    - Closed or invalid release state leaves the whole router unregistered, so no body parsing, policy evaluation, approval issuance, query disclosure, reconciliation, or sink access can occur.
  </behavior>
  <action>Add `/api/capabilities/propose`, `/approve`, `/execute`, `/receipts/{id}`, and `/reconcile/{id}` with strict request/response DTOs, safe correlation/applied/retryable truth, exact scopes, and Phase 1 Origin/CSRF/session controls. Approval issuance route alone receives ApprovalIssuer; execution receives only ExecutionGateway. In production `create_app`, read and validate the singleton release state before router inclusion and include the whole router only when it atomically digest-binds complete fresh Phase 1 and Phase 2 evidence; missing, closed, malformed, stale, or partial state leaves every capability path unregistered before request body parsing or handler/dependency construction. Controlled fixtures may register the router only through an explicit test-only dependency-injection flag paired with isolated ControlStore, clock, policy/manifest, and fixture adapters; production construction must reject that flag. Register released routes in the central default-deny inventory. Do not alter `/api/code/execute` here.</action>
  <verify><automated>python -m pytest -q tests/test_execution_gateway_integration.py tests/test_api_auth_matrix.py tests/test_origin_csrf.py tests/test_dashboard_execution_gate.py -x</automated></verify>
  <acceptance_criteria>
    - Auth/origin/CSRF/wrong-scope matrices cover all new routes.
    - Issuer is reachable only in approval command wiring.
    - Before release, no production capability endpoint is registered or can parse a body, issue approval, disclose a query, reconcile, or reach a sink.
  </acceptance_criteria>
  <done>Auth/origin tests pass for explicit fixtures, issuer wiring exists only on approval, and production construction with closed release state registers no capability endpoint before body parsing.</done>
</task>
</tasks>

## Artifacts this phase produces

- `ExecutionGateway`, `TrustedActionContext`, `CapabilityReleaseGate`, `execute`, `reconcile` in `src/core/execution_gateway.py`.
- Capability propose/approve/execute/receipt/reconcile route handlers and strict DTOs in `src/api/capability_routes.py`.

<threat_model>
Attacker goals: bypass ordering, inject trusted identity, mint approval through gateway, or reach adapters while release is closed.
Trust boundaries: HTTP-to-principal/proposal, gateway stages, release-state-to-adapter registry.

| Threat IDs | STRIDE | Abuse path | Mitigation | Verification |
|---|---|---|---|---|
| T-01..T-07 | Spoofing/Tampering/Elevation | injected/stale/replayed action | trusted resolver, policy, public verifier, atomic reserve | integration suites |
| T-24 | Elevation | direct adapter or closed-gate dispatch | gateway-only registry and durable default-closed release state | sink/integration suites |
| T-25 | Repudiation/Disclosure | unsafe/missing evidence | same-transaction audit and receipt projection | integration suite |

Residual risk: legacy AgentRuntime/skills still contain sinks until 02-13; production release remains closed.
</threat_model>
<verification>Run integration, auth/origin, effect and dashboard gate suites; assert the release row is absent/closed in isolated production construction.</verification>
<success_criteria>All six phase requirements traverse one closed-by-default ordered gateway with separated issuer/verifier authority.</success_criteria>
<output>Create `.planning/phases/02-capability-policy-and-execution-isolation/02-12-SUMMARY.md` when done.</output>
