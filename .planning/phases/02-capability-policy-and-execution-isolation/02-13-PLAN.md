---
phase: 02-capability-policy-and-execution-isolation
plan: 13
type: execute
wave: 6
depends_on: [02-12]
files_modified: [src/core/agent.py, src/api/dashboard_routes.py, src/api/server.py, src/core/swarm.py, src/core/workspaces.py, src/skills/code.py, src/skills/files.py, src/skills/system.py, tests/test_execution_sink_inventory.py]
autonomous: true
requirements: [CTRL-02, CTRL-03, CTRL-06, TOOL-03, TOOL-05, TOOL-07]
must_haves:
  truths:
    - "No direct consequential sink remains reachable outside ExecutionGateway."
    - "The sink inventory enumerates every direct subprocess, Git, project-registration/removal, worktree, swarm writer/integration, browser, application-open, filesystem-write, and GitHub/project-write path in all production callers, including server.py, swarm.py, and workspaces.py."
    - "Production accepts no raw shell string, direct os.startfile/subprocess, generic file/code/system mutation, GitHub write, or autonomous project-write authority."
    - "Phase 3-only GitHub, project create/remove, worktree/commit/integrate/cleanup, and swarm mutation authority returns a stable closed result before mutation and cannot be enabled by request/model input."
    - "`/api/code/execute` preserves its exact authenticated, Origin/CSRF-aware, body-agnostic HTTP 423 response."
    - "Every gateway/browser-adjacent verification resolves the manifest-recorded absolute interpreter, runs it with `-I`, activates and verifies the approved package target, and attests both `cryptography` and `playwright.sync_api` origins before pytest starts."
  artifacts:
    - {path: src/api/server.py, provides: body-agnostic closed Phase 3 project/swarm write routes and governed read seams}
    - {path: src/core/swarm.py, provides: unreachable/closed swarm writer and integration mutation sinks}
    - {path: src/core/workspaces.py, provides: read-only workspace identity resolution with no direct Git process or Phase 3 mutation authority}
    - {path: tests/test_execution_sink_inventory.py, provides: exhaustive AST/call-graph/runtime inventory and zero-sink reachability proof}
    - {path: src/core/agent.py, provides: sealed gateway caller replacing mutable dispatcher/digest/process sinks}
    - {path: src/api/dashboard_routes.py, provides: preserved closed code route and governed/closed browser/open seams}
    - {path: src/skills/code.py, provides: denied/gateway compatibility shim}
    - {path: src/skills/files.py, provides: denied/gateway compatibility shim}
    - {path: src/skills/system.py, provides: denied/gateway compatibility shim}
    - {path: scripts/verify_phase2_package_provenance.py, provides: manifest-bound isolated approved-target activation and cryptography/Playwright origin attestation for every Plan 02-13 suite}
  key_links:
    - {from: src/core/agent.py, to: src/core/execution_gateway.py, via: one dependency-injected sealed call}
    - {from: tests/test_execution_sink_inventory.py, to: src/api/server.py, src/core/swarm.py, and src/core/workspaces.py, via: every discovered sink must be cut over to ExecutionGateway or mapped to a runtime-proven stable Phase 3 closed gate}
    - {from: tests/test_execution_sink_inventory.py, to: scripts/verify_phase2_package_provenance.py, via: all agent/dashboard/server/swarm/workspace gateway suites run through the approved-target pytest module runner with cryptography and Playwright origins attested}
---

<objective>
Inventory every production subprocess/Git/project/swarm/browser/filesystem mutation sink, cut supported calls over to the single gateway, and prove every deferred sink unreachable.

Purpose: Make policy and isolation enforcement real at the brownfield call sites rather than a wrapper beside old authority.
Output: Agent/dashboard/skill cutover, server/swarm/workspace closure, exhaustive sink tests, and exact closed-route preservation.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/02-capability-policy-and-execution-isolation/02-12-SUMMARY.md
@src/core/agent.py
@src/api/dashboard_routes.py
@tests/test_execution_sink_inventory.py
@tests/test_dashboard_execution_gate.py
@scripts/verify_phase2_package_provenance.py
@docs/security/phase-2-package-provenance.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace AgentRuntime mutable dispatch, digest and process sinks</name>
  <files>src/core/agent.py, tests/test_execution_sink_inventory.py</files>
  <read_first>
    - src/core/agent.py
    - src/core/execution_gateway.py
    - src/security/action_contracts.py
    - tests/test_agent_runtime.py
    - tests/test_execution_sink_inventory.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Agent proposals resolve through ExecutionGateway and cannot call `_execute_tool`, `_run_process`, or mutable default-str approval digest paths.
    - Existing read/write/process/app intents map to typed manifests; raw command strings, Git/GitHub and autonomous project writes deny.
    - Existing ambiguity/control behavior consumes durable gateway receipts and never retries confirmed/unknown effects blindly.
  </behavior>
  <action>First expand the sink inventory's explicit production-caller table and AST/call-graph discovery so newly found imports/calls fail the test instead of falling outside a fixed regex. Replace, do not wrap, AgentRuntime `_approval_action_digest`, `_execute_tool`, `_run_process`, workspace writes, command_run, Git/GitHub, open_url/open_path and app_launch consequential paths with a dependency-injected ExecutionGateway call accepting a sealed resolved action. Preserve proposal/model roles and durable run/control semantics but remove production reachability of direct sinks. Map supported local/browser intents to exact manifest IDs/argv/path schemas; Git/GitHub/project-write and raw shell return stable deny. Compatibility methods, if retained for tests, must be fixture-only and unable to reach OS sinks. The test must fail if an AgentRuntime method directly or indirectly reaches subprocess, os.startfile, browser launch, raw filesystem mutation, Git/GitHub, WorkspaceRegistry mutation, or SwarmManager mutation without the gateway.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_agent_runtime.py tests/test_execution_gateway_integration.py tests/test_execution_sink_inventory.py -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - No production call path reaches old dispatcher/process/digest behavior.
    - Agent tests use gateway receipts and preserve stop/ambiguity truth.
    - Deferred GitHub/project-write authority stays closed.
  </acceptance_criteria>
  <done>Agent, gateway, and sink-inventory suites pass with no production reachability of legacy execution sinks and all deferred write authority denied.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Close dashboard and legacy skill bypasses while preserving exact code gate</name>
  <files>src/api/dashboard_routes.py, src/skills/code.py, src/skills/files.py, src/skills/system.py, tests/test_execution_sink_inventory.py</files>
  <read_first>
    - src/api/dashboard_routes.py
    - src/skills/code.py
    - src/skills/files.py
    - src/skills/system.py
    - src/core/execution_gateway.py
    - tests/test_dashboard_execution_gate.py
    - tests/test_execution_sink_inventory.py
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - `/api/code/execute` remains exact 423 `phase_2_execution_gate_closed`, authenticates/origin-checks first, rejects before body decode and reaches no sink.
    - Browser/open/file/application dashboard paths call typed capability routes or deny; syntax-only URL checks and os.startfile disappear as authority.
    - Generic consequential code/files/system skill dispatch denies or delegates sealed actions to the gateway only.
  </behavior>
  <action>Preserve the exact CODE_EXECUTION_GATE_CODE, message, retryable false, applied false, route path and 423 behavior. Keep Phase 1 middleware/session/Origin/CSRF classification and body-agnostic rejection. Replace dashboard browser/open/file/application sinks with typed capability calls that remain release-gated, and remove syntax-only validation as authority. Convert code/files/system consequential skill execution into gateway-only compatibility shims or stable denies; keep demonstrably read-only observations exact and bounded. Remove direct subprocess, asyncio subprocess, os.startfile, filesystem mutation, browser launch and adapter imports from these callers. Add all dashboard and skill entrypoints to the same exhaustive sink/call-graph table, with runtime monkeypatch sentinels proving malformed, denied, closed-release, wrong-scope, and adversarial inputs make zero sink calls.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_execution_sink_inventory.py tests/test_dashboard_execution_gate.py tests/test_agent_runtime.py -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - T-24 inventory and monkeypatch guards find no bypass.
    - Exact `/api/code/execute` contract is unchanged for authenticated/origin-aware calls.
    - All consequential compatibility paths are governed or denied.
  </acceptance_criteria>
  <done>Sink/dashboard/agent suites pass, every consequential compatibility path is governed or denied, and authenticated Origin-aware `/api/code/execute` remains body-agnostic exact HTTP 423.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Close server, swarm, workspace and Phase 3-only mutation sinks</name>
  <files>src/api/server.py, src/core/swarm.py, src/core/workspaces.py, tests/test_execution_sink_inventory.py</files>
  <read_first>
    - src/api/server.py
    - src/core/swarm.py
    - src/core/workspaces.py
    - src/core/execution_gateway.py
    - src/security/manifests.py
    - src/security/policy.py
    - tests/test_execution_sink_inventory.py
    - tests/test_dashboard_execution_gate.py
    - .planning/phases/02-capability-policy-and-execution-isolation/02-READINESS-AUDIT.md
    - scripts/verify_phase2_package_provenance.py
    - docs/security/phase-2-package-provenance.json
  </read_first>
  <behavior>
    - Every direct/indirect subprocess, Git, project register/remove, worktree/commit/integrate/cleanup, swarm writer/approval/resume/integration, filesystem mutation, and GitHub/project-write sink in server.py, swarm.py, and workspaces.py has an inventory entry classified only as gateway-cut-over or runtime-proven unreachable.
    - Supported exact read-only observations use typed released gateway actions; project/GitHub/Git mutation and swarm writer/integration actions return stable `phase_3_project_write_gate_closed` before any sink, body-dependent dispatch, or model-controlled enable check.
    - Workspace identity lookup for already registered roots remains read-only and bounded, but WorkspaceRegistry owns no direct subprocess helper and cannot create/remove/change projects or Git state during Phase 2.
    - Production manifests/policy contain no raw `git`, GitHub write, project-write, worktree, swarm writer, or integration authority, and runtime tests prove zero sink calls for every closed entrypoint.
  </behavior>
  <action>Extend `tests/test_execution_sink_inventory.py` before source changes. Build the inventory from AST imports/calls, route decorators, constructor dependency edges, and a maintained exhaustive caller table covering at least `src/api/server.py`, `src/core/swarm.py`, and `src/core/workspaces.py`; fail on an unclassified discovered sink or callable path rather than merely searching for known text. Enumerate every server project/swarm mutation route, including project create/delete, swarm run/resume, task approval, writer worktree, integrate/reject and any aliases discovered from decorators/call graph. Keep read-only queries only when they do not reach a process or mutation sink. Route exact Phase 2-supported observations through ExecutionGateway; for every Phase 3-owned project/Git/GitHub/swarm mutation, install one non-request-configurable `phase_3_project_write_gate_closed` boundary before body-dependent orchestration or sink construction and return stable non-retryable/applied-false truth. In SwarmManager, remove production reachability of writer worktree preparation, writer dispatch, commit/integration/rejection cleanup, and any direct subprocess/WorkspaceRegistry mutation; methods retained for compatibility must call the same stable closed authority before touching a workspace. In WorkspaceRegistry, remove `_git`/`_git_status` and every direct subprocess path; retain bounded database/root identity reads, route any exact Git observation through a typed released gateway action, and make register/remove/create/worktree/commit/integrate/cleanup mutations Phase 3-closed with no model/request flag that can open them. Do not add raw Git/GitHub/project-write executable specs to manifests or policy. Runtime tests must instantiate real server/swarm/workspace call paths with sink sentinels (subprocess, filesystem writes, registry mutations, integration calls, GitHub clients) and prove zero calls for each closed path; inventory allowlisting requires a concrete gateway call edge or a stable gate plus runtime proof, never a comment/source-text assertion.</action>
  <verify><automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_execution_sink_inventory.py tests/test_dashboard_execution_gate.py tests/test_execution_gateway_integration.py -x; exit $LASTEXITCODE"</automated></verify>
  <acceptance_criteria>
    - server.py, swarm.py, and workspaces.py are explicit sink-inventory callers and every discovered sink/call path is classified.
    - All supported consequential operations cross ExecutionGateway; all Phase 3 GitHub/project/Git/swarm writes fail stably before sink construction and record zero calls.
    - WorkspaceRegistry has no direct subprocess/Git helper and exposes only bounded read-only identity resolution during Phase 2.
    - An unclassified new sink, raw Git manifest/policy identity, or removed gate makes the inventory suite fail.
  </acceptance_criteria>
  <done>Agent, dashboard, skills, server, swarm, and workspace callers are exhaustively inventoried; each real sink is gateway-only or runtime-proven unreachable, and Phase 3-only mutation authority remains closed.</done>
</task>
</tasks>

## Artifacts this phase produces

- Gateway-backed sealed AgentRuntime execution call and typed intent mapping in `src/core/agent.py`.
- Governed dashboard browser/open adapters with permanently closed raw `/api/code/execute` in `src/api/dashboard_routes.py`.
- Gateway-only or denied consequential compatibility shims in `src/skills/code.py`, `files.py`, and `system.py`.
- Stable Phase 3 project/swarm write gates and governed read seams in `src/api/server.py`, with writer/integration sinks unreachable in `src/core/swarm.py`.
- Read-only workspace identity resolution without direct subprocess/Git mutation in `src/core/workspaces.py`.
- Exhaustive AST/call-graph/runtime sink classification and zero-call proof across all listed production callers in `tests/test_execution_sink_inventory.py`.

<threat_model>
Attacker goals: find one old sink, hide a sink outside a fixed regex inventory, submit raw shell/path/URL, or use deferred GitHub/project/swarm authority.
Trust boundaries: legacy callers-to-gateway, server-to-project/swarm, swarm-to-workspace/Git, and route/skill input-to-OS/browser sinks.

| Threat IDs | STRIDE | Abuse path | Mitigation | Verification |
|---|---|---|---|---|
| T-11, T-24 | Elevation | raw shell/direct sink survives | replace seam plus AST/runtime guards | sink inventory |
| T-02-SINK | Elevation/Tampering | server/swarm/workspace indirect sink or Phase 3 write becomes reachable | exhaustive AST/call graph plus stable non-configurable Phase 3 gate and runtime zero-call sentinels | `tests/test_execution_sink_inventory.py` |
| T-02-ATTEST | Tampering/Elevation | an ambient interpreter or same-version package shadow falsifies gateway/browser-adjacent evidence | manifest-bound absolute interpreter with `-I`, installed-target verification, approved-target activation, and cryptography/Playwright module-origin attestation before pytest | every task-level verification command |
| T-18..T-20 | Disclosure | syntax-only browser route bypass | typed broker or deny | browser/gateway suites |
| T-25 | Repudiation | legacy result skips receipt/audit | gateway-only effect path | integration suite |

Residual risk: all live adapters remain closed until 02-14 proves every external gate; closed-route preservation is permanent for raw code execution.
</threat_model>
<verification>Run every named sink, dashboard, agent, browser-adjacent, and gateway suite only through the manifest-recorded absolute interpreter with `-I` and `scripts/verify_phase2_package_provenance.py --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest`; ambient `python`, `py`, PATH, PYTHONPATH, and user-site are not evidence. Preserve each task's exact test selection and `-x`, require the AST/call-graph inventory to cover agent, dashboard, skills, server, swarm, and workspaces, and use runtime sentinels to prove each deferred sink records zero calls. Use `rg` only as supplemental discovery, never as reachability proof.</verification>
<success_criteria>Every consequential brownfield caller, including server.py, swarm.py, and workspaces.py, is governed by ExecutionGateway or runtime-proven denied; Phase 3 GitHub/project/Git/swarm mutation authority stays closed and `/api/code/execute` exact 423 remains unchanged.</success_criteria>
<output>Create `.planning/phases/02-capability-policy-and-execution-isolation/02-13-SUMMARY.md` when done.</output>
