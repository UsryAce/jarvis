---
phase: 02-capability-policy-and-execution-isolation
plan: 14
type: execute
wave: 7
depends_on: [02-13]
files_modified:
  - scripts/verify_phase2_release.py
  - tests/test_phase2_release_verifier.py
  - docs/security/phase-2-release-evidence.json
  - docs/security/phase-2-release-evidence.md
autonomous: false
requirements: [CTRL-02, CTRL-03, CTRL-06, TOOL-03, TOOL-05, TOOL-07]
must_haves:
  truths:
    - "The genuine executed Phase 1 Plan 01-14 SUMMARY and every referenced cross-SID, release, recovery, and human-verification receipt are mandatory; the Plan file, fixtures, ad-hoc output, or synthesized claims are never substitutes."
    - "One frozen evidence candidate binds the exact release-affecting source commit/tree/index/path inventory, manifest-recorded interpreter and runner, installed Python closure, cryptography and Playwright origins, approved Chromium archive/executable bytes, validation attestation, and real Windows/browser drills."
    - "Check-only rejects every staged, modified, deleted, conflicted, hidden, ignored-source, or untracked release-affecting path without deleting, moving, staging, committing, ignoring, or modifying user-owned content."
    - "Ambient `python`, `py`, direct-pytest, PATH, PYTHONPATH, user-site, and completed-plan results remain immutable `HISTORICAL_ONLY` receipts and cannot satisfy final validation."
    - "The manifest-recorded absolute interpreter with `-I` runs `scripts.verify_phase2_package_provenance.py`, verifies the installed approved target, attests cryptography and Playwright, and only then invokes `scripts.verify_phase2_release`."
    - "The same frozen source snapshot passes AGENTS.md's checked frontend gate from its exact resolved `frontend/` directory: `npm run build:checked` is bound to absolute Node/npm identities, local TypeScript/Vite identities, command/cwd/config/lockfile bytes, exit status, output, and a deterministic `dist/` manifest."
    - "Check-only freshly replays the unfiltered complete repository pytest collection and applicable compile inventory at the frozen source, covers every completed-plan selector, and accepts no skip/xfail except an exact safe gate that cannot waive a security or human prerequisite."
    - "Hostile verifier tests prove missing, stale, partial, ambient, differently collected, origin/package/browser-drifted, source-mismatched, or approval-mismatched evidence is CLOSED."
    - "The final release decision is a non-auto-approvable human decision that names the exact evidence/source/replay/runtime/package/browser/release/revision digests and is recorded verbatim in 02-14-SUMMARY.md."
    - "Plan 02-14 never opens `capability_release_state`, never registers production capability authority, never creates the final Brain handoff, and never runs Graphify refresh."
    - "Plan 02-14 succeeds only with the exact human-approved frozen receipt and the production release row still closed."
  artifacts:
    - {path: scripts/verify_phase2_release.py, provides: fail-closed check-only verifier plus a source-approved release entrypoint that cannot run without the exact recorded 02-14 decision}
    - {path: tests/test_phase2_release_verifier.py, provides: hostile source, runner, replay, Phase 1 prerequisite, approval-binding, TOCTOU, and no-production-mutation tests}
    - {path: docs/security/phase-2-release-evidence.json, provides: canonical frozen machine-readable evidence candidate and domain-separated digest}
    - {path: docs/security/phase-2-release-evidence.md, provides: redacted immutable human projection of the frozen candidate}
    - {path: frontend/package.json, provides: source-bound `build:checked` contract whose exact execution evidence is embedded in the canonical release receipt}
  key_links:
    - {from: .planning/phases/01-trust-and-durable-control-foundation/01-14-SUMMARY.md, to: scripts/verify_phase2_release.py, via: only the genuine executed summary and its digest-bound referenced receipts satisfy the Phase 1 prerequisite}
    - {from: .planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md, to: docs/security/phase-2-release-evidence.json, via: exact digest of the corrected 15-plan Wave 0-8 validation map plus a current source-bound plan/task/command attestation}
    - {from: git source snapshot, to: docs/security/phase-2-release-evidence.json, via: exact commit/tree/index/path inventory and read-only rejection of every unresolved release-affecting status entry}
    - {from: docs/security/phase-2-package-provenance.json, to: scripts/verify_phase2_release.py, via: the manifest-bound `-I` runner verifies installed closure and cryptography/Playwright origins before module execution}
    - {from: complete current pytest collection, to: docs/security/phase-2-release-evidence.json, via: fresh unfiltered execution and compile attestations cover every completed-plan selector at the frozen source}
    - {from: frontend/package.json `build:checked`, to: docs/security/phase-2-release-evidence.json, via: exact frozen frontend cwd/source/lock/config and absolute Node/npm/TypeScript/Vite identities bind command, exit, output, and deterministic dist manifest}
    - {from: exact human checkpoint response, to: .planning/phases/02-capability-policy-and-execution-isolation/02-14-SUMMARY.md, via: verbatim structured approval binds the frozen evidence/source/replay/runtime/package/browser/release/revision digests while authority remains closed}
---

<objective>
Construct and freeze the complete Phase 2 release evidence, replay every automated obligation through the attested source-bound runner, and obtain the final exact human release decision without opening any production authority.

Purpose: Separate evidence and approval from consequential authority mutation so the human approves exact immutable bytes and Plan 02-15 can only consume that recorded decision.
Output: Hostile-tested release verifier, canonical frozen JSON/Markdown evidence receipt, exhaustive check-only replay, and a verbatim digest-bound human decision recorded with `capability_release_state` still closed.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trust-and-durable-control-foundation/01-14-PLAN.md
@.planning/phases/01-trust-and-durable-control-foundation/01-14-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-PATTERNS.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-READINESS-AUDIT.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-13-SUMMARY.md
@docs/security/phase-2-package-provenance.json
@docs/security/phase-2-package-provenance.md
@scripts/verify_phase2_package_provenance.py
@src/core/capability_store.py
@src/core/control_store.py
@src/api/server.py
@tests/test_secret_protector_windows.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the frozen fail-closed release receipt and hostile verifier</name>
  <files>scripts/verify_phase2_release.py, tests/test_phase2_release_verifier.py, docs/security/phase-2-release-evidence.json, docs/security/phase-2-release-evidence.md</files>
  <read_first>
    - .planning/phases/01-trust-and-durable-control-foundation/01-14-SUMMARY.md (required genuine execution output; fail closed if absent)
    - .planning/phases/01-trust-and-durable-control-foundation/01-14-PLAN.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-READINESS-AUDIT.md
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - src/core/control_store.py
    - src/core/capability_store.py
    - src/api/server.py
    - tests/test_secret_protector_windows.py
  </read_first>
  <behavior>
    - Genuine Phase 1 Plan 01-14 SUMMARY/receipt identity, distinct-SID evidence, safe freshness, artifact digests, and checkpoint ordering are mandatory; a Plan, fixture, ad-hoc rerun, or synthesized summary returns CLOSED.
    - `collect_source_snapshot` records full release source commit/tree/index, stage/object identity, release-affecting path hashes, submodule state, sparse/skip-worktree/assume-unchanged flags, and porcelain-v2 untracked/ignored-source state without changing the repository.
    - `collect_historical_development_receipts` classifies completed-plan ambient results as `HISTORICAL_ONLY`; `collect_completed_plan_selection_inventory` turns their exact selectors into coverage obligations rather than PASS evidence.
    - `run_attested_repository_replay` uses only the already-attested manifest executable/runner process, disables ambient plugin/config/path influence, executes the unfiltered complete current repository pytest collection plus `compileall -q src tests scripts`, and binds exact commands, collection, outcomes, imports, packages, browser bytes, and source identity.
    - The replay separately runs the exact AGENTS.md frontend command `npm run build:checked` from the source-bound resolved `frontend/` cwd. It records absolute Node and npm executable/shim paths and SHA-256, `node --version`, `npm --version`, local TypeScript and Vite package/version/entry hashes, package/config/lockfile hashes, sanitized environment, start/end timestamps, exit status, bounded stdout/stderr digest, and deterministic `frontend/dist/` relative-path/size/SHA-256 manifest; it never routes npm through, substitutes for, or weakens the manifest-bound Python runner.
    - `verify_attested_replay` rejects external, partial, nonzero, stale, differently collected, source/origin/package/browser-drifted, compile-drifted, skipped, xfailed, completed-selection-incomplete, or missing/stale/nonzero/different-source/different-toolchain frontend evidence.
    - The canonical JSON contains the Phase 1 bundle, unchanged 02-VALIDATION digest and current supplemental attestation, source snapshot, historical classification, replay, packages/browser, real drills, release version/expected closed revision, and domain-separated final evidence digest; Markdown is generated only from that JSON.
    - `verify_recorded_release_decision` accepts only the exact structured human record later written verbatim to genuine 02-14-SUMMARY.md and requires equality of every evidence/source/replay/runtime/package/browser/release/revision field.
    - The pre-built `--release` path is cryptographically unreachable without that genuine recorded 02-14 decision, revalidates the frozen receipt immediately before and after the in-transaction state transition, and has no ambient or reconstructed-approval fallback; Plan 02-14 never invokes it.
    - Hostile tests use temporary/isolated stores or transaction fakes only and prove absent/stale/malformed/differently digested approval cannot call `open_release_state`; no test opens the production `data/control.db` row.
    - `main(argv)` has a `__main__` entry, accepts exactly `--check-only` or `--release`, and refuses ambiguous/missing modes; both modes must be invoked through the manifest-recorded `-I` provenance runner.
  </behavior>
  <action>Implement the verifier and tests in one source-approved change set. Per D-01 through D-04, resolve only exact canonical evidence identifiers, keep verification total/fail-closed, emit stable safe codes, and preserve immutable redacted records. Per D-05 through D-08, keep the human decision issuer separate from verification, require exact digest/source/replay/runtime/package/browser/release/revision binding, and make any eventual state transition a single compare-and-swap transaction; the verifier must never infer or recreate approval. Per D-09 through D-15, require typed local/browser receipts, preserve the raw-shell prohibition, bind real Windows Job/handle-safe filesystem/ephemeral-browser/every-hop-egress/quarantine cleanup drills, and rehash the exact approved Chromium archive and executable with approval-before-first-launch ordering. Per D-16, bind idempotency/reconciliation proof and allow only exact already-open same-binding reconciliation later; a mismatched open row is CLOSED.

Require the genuine executed Plan 01-14 SUMMARY and every referenced route/auth, Origin/CSRF, migration/restore, canary, DPAPI owner/different-SID, credential lifecycle, audit-tamper, persistent-stop, recovery, and human-decision receipt. Freeze the full release source identity with read-only Git plumbing and reject any staged, modified, deleted, conflicted, type-changed, hidden, sparse, submodule, untracked, or ignored release-affecting content as `source_snapshot_unbound_content`. Never clean/reset/checkout/restore/add/rm/commit, edit ignore rules, or delete/move/rewrite discovered content; regression-test byte-for-byte preservation of `backend/`, `frontend/design-qa.md`, `frontend/design-qa/`, and root `index.html` when present.

Run the complete current repository pytest collection in-process through the manifest-approved target with canonical unfiltered `-q`, no selector/`-k`/ignore/deselect/last-failed/stepwise/ambient `PYTEST_ADDOPTS` or ambient plugin autoload, and record ordered node IDs plus every setup/call/teardown outcome. Reattest installed closure and cryptography/Playwright module/distribution origins before collection and after execution. Run `compileall -q src tests scripts` through the same runner and bind its file inventory/result. Separately execute the literal command `npm run build:checked` from the frozen snapshot's resolved `frontend/` directory, never from the repository root or a copied/different tree. Resolve Node and npm to absolute files before execution; hash those files/shims; record Node/npm versions; resolve and hash the lockfile-selected local TypeScript and Vite packages/entrypoints; reject PATH alias changes, lifecycle/config overrides, ambient `NODE_OPTIONS`/`npm_config_*` influence, a cwd outside the frozen source, or any package/config/lockfile drift. Bind command, cwd, source identity, toolchain identity, timestamps, exit code, bounded output digest, and a deterministic post-build `frontend/dist/` manifest into `frontend_build_digest`. This child process is only the checked frontend gate: it cannot invoke, replace, attest, or provide evidence for Python pytest/compile/release execution, which remains exclusively behind the manifest-recorded absolute `-I` provenance runner. Every historical completed-plan selection must resolve into the fresh collection and pass. An `expected_gates` entry must bind exact node ID/outcome/reason/source/rationale and can never waive distinct-SID, T-01..T-25, provenance/runner/origin, release-verifier, checked frontend build, completed-selection, or human-release prerequisites.

Build the final JSON/Markdown candidate outside the source snapshot, hash the exact final bytes, promote only the prehashed pair, and verify the bytes again. Include check-only proof that `CapabilityStore.read_release_state(tx)` is closed at the expected revision. Implement the post-approval release entrypoint now so its code and hostile tests are inside the source the human reviews, but make it require the future genuine 02-14-SUMMARY structured decision and do not invoke it in this plan. It must expose pure/testable `verify_recorded_release_decision`, `revalidate_frozen_release_receipt`, and `release_from_verified_bundle` contracts for Plan 02-15, use only `ControlStore.immediate_transaction()`, `CapabilityStore.read_release_state(tx)`, and `CapabilityStore.open_release_state(tx, expected_revision=...)`, revalidate before and immediately after the uncommitted transition, append audit in the same transaction, rollback on any drift, and keep `/api/code/execute` and Phase 3 authority closed. Tests cover all hostile evidence/approval/TOCTOU cases without touching the production store.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_phase2_release_verifier.py -x; if ($LASTEXITCODE) { exit $LASTEXITCODE }; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module scripts.verify_phase2_release -- --check-only; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $e = Get-Content -LiteralPath 'docs/security/phase-2-release-evidence.json' -Raw | ConvertFrom-Json; if ([string]$e.frontend_build.command -cne 'npm run build:checked' -or [int]$e.frontend_build.exit_status -ne 0 -or [string]::IsNullOrWhiteSpace([string]$e.frontend_build.frontend_build_digest)) { exit 1 }; exit 0"</automated>
  </verify>
  <done>The hostile verifier suite passes only through the attested runner; the exact JSON/Markdown candidate is frozen and includes a zero-exit, exact-source/toolchain `npm run build:checked` receipt plus deterministic output manifest; hostile cases reject absent, stale, nonzero, or different-source frontend evidence; historical/ambient evidence cannot satisfy a predicate; stale or mismatched approval cannot reach any open call; and production release state remains closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Make the exact final human release decision while authority remains closed</name>
  <files>docs/security/phase-2-release-evidence.json, docs/security/phase-2-release-evidence.md</files>
  <what-built>A source/runtime/package/browser/frontend-bound frozen release candidate, exhaustive attested pytest/compile replay plus checked frontend build, genuine Phase 1 receipt bundle, hostile verifier suite, and read-only proof that production capability authority is closed.</what-built>
  <how-to-verify>
    1. Run the automated check-only command below; it must resolve the manifest's absolute executable, use `-I`, verify the installed approved target, attest cryptography and Playwright origins, and invoke `scripts.verify_phase2_release --check-only` with no ambient fallback.
    2. Confirm the genuine 01-14-SUMMARY and all referenced Phase 1 receipts exist, including a genuinely different SID. A missing summary/receipt, Plan/fixture substitute, stale timestamp, or partial/unconfirmed result is rejection.
    3. Confirm the report binds the clean frozen source commit/tree/index/path inventory, exact corrected 02-VALIDATION digest plus current 15-plan/Wave 0-8 attestation, exact runner/import/package/Chromium hashes, approval-before-first-browser-launch ordering, full pytest collection/outcomes, compile inventory, and completed-plan coverage.
    4. Inspect `frontend_build`: it must show the literal `npm run build:checked`, the exact frozen `frontend/` cwd and source identity, absolute hashed Node/npm identities and versions, lockfile-selected local TypeScript/Vite identities, package/config/lockfile hashes, sanitized environment, zero exit, bounded output digest, deterministic `dist/` manifest, and nonempty `frontend_build_digest`; missing, stale, nonzero, or different-source/toolchain evidence is rejection.
    5. Confirm every Windows Job/cancel/timeout/emergency-stop/backend-shutdown, handle-safe filesystem, browser peer-binding/subresource/popup/WebSocket, quarantine, descendant/profile/proxy/driver cleanup, canary, raw-code 423, and T-01 through T-25 result is genuine and PASS with no forbidden skip/xfail.
    6. Confirm check-only left every user-owned path byte-for-byte unchanged and `capability_release_state` is still closed at the exact expected revision.
  </how-to-verify>
  <action>Present only the exact immutable check-only JSON/Markdown receipt and its attested command result for the blocking human decision. Do not edit the candidate, open release state, register routes, write a final Brain handoff, refresh Graphify, or convert a free-form response into approval. Accept only the exact structured resume signal below, preserve it verbatim for the executor's 02-14-SUMMARY output, and re-read the production release row to prove it remains closed before completing the plan.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module scripts.verify_phase2_release -- --check-only; exit $LASTEXITCODE"</automated>
    <human-check>Approve only the exact displayed receipt after confirming the checked frontend build command/cwd/source/toolchain/zero-exit/output manifest and `frontend_build_digest`. The executor must copy the response verbatim into 02-14-SUMMARY.md; missing, stale, nonzero, different-source frontend evidence, any missing field, or free-form reconstruction is rejection.</human-check>
  </verify>
  <resume-signal>Reply exactly `APPROVE_PHASE_2_RELEASE evidence_digest=&lt;sha256&gt; source_commit=&lt;full-sha&gt; source_tree=&lt;sha256&gt; source_inventory_digest=&lt;sha256&gt; replay_digest=&lt;sha256&gt; validation_digest=&lt;sha256&gt; runtime_digest=&lt;sha256&gt; package_digest=&lt;sha256&gt; chromium_digest=&lt;sha256&gt; frontend_build_digest=&lt;sha256&gt; release_version=&lt;value&gt; expected_closed_revision=&lt;integer&gt;` using the values printed by check-only, or reply `REJECT_PHASE_2_RELEASE &lt;reason&gt;`.</resume-signal>
  <done>The exact non-auto-approvable decision is recorded verbatim in genuine 02-14-SUMMARY.md and binds every frozen receipt field; a final read proves `capability_release_state` remains closed, no production routes were registered, no Brain handoff was created, and Graphify refresh was not run.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/verify_phase2_release.py`: `collect_source_snapshot`, `collect_historical_development_receipts`, `collect_completed_plan_selection_inventory`, `run_attested_repository_replay`, `verify_attested_replay`, `verify_completed_plan_selection_coverage`, `verify_phase1_plan_01_14_bundle`, `verify_validation_attestation`, `verify_package_provenance`, `verify_real_drills`, `build_frozen_release_candidate`, `verify_recorded_release_decision`, `revalidate_frozen_release_receipt`, `release_from_verified_bundle`, and `main`.
- `tests/test_phase2_release_verifier.py`: deterministic genuine-prerequisite, no-ambient-evidence, exhaustive replay, source preservation, approval mismatch, TOCTOU rollback, and no-production-mutation tests.
- `docs/security/phase-2-release-evidence.json`: exact immutable candidate approved by digest.
- `docs/security/phase-2-release-evidence.md`: redacted projection whose digest is bound by the canonical JSON.
- `frontend/package.json` plus the frozen frontend config/lockfile/toolchain/output inventory: unchanged source inputs whose exact checked-build execution is receipt-bound as `frontend_build_digest`; no frontend source becomes a Python evidence runner.
- `02-14-SUMMARY.md`: executor output containing the verbatim structured human decision and final proof that authority remained closed.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plans | Status |
|---|---|---|---|---|
| GOAL | Phase 2 | deterministic, exact, bounded, auditable local/browser capability execution | 01-15 | COVERED |
| REQ | CTRL-02, CTRL-03, CTRL-06 | exact policy, approval, and containment | 01, 03, 07-15 | COVERED |
| REQ | TOOL-03, TOOL-05, TOOL-07 | typed local/browser actions, durable receipts, idempotency/reconciliation | 02-06, 08-15 | COVERED |
| CONTEXT | D-01..D-04 | exact trusted resolution, allow/ask/deny, hostile rejection, immutable safe records | 01, 07, 12-14 | COVERED |
| CONTEXT | D-05..D-08 | issuer/verifier separation, exact approval, atomic consume/reserve, asymmetric protected verification | 01, 05, 06, 08, 09, 12, 14, 15 | COVERED |
| CONTEXT | D-09..D-12 | typed local actions, no raw shell, Windows Job containment, handle-safe filesystem | 03, 10, 13-15 | COVERED |
| CONTEXT | D-13..D-15 | ephemeral browser, every-hop egress, quarantine/artifact bounds | 04-06, 11, 14, 15 | COVERED |
| CONTEXT | D-16 | stable idempotency and authoritative reconciliation | 02, 08, 09, 11-15 | COVERED |
| RESEARCH | T-01..T-25 | complete hostile/security matrix | 01-04, 07-15 | COVERED |
| RESEARCH | package legitimacy and no-ambient runner | exact approved closure plus Chromium archive/executable binding before any verifier mode | 05, 06, 10, 11, 14, 15 | COVERED |
| RESEARCH | Phase 1/live release prerequisite | genuine executed Plan 01-14 receipts and different-SID evidence | 14, 15 | COVERED |
| RESEARCH | frozen source and final replay | clean exact snapshot, exhaustive source-bound pytest/compile replay, completed-selection coverage | 14, 15 | COVERED |

Deferred Phase 3 queue/lease/restart orchestration, GitHub/project/swarm mutation authority, Phase 4 planner/verifier separation, and later Brain shared-pack/provider/UI/voice/operations work remain excluded exactly as CONTEXT.md directs.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 1 SUMMARY/receipts -> verifier | Untrusted or stale files must not substitute for genuine executed prerequisites. |
| Git/index/status -> frozen evidence | Dirty, hidden, ignored, or user-owned source content must be observed without mutation and must close the gate. |
| package/browser manifests -> attested runner | Ambient interpreter, import, package, or executable bytes must not enter release evidence. |
| pytest/compile observations -> canonical receipt | Partial or differently collected runs must not masquerade as exhaustive replay. |
| human checkpoint -> 02-14-SUMMARY | Free-form, stale, or reconstructed approval must not become release authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Spoofing/Tampering | Phase 1 prerequisite loader | mitigate | Require genuine executed 01-14-SUMMARY identity, referenced receipts, different-SID proof, digests, freshness, and checkpoint ordering. |
| T-02-02 | Tampering/Repudiation | source snapshot | mitigate | Bind exact commit/tree/index/path/status state; reject read-only on every unresolved entry and byte-check preservation of user paths. |
| T-02-03 | Tampering/Elevation | runner and package/browser inputs | mitigate | Require manifest absolute interpreter, `-I`, installed closure, import-origin attestations, exact Chromium bytes, and approval-before-launch. |
| T-02-04 | Repudiation/Tampering | exhaustive replay and checked frontend build | mitigate | Bind unfiltered collection/execution/compile results and every completed-plan selector; run literal `npm run build:checked` only from frozen `frontend/`; bind absolute Node/npm and local TypeScript/Vite identity, zero exit, output digest and deterministic dist manifest; hostile-test absent/stale/nonzero/different-source evidence; forbid security/human gate allowlisting. |
| T-02-05 | Spoofing/Elevation | human decision | mitigate | Exact non-auto structured response copied verbatim to genuine 02-14-SUMMARY; reject missing or mismatched fields. |
| T-02-06 | Elevation | dormant release entrypoint | mitigate | Exact recorded decision is mandatory and unavailable during Plan 02-14; hostile tests prove no production-store call. |
| T-02-SC | Tampering | Python/Chromium supply chain | mitigate | Reuse approved provenance audit, rehash installed/archive/executable bytes, and permit no install or ambient fallback. |

Residual risk: production authority intentionally remains closed until Plan 02-15 consumes this exact receipt. Phase 3 GitHub/project/Git/swarm mutation and raw `/api/code/execute` remain default-deny.
</threat_model>

<verification>Run the hostile verifier suite and check-only mode only through the manifest-recorded absolute interpreter with `-I`, installed-target verification, and cryptography/Playwright origin attestation. Check-only must bind the genuine Plan 01-14 bundle, clean frozen source, corrected 15-plan/Wave 0-8 validation digest and attestation, exact package/browser bytes, full unfiltered pytest/compile replay, literal source-bound `frontend/` `npm run build:checked` with exact Node/npm/TypeScript/Vite identity, zero exit, bounded output and deterministic dist manifest, completed-plan selection coverage, real containment drills, and stable user-file preservation. Frontend execution is a separate checked-build subprocess and cannot supply or bypass Python evidence. The checkpoint may approve only the exact printed digests including `frontend_build_digest` and must record the response verbatim while a final read proves release state is closed. Do not run `--release`, construct a matching-open app, create the final Brain handoff, or invoke Graphify refresh in this plan.</verification>

<success_criteria>Ahmed has one exact human-approved, digest-bound frozen release decision backed by genuine Phase 1 prerequisites and exhaustive source-bound evidence, while production capability authority, Brain handoff, Graphify refresh, raw code execution, and Phase 3 mutation authority all remain closed.</success_criteria>

<output>Create `.planning/phases/02-capability-policy-and-execution-isolation/02-14-SUMMARY.md` when done, including the exact checkpoint response and final closed-state proof.</output>
