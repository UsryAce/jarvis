---
phase: 02-capability-policy-and-execution-isolation
plan: 15
type: execute
wave: 8
depends_on: [02-14]
files_modified:
  - data/control.db
  - .planning/graphs/graph.json
  - .planning/graphs/graph.html
  - .planning/graphs/GRAPH_REPORT.md
  - .planning/graphs/.last-build-snapshot.json
  - C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-08-01-phase-2-execution-isolation.md
  - C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/Graphify Report.md
  - C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/graph-status.json
autonomous: false
requirements: [CTRL-02, CTRL-03, CTRL-06, TOOL-03, TOOL-05, TOOL-07]
must_haves:
  truths:
    - "Release mode consumes only the genuine 02-14-SUMMARY verbatim approval whose evidence/source/replay/runtime/package/browser/frontend-build/release/revision fields exactly equal the frozen 02-14 receipt; it never reconstructs approval from prompts, plans, fixtures, current output, or partial fields."
    - "The frozen JSON/Markdown receipt approved in Plan 02-14 is byte-for-byte immutable throughout Plan 02-15 and no ambient release evidence can supplement it."
    - "The manifest-recorded absolute interpreter with `-I` verifies the installed approved target and cryptography/Playwright origins before invoking the source-approved release entrypoint."
    - "Inside one `ControlStore.immediate_transaction()`, release mode revalidates the exact frozen receipt immediately before `open_release_state`, opens only the expected closed revision, revalidates immediately after the uncommitted transition, appends audit, and commits once; any drift rolls back to closed."
    - "A post-commit read proves the open row, evidence digest, source commit, release version, revision, and durable audit/receipt exactly match the recorded approval; stale or mismatched approval/source/release/revision remains fail-closed."
    - "The application is reconstructed after the matching open state, and released production typed HTTP execute/query/reconcile/issuer-only/auth/Origin/CSRF contracts pass with zero fixture adapters through the same source-bound runner."
    - "The pre-open app remains unchanged, raw `/api/code/execute` remains exact authenticated/origin-aware HTTP 423, and Phase 3 GitHub/project/Git/swarm mutation remains `phase_3_project_write_gate_closed`."
    - "The final redacted Brain/Obsidian handoff records exact safe bindings, durable receipts, residual closures, and no secret or canary material."
    - "The executor runs exactly `scripts\brain.ps1 refresh` and only after success runs `scripts\brain.ps1 status`; generated repository and vault Graphify artifacts are never edited manually."
    - "Brain refresh rewrites `.planning/graphs/.last-build-snapshot.json`; ignored `graphify-out/graph.json`, optional `graphify-out/graph.html`, and `graphify-out/GRAPH_REPORT.md` remain verified non-canonical generated intermediates rather than approved-source or durable-release artifacts."
    - "A blocking human verification confirms the released typed HTTP proof, durable audit, raw/Phase 3 closures, and current Brain/Graphify handoff."
  artifacts:
    - {path: data/control.db, provides: single transactional `capability_release_state` open row plus same-transaction durable release audit and production receipts}
    - {path: .planning/graphs/graph.json, provides: released-source graph snapshot generated only by Brain refresh}
    - {path: .planning/graphs/graph.html, provides: released-source graph explorer generated only by Brain refresh}
    - {path: .planning/graphs/GRAPH_REPORT.md, provides: released-source Graphify report generated only by Brain refresh}
    - {path: .planning/graphs/.last-build-snapshot.json, provides: generated Graphify source/build snapshot rewritten by `scripts/brain.ps1 refresh`}
    - {path: C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-08-01-phase-2-execution-isolation.md, provides: final durable redacted architecture/release handoff}
    - {path: C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/Graphify Report.md, provides: vault-synced released-source Graphify report}
    - {path: C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/graph-status.json, provides: vault-synced generated graph status}
  key_links:
    - {from: .planning/phases/02-capability-policy-and-execution-isolation/02-14-SUMMARY.md, to: scripts/verify_phase2_release.py, via: exact structured approval is parsed verbatim and matched field-for-field to the immutable frozen receipt}
    - {from: docs/security/phase-2-release-evidence.json, to: src/core/capability_store.py, via: pre/post in-transaction receipt revalidation binds the only allowed `open_release_state` call}
    - {from: scripts/verify_phase2_package_provenance.py, to: scripts/verify_phase2_release.py, via: the same manifest-recorded `-I` approved-target runner verifies closure/import origins before release mode}
    - {from: src/core/capability_store.py, to: data/control.db, via: `read_release_state(tx)` plus compare-and-swap `open_release_state(tx, expected_revision=...)` and same-transaction audit}
    - {from: capability_release_state matching open row, to: src/api/server.py create_app, via: fresh app reconstruction requires exact evidence/source/release/revision equality before route registration}
    - {from: released production create_app, to: typed capability HTTP routes, via: real execute/query/reconcile/issuer-only/auth/Origin/CSRF proof with zero fixture adapters}
    - {from: scripts/brain.ps1, to: .planning/graphs/GRAPH_REPORT.md, via: exact refresh regenerates graph.json/graph.html/report and vault projections before separate status inspection}
    - {from: scripts/brain.ps1, to: .planning/graphs/.last-build-snapshot.json, via: source-verified refresh copies canonical Graphify projections then runs `node gsd-tools graphify build snapshot` before vault sync/status}
---

<objective>
Consume the exact recorded Plan 02-14 decision, transactionally open only its approved typed capability authority, reconstruct the matching production app, prove released HTTP behavior, and publish the final durable Brain/Graphify handoff.

Purpose: Turn the already-approved immutable receipt into narrowly bound production authority without any approval reconstruction, source drift, ambient evidence, raw-code opening, or Phase 3 authority expansion.
Output: Exact evidence/source/release/revision-bound open row and audit, released production HTTP proof, immutable frozen receipt, and refreshed final Brain/Obsidian/Graphify handoff.
</objective>

<execution_context>
@C:/Users/Usry/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Usry/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-CONTEXT.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-RESEARCH.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-PATTERNS.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-READINESS-AUDIT.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-VALIDATION.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-12-SUMMARY.md
@.planning/phases/02-capability-policy-and-execution-isolation/02-14-SUMMARY.md
@docs/security/phase-2-release-evidence.json
@docs/security/phase-2-release-evidence.md
@docs/security/phase-2-package-provenance.json
@scripts/verify_phase2_package_provenance.py
@scripts/verify_phase2_release.py
@src/core/control_store.py
@src/core/capability_store.py
@src/core/execution_gateway.py
@src/api/server.py
@tests/test_phase2_release_verifier.py
@tests/test_execution_gateway_integration.py
@tests/test_api_auth_matrix.py
@tests/test_origin_csrf.py
@tests/test_dashboard_execution_gate.py
@AGENTS.md
@scripts/brain.ps1
@C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/Session Template.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume the exact approval, open transactionally, and prove the reconstructed production app</name>
  <files>data/control.db</files>
  <read_first>
    - .planning/phases/02-capability-policy-and-execution-isolation/02-14-SUMMARY.md (required genuine executor output with verbatim structured approval)
    - docs/security/phase-2-release-evidence.json (immutable approved bytes)
    - docs/security/phase-2-release-evidence.md (immutable approved bytes)
    - scripts/verify_phase2_release.py
    - docs/security/phase-2-package-provenance.json
    - scripts/verify_phase2_package_provenance.py
    - src/core/control_store.py
    - src/core/capability_store.py
    - src/core/execution_gateway.py
    - src/api/server.py
    - tests/test_phase2_release_verifier.py
    - tests/test_execution_gateway_integration.py
    - tests/test_api_auth_matrix.py
    - tests/test_origin_csrf.py
    - tests/test_dashboard_execution_gate.py
  </read_first>
  <action>Do not modify source or the approved JSON/Markdown receipt. Abort before opening the store unless the genuine 02-14-SUMMARY exists, proves Plan 02-14 completed its non-auto checkpoint with release authority closed, and contains the exact verbatim `APPROVE_PHASE_2_RELEASE` record. `verify_recorded_release_decision` must parse that record without defaulting or normalizing missing values and require field-for-field equality with the immutable receipt's evidence digest, full source commit/tree/path-inventory digest, replay/validation/runtime/package/Chromium/frontend-build digests, release version, and expected closed revision. A Plan file, checkpoint prompt, conversation text, fixture, current verifier output, newly assembled record, partial approval, missing or mismatched `frontend_build_digest`, `REJECT_PHASE_2_RELEASE`, stale summary, or mismatched field is `recorded_release_decision_invalid` and performs zero store/app mutation.

Invoke release mode only as the Plan 02-14-approved source through the manifest-recorded absolute interpreter with `-I`, approved status, installed-closure verification, approved-target activation, and cryptography/Playwright origin attestation. Preserve the no-ambient-release-evidence invariant: do not use `python`, `py`, direct pytest, PATH/PYTHONPATH/user-site packages, externally supplied replay/frontend output, or any new evidence to fill the frozen receipt. Rehash the immutable JSON/Markdown bytes and their embedded Phase 1/source/replay/validation/runtime/package/browser/frontend-build bindings before entering the transaction. Permit only the exact recorded post-snapshot 02-14-SUMMARY approval receipt as control metadata; every release-affecting source/config/test/script/package/browser/frontend byte must still equal the approved inventory, and no other source/status drift is allowed.

Per D-01 through D-08, open one `ControlStore.immediate_transaction()`, call `CapabilityStore.read_release_state(tx)`, require exactly closed plus the approved expected revision, and call only `CapabilityStore.open_release_state(tx, expected_revision=..., evidence_digest=..., source_commit=..., release_version=..., ...)`. No direct SQL, hidden store API, inferred approval, or authority repair is allowed. Immediately before that call revalidate the frozen receipt/decision/source/runtime/package/browser bindings; immediately after the open transition and before commit, rehash and revalidate the same bindings again, read back the exact uncommitted row, append the redacted release audit in the same transaction, and raise on any drift so the transaction rolls back to closed. After commit, perform a fresh read-only verification of the row and audit. An exact already-open same-binding retry may return the authoritative existing receipt per D-16 but must never reopen or append a conflicting transition; stale/mismatched state, digest, source, version, or revision remains closed/non-retryable.

Reconstruct a new production `src.api.server.create_app` only after the matching open row exists and pass the immutable evidence/source/release/revision configuration; never mutate a pre-open app or register routes dynamically into it. Per D-09 through D-16, run the named real production contracts through the same source-bound approved runner: closed/unbound/mismatch route absence, reconstruction after matching open, matching-open typed execute/query/reconcile with durable receipts, issuer-only approval and zero fixture adapters, exact scope/session matrix, exact Origin/CSRF matrix, raw `/api/code/execute` authenticated/origin-aware body-agnostic HTTP 423, and execution-sink inventory proving GitHub/project/Git/swarm mutations remain `phase_3_project_write_gate_closed`. Preserve pure policy/approval, Windows Job, filesystem, browser/egress/quarantine, and idempotency/reconciliation boundaries; this plan opens only the typed capabilities approved by the receipt.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$m = Get-Content -LiteralPath 'docs/security/phase-2-package-provenance.json' -Raw | ConvertFrom-Json; $p = [string]$m.python.runtime_binding.executable; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module scripts.verify_phase2_release -- --release; if ($LASTEXITCODE) { exit $LASTEXITCODE }; &amp; $p -I scripts/verify_phase2_package_provenance.py --manifest docs/security/phase-2-package-provenance.json --scope python --require-status approved --verify-installed --activate-approved-target --require-import cryptography --require-import playwright.sync_api --run-module pytest -- -q tests/test_execution_gateway_integration.py::test_production_create_app_capabilities_absent_for_closed_unbound_or_mismatched_release tests/test_execution_gateway_integration.py::test_production_create_app_requires_reconstruction_after_matching_release_opens tests/test_execution_gateway_integration.py::test_production_create_app_serves_matching_release_execute_query_and_reconcile tests/test_execution_gateway_integration.py::test_production_create_app_keeps_issuer_approval_only_and_has_zero_fixture_adapters tests/test_api_auth_matrix.py::test_released_capability_routes_have_exact_scopes_and_auth_matrix tests/test_origin_csrf.py::test_released_capability_mutations_require_exact_origin_and_csrf tests/test_dashboard_execution_gate.py tests/test_execution_sink_inventory.py -x; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The exact recorded decision alone opens one matching release row and same-transaction audit; pre/post in-transaction and post-commit receipt checks agree; a newly reconstructed production app passes typed HTTP/receipt/reconcile/auth/Origin/CSRF proof; raw code stays 423 and Phase 3 mutations stay closed.</done>
</task>

<task type="auto">
  <name>Task 2: Write the final Brain handoff and refresh then inspect Graphify</name>
  <files>.planning/graphs/graph.json, .planning/graphs/graph.html, .planning/graphs/GRAPH_REPORT.md, .planning/graphs/.last-build-snapshot.json, C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-08-01-phase-2-execution-isolation.md, C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/Graphify Report.md, C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/graph-status.json</files>
  <read_first>
    - AGENTS.md
    - scripts/brain.ps1
    - C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/Session Template.md
    - .planning/phases/02-capability-policy-and-execution-isolation/02-14-SUMMARY.md
    - docs/security/phase-2-release-evidence.md
    - .planning/graphs/GRAPH_REPORT.md
    - C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/graph-status.json
  </read_first>
  <action>After Task 1's post-commit release-row/audit and production HTTP proofs pass, create the final redacted session handoff from the Brain template. Record the exact safe evidence/source/replay/runtime/package/browser/frontend-build/release/revision identifiers, transaction-only release record, post-open app reconstruction, named typed HTTP/receipt/reconcile/auth/Origin/CSRF proof, issuer/verifier separation, Windows/browser containment, TOOL-07 ownership, raw-code 423, and Phase 3 GitHub/project/Git/swarm closure. Include evidence paths, verification commands/results, residual risks, and next safe action. Do not include secrets, approval signatures, canary values, raw prompts/logs/provider bodies, ciphertext, private runtime data, or attacker-controlled text.

Follow the source-verified `scripts/brain.ps1` contract exactly: current `refresh` runs Graphify update at repository root, copies `graphify-out/graph.json`, `graphify-out/graph.html`, and `graphify-out/GRAPH_REPORT.md` into `.planning/graphs/`, invokes `node gsd-tools graphify build snapshot` to rewrite `.planning/graphs/.last-build-snapshot.json`, then syncs the vault projections. Before refresh, prove each named `graphify-out/` path is excluded by `.gitignore` and is not tracked; classify `graphify-out/graph.json`, `graphify-out/GRAPH_REPORT.md`, and `graphify-out/graph.html` when emitted as ignored generated intermediates only. They are not canonical outputs, approved source, or durable worktree ownership because the canonical repository copies and snapshot are the declared `.planning/graphs/` artifacts. Do not add, stage, preserve as evidence, or manually edit those intermediates. `graphify-out/graph.html` is optional when Graphify's node limit skips explorer generation; because the current script attempts the copy, an absent/skipped explorer must fail refresh rather than reuse or claim a stale canonical HTML, while JSON/report/snapshot remain mandatory. Do not modify `scripts/brain.ps1` or any approved source to work around this contract.

Run `powershell -ExecutionPolicy Bypass -File scripts/brain.ps1 refresh`; require exit zero, fresh canonical graph/report outputs, and a rewritten `.planning/graphs/.last-build-snapshot.json` whose source/build identity agrees with the released tree. Never edit generated files manually. Only after refresh succeeds, run `powershell -ExecutionPolicy Bypass -File scripts/brain.ps1 status` as a distinct command and inspect source commit, node/edge/hyperedge counts, explorer/report paths, snapshot identity, and vault projection freshness. A handoff write, refresh, snapshot, sync, or status failure leaves this task incomplete and must not be reported as current; it does not authorize changing the frozen release receipt or broadening authority.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$g = @('graphify-out/graph.json','graphify-out/graph.html','graphify-out/GRAPH_REPORT.md'); foreach ($x in $g) { git check-ignore -q -- $x; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git ls-files --error-unmatch -- $x 2&gt;$null; if (-not $LASTEXITCODE) { exit 1 } }; $started = [DateTime]::UtcNow; powershell -ExecutionPolicy Bypass -File scripts/brain.ps1 refresh; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $snapshot = Get-Item -LiteralPath '.planning/graphs/.last-build-snapshot.json' -ErrorAction Stop; if ($snapshot.LastWriteTimeUtc -lt $started -or $snapshot.Length -le 0) { exit 1 }; powershell -ExecutionPolicy Bypass -File scripts/brain.ps1 status; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The final Brain session is redacted and current; exact `scripts\brain.ps1 refresh` succeeds before separate `status`; canonical repository/vault Graphify projections and `.planning/graphs/.last-build-snapshot.json` are freshly generated from released source; the three named `graphify-out/` paths are verified ignored/untracked intermediates (with HTML optional only when Graphify skips it and refresh therefore fails rather than claiming stale HTML); the frozen receipt is unchanged and deferred authority remains closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the released authority and final durable handoff</name>
  <files>data/control.db, .planning/graphs/graph.json, .planning/graphs/graph.html, .planning/graphs/GRAPH_REPORT.md, .planning/graphs/.last-build-snapshot.json, C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-08-01-phase-2-execution-isolation.md, C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/Graphify Report.md, C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/graph-status.json</files>
  <what-built>An exact recorded-decision release transaction including the approved frontend-build digest, post-transition binding checks, reconstructed production typed HTTP proof, durable audit/receipts, and refreshed Brain/Obsidian/Graphify handoff with a rewritten build snapshot.</what-built>
  <how-to-verify>
    1. Inspect the release receipt and audit: evidence/source/frontend-build/release/revision must exactly match the 02-14 structured decision, and the approved JSON/Markdown bytes must be unchanged.
    2. Confirm the production app was reconstructed after opening and the named typed execute/query/reconcile/issuer-only/auth/Origin/CSRF tests passed through the manifest-recorded source-bound runner with zero fixture adapters.
    3. Confirm `/api/code/execute` is still exact authenticated/origin-aware HTTP 423 and every GitHub/project/Git/swarm mutation remains `phase_3_project_write_gate_closed`.
    4. Inspect the redacted Brain session, `.planning/graphs/.last-build-snapshot.json`, and Graphify status; refresh must precede status, the snapshot must identify the released source/build, repository/vault projections must be current, and the three named `graphify-out/` paths must remain ignored/untracked intermediates rather than canonical outputs.
  </how-to-verify>
  <action>Present the immutable 02-14 receipt comparison, release-row/audit identifiers, named production HTTP results, raw and Phase 3 closed proofs, redacted Brain session, and refresh-before-status output for final human inspection. Do not reopen authority, reconstruct the 02-14 decision, modify the frozen evidence, rerun refresh, or broaden any route while waiting; a reported mismatch keeps this plan incomplete for correction within the already approved scope.</action>
  <verify>
    <automated>powershell -NoProfile -Command "powershell -ExecutionPolicy Bypass -File scripts/brain.ps1 status; exit $LASTEXITCODE"</automated>
    <human-check>Approve only if the durable release/audit including `frontend_build_digest`, reconstructed production HTTP proof, raw and Phase 3 closures, frozen receipt bytes, redacted Brain session, canonical Graphify outputs, rewritten snapshot, ignored intermediate classification, and freshness all exactly agree.</human-check>
  </verify>
  <resume-signal>Type `approved` or describe the exact mismatch; this checkpoint cannot create or reconstruct the Plan 02-14 release approval.</resume-signal>
  <done>Human verification confirms the exact approved typed authority is live and auditable, the approved receipt including checked-frontend evidence is immutable, raw/Phase 3 authority remains closed, the snapshot is current, and Graphify intermediates remain non-canonical.</done>
</task>

</tasks>

## Artifacts this phase produces

- `data/control.db`: the single evidence/source/release/revision-bound open release row, same-transaction redacted audit, and durable production receipts.
- `.planning/graphs/graph.json`, `.planning/graphs/graph.html`, `.planning/graphs/GRAPH_REPORT.md`, and `.planning/graphs/.last-build-snapshot.json`: canonical generated repository Graphify projection and source/build snapshot produced only by Brain refresh.
- `graphify-out/graph.json`, optional `graphify-out/graph.html`, and `graphify-out/GRAPH_REPORT.md`: ignored, untracked Graphify intermediates verified as excluded from canonical ownership; they are neither approved source nor durable release artifacts and are never staged or manually edited.
- `C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Sessions/2026-08-01-phase-2-execution-isolation.md`: final redacted durable release handoff.
- `C:/Users/Usry/OneDrive/Documents/Codex Agent Brain/Projects/Jarvis/Graph/Graphify Report.md` and `graph-status.json`: generated vault Graphify projection and status.
- `02-15-SUMMARY.md`: executor output containing the exact consumed 02-14 approval identity, transactional open receipt, released HTTP proof, durable audit, and final human verification.

## Interfaces consumed without source modification

- `scripts.verify_phase2_release.release_from_verified_bundle`: source-approved Plan 02-14 entrypoint; accepts only the genuine exact 02-14-SUMMARY decision and immutable receipt.
- `scripts.verify_phase2_release.revalidate_frozen_release_receipt`: rehashes approved bytes and source/runtime/package/browser bindings before and after the in-transaction transition.
- `CapabilityStore.read_release_state(tx)` / `open_release_state(tx, expected_revision=...)`: Plan 02-12 transaction-only release-state APIs.
- `src.api.server.create_app`: Plan 02-12 production reconstruction boundary that registers typed routes only for exact matching open bindings.
- `scripts\brain.ps1 refresh` then `scripts\brain.ps1 status`: source-verified generated graph and vault synchronization contract.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plans | Status |
|---|---|---|---|---|
| GOAL | Phase 2 | deterministic, exact, bounded, auditable local/browser capability execution | 01-15 | COVERED |
| REQ | CTRL-02, CTRL-03, CTRL-06 | exact policy/approval/containment enforced at released production boundary | 01, 03, 07-15 | COVERED |
| REQ | TOOL-03, TOOL-05, TOOL-07 | released typed local/browser actions plus receipts/reconciliation | 02-06, 08-15 | COVERED |
| CONTEXT | D-01..D-08 | exact policy/approval identity, issuer separation, atomic state transition, protected verification | 01, 05-09, 12, 14, 15 | COVERED |
| CONTEXT | D-09..D-16 | typed adapters, no shell, OS/browser/egress/quarantine bounds, idempotent reconciliation | 02-06, 08-15 | COVERED |
| RESEARCH | T-01..T-25 | hostile controls remain required by immutable approved receipt | 01-04, 07-15 | COVERED |
| RESEARCH | no-ambient release and source freshness | same source-bound manifest runner plus immutable 02-14 receipt before/after opening | 06, 10, 14, 15 | COVERED |
| RESEARCH | production release proof | exact transaction, reconstructed matching app, typed HTTP proof, raw/Phase 3 closure | 12-15 | COVERED |
| RESEARCH | durable knowledge handoff | final redacted Brain session and exact refresh then status | 15 | COVERED |

Deferred Phase 3 queue/lease/restart orchestration, GitHub/project/Git/swarm mutation authority, Phase 4 planner/verifier separation, and later Brain shared-pack/provider/UI/voice/operations work remain excluded exactly as CONTEXT.md directs.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| 02-14-SUMMARY -> release verifier | A human decision crosses into consequential authority and must remain exact, genuine, and non-reconstructible. |
| frozen receipt/source/package/browser -> release transaction | Approved immutable inputs can drift between verification and mutation. |
| verifier -> ControlStore transaction | Only the transaction-bound store API may change the singleton release row and audit. |
| open row -> reconstructed production app | Cached or mismatched authority must not register or dispatch typed routes. |
| released app -> HTTP client | Auth, Origin/CSRF, policy, approval, receipt, and Phase 3 boundaries remain untrusted. |
| released repository -> Brain/Graphify vault | Generated knowledge artifacts must be current, redacted, and source-derived. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-15-01 | Spoofing/Elevation | recorded release decision | mitigate | Require genuine 02-14-SUMMARY and exact field equality; reject prompts/plans/fixtures/current output and every reconstructed or partial approval. |
| T-02-15-02 | Tampering/Repudiation | frozen receipt and source bindings | mitigate | Keep JSON/Markdown immutable and revalidate bytes plus source/runtime/package/browser bindings immediately before and after the uncommitted transition. |
| T-02-15-03 | Tampering/Elevation | release state transaction | mitigate | Use one immediate transaction, expected closed revision, transaction-only store APIs, same-transaction audit, rollback on drift, and exact post-commit read. |
| T-02-15-04 | Elevation | app reconstruction | mitigate | Reconstruct after matching open; pre-open app remains absent; gateway re-reads release truth; named production tests use zero fixture adapters. |
| T-02-15-05 | Elevation | raw/deferred routes | mitigate | Keep `/api/code/execute` exact 423 and prove GitHub/project/Git/swarm mutation paths return the stable Phase 3 closed code before sinks. |
| T-02-15-06 | Repudiation/Information Disclosure | Brain/Graphify handoff | mitigate | Redacted template handoff; source-verified refresh order; declared snapshot ownership; ignored/untracked classification for the three `graphify-out/` intermediates; fail on stale/absent canonical copies; separate status and human freshness inspection. |
| T-02-SC | Tampering | approved Python/Chromium supply chain | mitigate | Same manifest absolute `-I` runner, installed-closure/import-origin checks, immutable approved hashes, and no install/ambient fallback. |

Residual risks remain intentionally closed: Phase 3 owns queue/lease/restart and GitHub/project/Git/swarm mutation; later phases own planner/verifier, provider/UI/voice, shared Brain packs, and operations. Raw code execution remains permanently outside the released typed capability surface.
</threat_model>

<verification>Plan completion requires the genuine exact 02-14 decision including `frontend_build_digest` and immutable receipt, same source-bound attested runner, immediate pre/post in-transaction and post-commit binding checks, one exact release row/audit, fresh application reconstruction, named released production typed HTTP/receipt/reconcile/issuer/auth/Origin/CSRF proofs, raw route 423, Phase 3 sink closure, redacted Brain handoff, verified ignored/untracked `graphify-out/` intermediates, and exact `scripts\brain.ps1 refresh` that rewrites `.planning/graphs/.last-build-snapshot.json` followed by separate `status`. No approved source, evidence, approval, install, or package/browser/frontend bytes are changed or reconstructed in this plan.</verification>

<success_criteria>Ahmed can invoke only the exact human-approved typed local/browser capabilities through a reconstructed evidence/source/release/revision-matched production app; stale or mismatched approval stays closed, receipts/audit are durable, raw and Phase 3 authority remain closed, and the Brain/Graphify handoff is current.</success_criteria>

<output>Create `.planning/phases/02-capability-policy-and-execution-isolation/02-15-SUMMARY.md` when done, including the exact release-row/audit identifiers, production HTTP proof, immutable-receipt check, and Brain/Graphify status.</output>
