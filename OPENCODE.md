# OpenCode Entry Point

Read `AGENTS.md`, then `docs/KNOWLEDGE_SYSTEM.md`. Use `scripts/brain.ps1 status` before substantial work and write a concise session handoff to the shared Jarvis vault when finished.
